---
context: default
phase: null
task: null
total_tasks: null
status: in_progress
last_updated: "2026-07-02T11:36:31.815Z"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints. Nothing failed this session — it was a short, clean operational
session (two bug fixes, one config update, all verified live). The advisory anti-patterns
below are carried forward from the previous session and remain valid._

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Shared dev/test Postgres | `DEV_DSN` (used by test fixtures across the repo) is the SAME live Postgres the running docker-compose stack uses — not an isolated test DB. | advisory | Before running any Postgres-touching test file, confirm its postgres-param variants carry the real `pytest.mark.db_live` marker (fixed in the two known files, commit `7fd0023`, but new test files could reintroduce it). Architectural fix (separate test DB) still outstanding. |
| Live-consumer test contention | `tests/test_bus_rabbitmq.py` / `tests/test_bus_consume.py` share `q.triage` with the production `infotriage-triage` worker — worker steals test messages, false failures. | advisory | Stop the `triage` container before RabbitMQ-consumer tests, restart after. |
| Silent yt-dlp failures | `yt_dlp_list` used to swallow non-zero yt-dlp exits as "empty channel" — dead channel handles (@bellingcat 404) went unnoticed. FIXED this session (`0c6d75e`, stderr now surfaced), noted so empty feeds are read as real signal now. | advisory | If a channel feed is empty, check container stderr — a real yt-dlp error will now be printed there. |

<current_state>
Between phases. Phase 5 (Triage app) fully closed (UAT/SECURITY/REVIEW/VERIFICATION/
VALIDATION all committed). Phase 6 (Brief app) not started — no CONTEXT.md, no PLAN.md.

This session was purely operational: both pending youtube-ingest bugs from the prior
handoff were fixed, committed (`0c6d75e`), deployed, and live-verified; the YT_CHANNELS
config was updated per user request. System healthy end-to-end.
</current_state>

<completed_work>

1. **max_n key mismatch fixed** — `ingest()` read `c.get("max_per_run", 3)` but
   `YT_CHANNELS` uses `"max_n"`; per-channel limits were silently ignored. Regression
   test added and verified to fail on pre-fix code (via git stash).
2. **yt-dlp over-fetch root-caused and fixed** — bare channel-root URLs expand to up to
   3 tab playlists (Videos/Shorts/Live) and `-I 1:N` applies PER TAB (NATO returned 15
   for max_n=5; confirmed via `%(playlist_title)s`: 3 tabs × 5). Fixed with
   `_videos_tab_url()` pinning bare roots to `/videos`; tab-suffixed URLs untouched.
3. **yt-dlp error surfacing** — non-zero exits were parsed as "empty channel"; stderr
   tail now printed. This is how the dead handles below were discovered.
4. All three in commit `0c6d75e`; container rebuilt; live-verified NATO returns
   exactly 5. Test suite 7/7 (`tests/test_ingest_youtube.py`, `-m "not db_live"`).
5. **YT_CHANNELS updated in `.env`** (user request; .env not git-tracked, backup at
   `.env.bak-channels`): `@bellingcat` → `@BellingcatOfficial` (verified real channel);
   dropped `@theisw` (yt-dlp lists NO tabs/videos despite curl 200) and `@NRKnyheter`
   (404). Fresh run verified: zero errors, exact counts per channel (Bellingcat 10,
   NATO 5, Karpathy 5, NVIDIA 5). Postgres now has 45 `source_type='yt'` articles.
6. Prior HANDOFF.json/.continue-here.md consumed and removed; STATE.md updated
   (commits `19c8be2`, `fe59b49`).
</completed_work>

<remaining_work>

- **CR-01** (`05-REVIEW.md`): `RabbitMQBus._rebuild_topology()` deletes all 5 production
  queues on any AMQP 406, no dev/prod gate. Advisory but real data-loss risk.
- **PostgresStore reconnect-on-error** for the triage worker (mirror RabbitMQ's
  `connect_robust`) — the poisoned-connection incident recurs on any Postgres hiccup.
- **Separate test DB** — the shared `DEV_DSN` architecture (see anti-patterns) is unfixed.
- **Stale artifacts**: `data/feeds/youtube-isw.xml`, `youtube-nrk-nyheter.xml`,
  `youtube-youtubeofficial.xml` linger for dropped/old channels; FreshRSS may still
  subscribe. `.env.bak-channels` can be deleted once the channel list is confirmed.
- **Phase 6 (Brief app)** — not started. `/gsd-discuss-phase 6` is the entry point.
</remaining_work>

<decisions_made>

- Pinned yt-dlp to the `/videos` tab (skip Shorts/Live) rather than truncating the merged
  list — root cause was per-tab `-I 1:N`; Videos-only is deterministic and shorts-free.
- Dropped BOTH `@theisw` and `@NRKnyheter`: the user's "drop @…" message was mangled by a
  path autocomplete and the clarifying question timed out (AFK). Both were verifiably
  broken, so dropping both was safe; old line in `.env.bak-channels` for rollback.
  **Confirm with user** — exact intent never verified.
- Left legacy `apps/ingest/yt_to_atom.py` on `max_per_run` (not deployed, internally
  consistent, out of scope).
- Did not touch FreshRSS subscriptions / stale feed XMLs (user-facing decision).
</decisions_made>

<blockers>
- None blocking. Carried advisories: CR-01, worker Postgres reconnect, shared DEV_DSN.
</blockers>

## Required Reading (in order)
1. This file — complete session narrative
2. `.planning/HANDOFF.json` — same state, machine-readable
3. `.planning/phases/05-triage-app/05-REVIEW.md` — CR-01 detail, if picking that up

## Infrastructure State
- Full docker-compose stack up + healthy: postgres, rabbitmq, triage (processing),
  ingest-imap, ingest-youtube (rebuilt this session, running `0c6d75e`), freshrss,
  rss-bridge, feeds.
- `infotriage.articles`: 67+ rows total, 45 with `source_type='yt'`. DLQ: 0.
- `.env` modified (YT_CHANNELS — 4 channels now); backup `.env.bak-channels` untracked.
- Working tree clean except untracked `.env.bak-channels` and pre-existing
  `.planning/research/` (not mine — do not touch without asking).

<context>
Short, clean operational session. Both youtube bugs from the prior handoff were
fixed with live verification at each step (container rebuild → real yt-dlp runs →
exact per-channel counts). The one judgment call was dropping both broken channels
when the user's message was truncated — flagged in human_actions_pending rather
than silently absorbed.
</context>

<next_action>
If the user is present: confirm the YT_CHANNELS list (both @theisw and @NRKnyheter
dropped — right call?). Then either `/gsd-discuss-phase 6` (Brief app, natural next
step) or pick up CR-01 / the Postgres reconnect advisory. No infrastructure setup
needed — everything is running.
</next_action>
