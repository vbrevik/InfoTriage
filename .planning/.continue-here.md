---
context: default
phase: 12-cnr-alerting-dissemination
task: 3
total_tasks: 3
status: in_progress_checkpoint
last_updated: "2026-08-02T16:30:00.000Z"
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_Phase 12 Plan 09 is checkpointed at Task 3 — a blocking **operator** human-verify gate.
Nothing automated can settle it; the phase seals only when the operator types "approved"._

## Current State (2026-08-02)

- **Phase 12 (CNR alerting / dissemination) is at its FINAL plan (12-09), W5.** Plans
  12-01..12-08 are complete (tracer, alert_state substrate, ntfy bearer-token ACL +
  ADR-018 pre-bake, dual-trigger dedupe, throttle + hourly digest, outbox retry/DLX,
  Item.body contract + store path, 6/7 adapter body-population).
- **12-09 Tasks 1-2 shipped 2026-08-02:** `tests/test_alerting_prohibitions.py` (9
  structural guards: P1-P5 + AC8) + ADR-015 reconciliation amendment. Full
  `make -f ops/Makefile test-safe` → **811 passed / 0 failed / 0 skipped**.
- **Task 3 = operator UAT, OPEN.** Checklist in
  `.planning/phases/12-cnr-alerting-dissemination/12-09-SUMMARY.md` (Task 3 section):
  ntfy-up + `docker compose up -d --wait alerting` → `/health` 200 → trigger a real CAT I
  alert (exactly one push, no SAB re-fire) → tap-through (A2 vault name) → confirm the two
  link fields (A-01) → force a digest and check grouping → P5 SAB-stays-canonical judgment.
  Type "approved" to seal Phase 12 — the last open plan.
- **Debt (backlog):** 999.2 (dedup threshold), 999.5 (test-infra), 999.6 (Trace Labs),
  999.7 (ACLED adapter, gated on Q3), 999.8 (freshrss/rssbridge non-loopback exposure,
  opened 2026-08-02 by the P1 guard).
- **Known gaps:** `.env.example` now documents the 5 alerting keys (NTFY_TOKEN etc.) —
  operator must still mint NTFY_TOKEN via `make -f ops/Makefile ntfy-token`; outbox.py
  X-Title ASCII-safety fixed 2026-08-02 (was latent since 12-01).

## Advisory Anti-Patterns (carried, still valid)

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Live-consumer test contention | `tests/test_bus_rabbitmq.py` / `tests/test_bus_consume.py` share queues with running production containers — worker steals test messages. | advisory | Stop relevant containers before RabbitMQ-consumer tests, restart after. |
| TIME-BOMB tests | Any test hardcoding a calendar `datetime`/`date` goes stale silently; only `make test-safe` catches it. | advisory | Use relative offsets (`now - X`) or parameterize; grep for hardcoded dates in new fixtures. |
| STATE.md format | `gsd-tools query state.record-session` clobbers this project's custom STATE.md frontmatter. | advisory | Hand-edit STATE.md; never run the state.* SDK verbs against it. |

## Next Action

Operator completes the 12-09 Task 3 UAT checklist and types **"approved"** to seal Phase
12 (then `STATE.md`/`ROADMAP.md`/`12-VALIDATION.md` close-out). Optional follow-ups:
promote a backlog item via `/gsd-review-backlog`, or decide the freshrss/rssbridge
retightening (Phase 999.8). Full machine-readable state: `.planning/HANDOFF.json`.
