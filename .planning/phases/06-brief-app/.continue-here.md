---
context: phase
phase: 06-brief-app
task: 3 of 3 (plan 06-01)
total_tasks: 3
status: in_progress
last_updated: 2026-07-05T16:13:31.152Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.

- [ ] **SUMMARY.md contains false completion claims** — 06-01-SUMMARY.md says main.py, Dockerfile, tests/test_brief_app.py, libs/contracts updates were created. NONE of these exist on disk. The previous executor (subagent bg_da478432) ran for 52 minutes then stalled without producing code. The summary was written by a different run that hallucinated completions.

**Severity:** blocking — Do NOT treat phase 6 as complete. Re-verify against actual disk state.

**Prevention:** Before writing any SUMMARY.md, run `git diff --stat` and check actual files exist on disk, not just that the plan frontmatter claims they should.

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Summary written before files exist | 06-01-SUMMARY.md was created with false completion claims (main.py, Dockerfile listed as created when they weren't) | blocking | Verify `ls` output of claimed artifacts BEFORE writing SUMMARY.md |
| Subagent executor stalls silently | bg_da478432 ran 52 minutes with no output, no error, no files | advisory | Check `background_output` for task completion status; if agent ran >30 min without output, interrupt and retry inline |
| Plan references files that don't exist | Plan 06-01 claims SabPublished needs to be added to _events.py, but it already existed with richer fields | advisory | Read actual source files before executing tasks that assume new files need creating |

<current_state>
Phase 6 (brief-app), Plan 06-01, Task 3 of 3.

What the plan says should exist vs actual disk state:

| Claimed Artifact | Actual Status |
|-----------------|---------------|
| libs/contracts/src/contracts/_events.py (SabPublished) | ✅ EXISTS — but with richer schema than plan specified (pre-existing from Phase 4/5) |
| apps/brief/__init__.py | ✅ EXISTS — empty package marker |
| apps/brief/renderer.py | ✅ EXISTS — 14KB, all 4 functions implemented (23 tests pass) |
| apps/brief/consumer.py | ✅ EXISTS — 8KB, RabbitMQ verdict.ready consumer (bonus, not in plan) |
| tests/test_brief_renderer.py | ✅ EXISTS — 23 contract tests, all passing |
| libs/contracts/tests/test_events.py | ❌ DOES NOT EXIST — plan Task 1 says to add SabPublished tests here |
| apps/brief/main.py | ❌ DOES NOT EXIST — FastAPI server for port 22040 |
| apps/brief/html_renderer.py | ❌ DOES NOT EXIST — Wave 2 HTML output |
| apps/brief/clustering.py | ❌ DOES NOT EXIST — Wave 2 pgvector clustering |
| apps/brief/Dockerfile | ❌ DOES NOT EXIST |
| docker-compose.yml (updated) | ❌ NEEDS CHECK |
| libs/contracts/src/contracts/_events.py (updated with item_count/slide_count) | Partially exists — SabPublished exists but may not have plan-specified fields |

**Key insight:** The plan's 3 tasks map to:
- Task 1: SabPublished event schema (mostly done, but test file missing)
- Task 2: Package structure + renderer stubs (done — both files exist)
- Task 3: Renderer implementation + contract tests (done — 23 tests pass)

What's genuinely missing:
- Task 1 contract test (test_events.py doesn't exist)
- Wave 2 items: main.py, html_renderer.py, Dockerfile, docker-compose entry

The 06-01-SUMMARY.md incorrectly claims Wave 2 items are complete. It needs to be corrected.
</current_state>

<completed_work>

Task 1: Add SabPublished event schema to libs/contracts
- ✅ SabPublished already existed with richer schema (pub_ts, snapshot_day, ccir_topics, bluf_by_topic, item_refs, total_keep, since_ts)
- ✅ ContractEvent union already includes SabPublished
- ⚠️ test_events.py does NOT exist — plan says to add test_sab_published_roundtrip

Task 2: Create apps/brief/ package structure and renderer.py stubs
- ✅ apps/brief/__init__.py created (empty package marker)
- ✅ apps/brief/renderer.py created with all 4 function signatures (render_brief, render_list, render_bluf, render_cluster)

Task 3: Implement render_brief, render_list, render_bluf, render_cluster
- ✅ All 4 functions implemented with correct behavior
- ✅ 23 contract tests created and all passing
- ✅ CNR ordering verified (CAT I → CAT II → Routine)
- ✅ CCIR_ORDER ordering verified
- ✅ LLM failure → placeholder text, items not dropped
- ✅ Score threshold filtering verified (score >= 8)
- ✅ render_bluf uses same prompt template frame as digest.py write_bluf()
</completed_work>

<remaining_work>

- Task 1 (residual): Create libs/contracts/tests/test_events.py with test_sab_published_roundtrip
- Wave 2: Create apps/brief/main.py — FastAPI SAB server on port 22040
- Wave 2: Create apps/brief/html_renderer.py — HTML SAB output
- Wave 2: Create apps/brief/Dockerfile — slim Python container build
- Wave 2: Update docker-compose.yml — brief app service entry
- Correct 06-01-SUMMARY.md to reflect actual completion state (Wave 2 items incomplete)
</remaining_work>

<decisions_made>

- FastAPI serving: Phase 6 delivers a lightweight FastAPI server for SAB viewing (port 22040) with markdown rendering. HTML templating deferred to Wave 2.
- Event-driven architecture: Consumer subscribes to RabbitMQ verdict.ready events, writes to data/digests/ directory.
- Renderer as library: renderer.py is a pure library — no dependencies on HTTP or Docker. Tests can run in memory.
- CNR priority mapping: I → "CAT I first", II → "CAT II", none → "Routine"
- BLUF uses digest.py's write_bluf() prompt template frame for consistency
- SabPublished already existed with richer schema — plan adapted to reality rather than duplicating
</decisions_made>

<blockers>

- 06-01-SUMMARY.md contains false completion claims — needs correction before phase can be considered complete
- No tests for SabPublished event schema roundtrip in test_events.py (file doesn't exist)
</blockers>

## Required Reading (in order)

1. `.planning/phases/06-brief-app/06-01-PLAN.md` — full plan with acceptance criteria
2. `.planning/phases/06-brief-app/06-CONTEXT.md` — design decisions D-01 through D-14
3. `apps/brief/renderer.py` — current implementation to verify against
4. `apps/triage/digest.py` — reference for render functions (write_brief, write_bluf, write_list, write_cluster, CCIR_ORDER)
5. `apps/triage/worker.py` — reference for RabbitMQ consumer pattern
6. `libs/contracts/src/contracts/_bus_rabbitmq.py` — RabbitMQ topology and routing keys
7. `apps/ingest-imap/Dockerfile` — reference for container build pattern

## Critical Anti-Patterns (do NOT repeat these)

- **Summary before files**: Do NOT write SUMMARY.md before verifying files actually exist on disk. Read `git status` output first.
- **Assume plan = reality**: Plan was written before executor ran — always verify plan claims against actual codebase state.
- **Subagent stalls**: Long-running background agents (52+ min) that produce no output have likely stalled. Interrupt and retry inline.

## Infrastructure State

- RabbitMQ: INFOTRIAGE_AMQP_DSN configured, queue mapping verified (verdict.ready → q.brief)
- Postgres: INFOTRIAGE_PG_DSN configured, store available
- FastAPI/uvicorn: Available via conda (fastapi 0.115.0)
- Python 3.13: System Python

## Pre-Execution Critique Required

- Design artifact: `apps/brief/renderer.py` (Wave 1 complete)
- Design artifact: `apps/brief/consumer.py` (bonus, not in plan)
- Critique focus: Does the renderer.py implementation match the plan's acceptance criteria exactly? Does consumer.py correctly subscribe to the right queue?

## Context

Phase 6 brief-app was executing Plan 06-01 (Wave 1). The plan has 3 tasks, and all 3 were executed inline (not via background agent after the first attempt failed). The renderer.py and contract tests are solid — 23 tests all pass. The consumer.py was created as a bonus beyond the plan scope.

The main blocker is the false SUMMARY.md. It claims Wave 2 items (main.py, Dockerfile, tests/test_brief_app.py) are complete when they don't exist.

Next action: 
1. Fix 06-01-SUMMARY.md to reflect actual state
2. Create test_events.py with SabPublished roundtrip test
3. Create Wave 2 files: main.py, html_renderer.py, Dockerfile, docker-compose entry
4. Re-verify all acceptance criteria against actual code

## Next Action

Start with: Fix 06-01-SUMMARY.md to remove false completion claims, then create libs/contracts/tests/test_events.py with SabPublished roundtrip test.
