---
context: phase
phase: 06-brief-app
task: UAT verification
total_tasks: 9
status: in_progress
last_updated: 2026-07-09T19:02:03Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: Semantic clustering threshold 0.75 is too high for current embeddings** — Cosine similarities between FFIR-3 items range 0.72-0.82; threshold 0.75 means only ~50% of pairs merge. Must calibrate on larger corpus or lower threshold.

- [ ] **CONSTRAINT: HTML renderer still uses keyword clustering** — `sab_html.py::cluster()` uses keyword overlap, not semantic embeddings. Must integrate semantic clustering or monkey-patch.

- [ ] **CONSTRAINT: db_live tests require INFOTRIAGE_TEST_DSN** — Never run bare `pytest` without setting INFOTRIAGE_TEST_DSN; test fixtures target isolated test DB on port 22062. Production DSN (port 22000) must never be used in tests.

- [ ] **CONSTRAINT: pgvector.Vector not iterable** — Use `.to_list()` to convert pgvector.Vector to list[float] before passing to Python math. Fixed in `clustering.py::_as_list()`.

**Do not proceed until all boxes are checked.**

---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Hardcoded prod DSN in tests | `DEV_DSN = 'postgresql://...:22000'` in test files | blocking | Guard test `test_dsn_safety.py` scans for prod DSN literals |
| pgvector.Vector not iterable | `list(Vector)` fails; use `.to_list()` | blocking | `_as_list()` helper in `clustering.py` handles Vector→list |
| Threshold 0.75 too high | Items with 0.72-0.82 similarity don't merge | advisory | Calibrate threshold on larger corpus; consider 0.70-0.72 |
| Keyword clustering in HTML | `sab_html.py::cluster()` uses keyword overlap | blocking | Replace with semantic clustering or monkey-patch |
| Test fixtures wipe prod DB | `_truncate_all` on prod DSN wipes live data | blocking | Fixtures now bootstrap `init_schema()` before TRUNCATE |

---

## Current State

**Phase:** 06-brief-app (Brief app — SAB renderer + Obsidian vault-writer)  
**UAT Status:** `testing` — 9 tests defined, 0 passed, 0 issues  
**Progress:** All 6 plans (06-01 through 06-06) have SUMMARY.md; UAT started but needs re-run after gap-closure fixes  
**Last Verified:** 2026-07-09 — container healthy, /health returns 200, /sab renders but clusters not merging

---

## Completed Work

### Phase 6 Plans — All Complete

| Plan | Description | Status | Key Commits |
|------|-------------|--------|-------------|
| 06-01 | Wave 1 renderer + Wave 2 serving layer | ✅ Complete | 316a20f, 01ed73c, af9dcac |
| 06-02 | pgvector semantic clustering | ✅ Complete | 3047116 |
| 06-03 | Clustering data-flow gap closure | ✅ Complete | e6fab90 |
| 06-04 | Vault-writer gap closure | ✅ Complete | e6fab90 |
| 06-05 | Test-DSN safety | ✅ Complete | 2d6c255, 6fecb46, f92f8ed |
| 06-06 | Store txn hygiene | ✅ Complete | 4f16534, 53d2174, ec89812 |

### Key Fixes Applied

1. **Clustering data-flow (06-03)**: `consumer.py` and `main.py` now JOIN `infotriage.embeddings`; renderer no longer defaults to `[0.0]*4`
2. **Vault-writer (06-04)**: New `apps/brief/vault_writer.py` with front-matter codec, email inclusion, wikilinks
3. **Test-DSN safety (06-05)**: `INFOTRIAGE_TEST_DSN` only; guard test `test_dsn_safety.py`; ephemeral tmpfs test DB on :22062
4. **Store txn hygiene (06-06)**: `rollback()` after every read method; `idle_in_transaction_session_timeout=300000` backstop

---

## Remaining Work

### UAT Verification (Phase 6)
**9 tests pending** — all need re-execution after gap-closure fixes:

| # | Test | Expected | Status |
|---|------|----------|--------|
| 1 | Cold Start Smoke Test | docker compose up -d → /health 200 | pending |
| 2 | View SAB /sab endpoint | HTML with CNR, CCIR clusters, timestamp | pending |
| 3 | Semantic clustering | ≥1 CCIR section with multi-item merge | pending |
| 4 | Vault writer .md files | .md files in brief-outbox with valid front-matter | pending |
| 5 | Email in vault | imap:// items in vault by default | pending |
| 6 | CLUSTER_THRESHOLD validation | ValueError on out-of-range; default 0.75 | pending |
| 7 | Event-driven rendering | verdict.ready → 4 digests + sab.published | pending |
| 8 | Semantic clustering on real data | pgvector merges similar items per CCIR | pending |
| 9 | CLUSTER_THRESHOLD configurable | Env var validated, flows to renderer | pending |

### Known Issues to Verify Fixed
1. **Semantic clustering threshold** — Current 0.75 may be too high (similarities 0.72-0.82)
2. **HTML clustering** — `sab_html.py` still uses keyword clustering; need semantic integration
3. **Vault writer live verify** — Compose mount `OBSIDIAN_VAULT_PATH` needed

---

## Blockers

---

## Decisions Made

- **Clustering approach**: pgvector HNSW greedy centroid assignment per CCIR section (not keyword overlap)
- **Vault-writer scope**: Interim heuristic wikilinks (proper nouns + known topics); full entity resolution deferred to Phase 8
- **Test-DSN pattern**: `INFOTRIAGE_TEST_DSN` only, no fallback, always-run guard test
- **Idle-in-transaction**: 5-min server-side backstop + explicit `rollback()` after reads
- **Threshold config**: `CLUSTER_THRESHOLD` env var (0.0-1.0), default 0.75, validated in `main.py`

---

## Blockers

None currently — all gap-closure plans executed and committed.

---

## Required Reading (in order)

1. `.planning/phases/06-brief-app/06-06-SUMMARY.md` — Store txn hygiene summary
2. `.planning/phases/06-brief-app/06-05-SUMMARY.md` — Test-DSN safety summary
3. `.planning/phases/06-brief-app/06-03-SUMMARY.md` — Clustering data-flow fix
4. `.planning/phases/06-brief-app/06-04-SUMMARY.md` — Vault-writer summary
5. `.planning/phases/06-brief-app/06-UAT.md` — Current UAT test definitions
6. `.planning/phases/06-brief-app/06-VERIFICATION.md` — Pre-gap-closure verification (historical)

---

## Critical Anti-Patterns (do NOT repeat)

| ANTI-PATTERN | Manifestation | Structural Mitigation |
|--------------|---------------|----------------------|
| Writing SUMMARY before verifying artifacts | 06-01 SUMMARY claimed deliverables not on disk | `git status` + `ls` first; never trust agent output |
| Running tests against prod DB | `DEV_DSN = prod :22000` in test fixtures | `test_dsn_safety.py` guard; `INFOTRIAGE_TEST_DSN` only |
| Monkey-patch without verification | HTML renderer monkey-patch didn't apply | Test monkey-patch in isolation before integrating |
| Threshold too high | 0.75 excluded 0.72-0.82 similarity pairs | Calibrate on held-out corpus; log merge decisions |
| Monkey-patch not applied | `sab_html.cluster` called directly, not via module | Modify module function, not local reference |

---

## Infrastructure State

| Service | Status | Notes |
|---------|--------|-------|
| postgres (prod) | running | port 22000, healthy |
| postgres (test) | tmpfs | port 22062, ephemeral |
| rabbitmq | running | port 22001 |
| freshrss | running | port 22008 |
| triage | running | port 22030 |
| brief | running | port 22040, healthy |
| ingest-* | running | imap, youtube, obsidian, gmail, scheduler |

---

## Pre-Execution Critique Required

**Next step after UAT**: Phase 7 (Ops + cutover) — requires design critique before execution.

- **Design artifact**: `.planning/phases/07-ops-cutover/` (to be created)
- **Critique focus**: Healthcheck design, DLQ strategy, structured logging, host path retirement
- **Gate**: Do NOT begin Phase 7 execution until critique complete and design revised

---

## Context

**Mental model**: Phase 6 gap-closure complete. All 6 plans executed with SUMMARY.md. UAT needs re-run to confirm gap closures actually work on live data. Key technical debt: semantic clustering threshold calibration and HTML renderer integration. Ready for UAT re-run → if all pass, Phase 6 complete → Phase 7 planning.

**What I was doing**: Finalized gap-closure plans (06-03 through 06-06), committed all changes, created fresh UAT.md with 9 comprehensive tests. Next step is running UAT tests manually against live stack.

---

## Next Action

Start with: **Run UAT Test 1 — Cold Start Smoke Test**

```bash
docker compose down -v
docker compose up -d
sleep 10
curl -s http://localhost:22040/health
```

Then proceed through all 9 UAT tests sequentially. Record results in `.planning/phases/06-brief-app/06-UAT.md`.

If all pass → mark UAT complete → proceed to Phase 7 planning (`/gsd-plan-phase 7`).

---

## Resume Command

```bash
/gsd-resume-work
```

This will load the handoff context and position you at the next UAT test.