---
phase: 05-triage-app
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - apps/triage/requirements.txt
  - apps/triage/Dockerfile
  - docker-compose.yml
autonomous: false
requirements: [R7]
user_setup:
  - service: oMLX (local LLM + embedder)
    why: "triage scores with qwen36 and embeds with mE5-large via the OpenAI-compatible API"
    env_vars:
      - name: LLM_BASE_URL
        source: "Host oMLX endpoint reachable from the container, e.g. http://host.docker.internal:8000/v1"
      - name: LLM_API_KEY
        source: "oMLX key (default 'omlx')"
      - name: LLM_MODEL
        source: "qwen36-ud-4bit (default)"
    dashboard_config:
      - task: "Ensure oMLX is running on the host with intfloat/multilingual-e5-large and qwen36 loadable on demand"
        location: "Host Mac, port 8000"

must_haves:
  truths:
    - "docker compose build triage produces an image whose CMD runs python worker.py (R7)"
    - "docker compose up -d triage reaches running state; GET http://localhost:22030/health returns 200 (R7)"
    - "The triage container reaches host oMLX via LLM_BASE_URL (host.docker.internal on Docker Desktop / host-gateway via extra_hosts) for both scoring and embeddings (ADR-004 — local only, never cloud)"
    - "The container survives a temporary RabbitMQ outage and reconnects via connect_robust — /health stays 200 and the process does not crash (R7 unclassified edge)"
    - "The container runs as a non-root user (least privilege)"
  artifacts:
    - apps/triage/Dockerfile
    - apps/triage/requirements.txt
    - docker-compose.yml (triage service stanza)
  key_links:
    - "Dockerfile mirrors apps/ingest-imap: COPY libs/contracts + libs/store, pip install --no-deps, then COPY apps/triage and pip install -r requirements.txt"
    - "compose healthcheck uses a python urllib one-liner (python:3.12-slim has no curl)"
    - "depends_on postgres + rabbitmq service_healthy; ports bind 127.0.0.1:22030:22030 (localhost only)"
    - "extra_hosts host.docker.internal:host-gateway makes the host oMLX reachable on Linux engines too"
  prohibitions: []
---

<objective>
Containerize the triage worker as the triage service at port 22030. Delivers SPEC R7: a
Docker image, a requirements.txt (no new packages — RESEARCH), a docker-compose stanza with
healthcheck and dependency ordering, and a blocking human-verify checkpoint that proves the
live container answers /health and survives a RabbitMQ outage.

Purpose: turns the Wave-2 worker into a running service in the stack.

Output: apps/triage/Dockerfile, apps/triage/requirements.txt, the docker-compose triage
service, and operator confirmation of live health + reconnect.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-triage-app/05-SPEC.md
@.planning/phases/05-triage-app/05-CONTEXT.md
@.planning/phases/05-triage-app/05-RESEARCH.md
@.planning/phases/05-triage-app/05-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

New symbols introduced by 05-04 (excluded from drift verification — created here):
- File `apps/triage/Dockerfile`
- File `apps/triage/requirements.txt`
- docker-compose.yml service `triage` (container_name `infotriage-triage`, port 22030, healthcheck)

<tasks>

<task type="auto">
  <name>Task 1: requirements.txt + Dockerfile for the triage container</name>
  <files>apps/triage/requirements.txt, apps/triage/Dockerfile</files>
  <read_first>
    - apps/ingest-imap/Dockerfile (the exact python:3.12-slim local-lib install pattern to mirror; how runtime secrets arrive via env_file only)
    - apps/ingest-imap/requirements.txt (formatting baseline)
    - .planning/phases/05-triage-app/05-RESEARCH.md (Standard Stack — requirements list aio-pika, psycopg[binary], pgvector; no new packages)
    - apps/triage/worker.py (confirms CMD target is worker.py)
  </read_first>
  <action>
    Create apps/triage/requirements.txt listing exactly aio-pika, psycopg[binary], pgvector
    (no new packages — RESEARCH Package Legitimacy Audit; these are Phase 2/3 proven). Create
    apps/triage/Dockerfile mirroring apps/ingest-imap/Dockerfile: FROM python:3.12-slim;
    WORKDIR /app; COPY libs/contracts /build/contracts and COPY libs/store /build/store then
    RUN pip install --no-deps /build/contracts /build/store; COPY apps/triage/requirements.txt
    and RUN pip install -r requirements.txt; COPY apps/triage/ .; add a non-root USER (e.g.
    create and switch to a nobody-class user before CMD — least privilege, T-05-04); EXPOSE
    22030; CMD ["python", "worker.py"]. Do NOT bake any credential ARG/ENV — secrets arrive at
    runtime via env_file (NF-6). triage writes no blobs but PostgresStore takes a blob_root, so
    no blob volume is required at build time.
  </action>
  <verify>
    <automated>docker build -f apps/triage/Dockerfile -t infotriage-triage:plancheck .</automated>
  </verify>
  <done>The triage image builds successfully from apps/triage/Dockerfile with worker.py as CMD and a non-root USER.</done>
  <acceptance_criteria>
    - apps/triage/requirements.txt contains aio-pika, psycopg[binary], pgvector and no other packages
    - apps/triage/Dockerfile FROM python:3.12-slim, installs libs via pip install --no-deps, ends with CMD ["python", "worker.py"]
    - Dockerfile sets a non-root USER before CMD and contains no credential ENV/ARG
    - `docker build -f apps/triage/Dockerfile -t infotriage-triage:plancheck .` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add triage service to docker-compose.yml (port 22030, healthcheck, deps)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (the ingest-imap service block ~lines 90-140: build context, container_name, restart, ports, env_file, environment, depends_on service_healthy, networks; postgres/rabbitmq service names and internal ports)
    - .planning/phases/05-triage-app/05-PATTERNS.md (docker-compose stanza section — what to change vs ingest-imap)
    - apps/triage/worker.py (env vars consumed: INFOTRIAGE_PG_DSN, INFOTRIAGE_AMQP_DSN, INFOTRIAGE_BLOB_ROOT, LLM_BASE_URL, LLM_API_KEY, LLM_MODEL; health port 22030)
  </read_first>
  <action>
    Add a triage service to docker-compose.yml by adapting the ingest-imap block. Set build
    context "." with dockerfile apps/triage/Dockerfile; container_name infotriage-triage;
    restart unless-stopped; ports "127.0.0.1:22030:22030" (localhost only, health port maps 1:1);
    env_file .env (required:false). environment: INFOTRIAGE_PG_DSN default
    postgresql://infotriage:infotriage_dev@postgres:5432/infotriage; INFOTRIAGE_AMQP_DSN default
    amqp://infotriage:infotriage_rmq@rabbitmq:5672; INFOTRIAGE_BLOB_ROOT /data/blobs; LLM_BASE_URL
    default http://host.docker.internal:8000/v1 (the host oMLX — ADR-004, never a cloud host);
    LLM_API_KEY default omlx; LLM_MODEL default qwen36-ud-4bit. Add extra_hosts entry
    host.docker.internal:host-gateway so the host endpoint resolves on non-Docker-Desktop engines
    too. volumes ./data:/data. depends_on postgres and rabbitmq each with condition
    service_healthy. networks [infotriage]. Add a healthcheck that does NOT need curl: a CMD using
    python urllib to open http://localhost:22030/health and exit non-zero on failure, with a
    sensible interval/timeout/retries/start_period.
  </action>
  <verify>
    <automated>docker compose config | grep -A2 "infotriage-triage"</automated>
  </verify>
  <done>docker-compose.yml has a valid triage service on 127.0.0.1:22030 with healthcheck, host.docker.internal extra_hosts, and postgres+rabbitmq healthy dependencies.</done>
  <acceptance_criteria>
    - docker compose config parses and includes a triage service with container_name infotriage-triage
    - ports maps 127.0.0.1:22030:22030; healthcheck uses python (no curl)
    - environment includes LLM_BASE_URL defaulting to a host.docker.internal endpoint and extra_hosts host.docker.internal:host-gateway
    - depends_on lists postgres and rabbitmq with condition service_healthy
    - `docker compose config` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Live /health + RabbitMQ-reconnect verification (R7)</name>
  <action>
    Claude brings up the triage container and runs the live checks, then the operator confirms
    the results before the phase proceeds. Steps: (1) ensure host oMLX is up on :8000; (2)
    docker compose up -d --build triage; (3) docker compose ps triage shows running/healthy;
    (4) curl http://localhost:22030/health returns 200; (5) docker compose stop rabbitmq, wait
    ~10s, curl /health again (still 200, no crash), then docker compose start rabbitmq and
    confirm reconnection in logs; (6) confirm no DSN appears in docker compose logs triage. This
    is a blocking human-verify gate (autonomous:false).
  </action>
  <what-built>
    The triage container (apps/triage/Dockerfile + requirements.txt) and its docker-compose
    service on 127.0.0.1:22030, wired to Postgres :22000, RabbitMQ :22001, and host oMLX :8000.
  </what-built>
  <how-to-verify>
    1. Ensure oMLX is running on the host at port 8000 with intfloat/multilingual-e5-large and qwen36 available.
    2. Run: docker compose up -d --build triage
    3. Wait ~15s, then run: docker compose ps triage  → STATUS should be running/healthy.
    4. Run: curl -s -o /dev/null -w "%{http_code}" http://localhost:22030/health  → expect 200.
    5. Reconnect test: docker compose stop rabbitmq  → wait 10s → curl the /health URL again → still 200 (worker did not crash). → docker compose start rabbitmq → confirm logs show reconnection (no DSN printed).
    6. Confirm `docker compose logs triage` shows no INFOTRIAGE_PG_DSN / INFOTRIAGE_AMQP_DSN values in plaintext.
  </how-to-verify>
  <resume-signal>Type "approved" once /health returns 200 and the container survives the RabbitMQ stop/start, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host network → container | published port 22030 and the host oMLX endpoint cross the container boundary |
| .env → container runtime | credentials injected via env_file at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | LLM_BASE_URL | high | mitigate | Default points to host oMLX (host.docker.internal:8000); no cloud host — ADR-004 |
| T-05-04 | Elevation of Privilege | container user | high | mitigate | Dockerfile switches to a non-root USER before CMD |
| T-05-06 | Information Disclosure | published port | medium | mitigate | 22030 bound to 127.0.0.1 only; not exposed on the LAN |
| T-05-02 | Information Disclosure | container logs | medium | mitigate | Checkpoint step 6 confirms no DSN appears in logs |
| T-05-SC | Tampering | requirements.txt | high | mitigate | Only Phase 2/3-proven packages (aio-pika, psycopg[binary], pgvector); no new/unaudited deps |
</threat_model>

<verification>
- `docker build -f apps/triage/Dockerfile .` succeeds and `docker compose config` parses
- Operator checkpoint: /health → 200; container survives RabbitMQ stop/start; no DSN in logs
</verification>

<success_criteria>
- triage image builds and runs as non-root with CMD python worker.py
- docker compose up -d triage → running/healthy; GET /health → 200
- Container reconnects to RabbitMQ after an outage without crashing
</success_criteria>

<output>
Create `.planning/phases/05-triage-app/05-04-SUMMARY.md` when done
</output>
