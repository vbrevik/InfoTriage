---
phase: 05-triage-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/store/sql/006-enrichment.sql
  - libs/store/src/store/_protocol.py
  - libs/store/src/store/_postgres.py
  - libs/store/src/store/_inmemory.py
  - tests/test_triage_enrichment.py
autonomous: true
requirements: [R1, R4, ADR-004]

must_haves:
  truths:
    - "After init_schema(), infotriage.enrichment has columns ccir, cnr, score, bucket, why, pmesii, tessoc (R1)"
    - "put_enrichment is idempotent: writing the same item_id twice updates in place, never errors or duplicates (R1 idempotency edge)"
    - "score column rejects values outside 0..10 via CHECK constraint (R1 boundary edge)"
    - "find_near_duplicate returns the matched item_id (Optional[str], D-06) when a stored embedding is within cosine threshold, else None (R4)"
    - "find_near_duplicate returns None when no embeddings exist — the first article in a window is never a false-positive duplicate (R4 unclassified edge)"
    - "put_embedding is idempotent: re-writing the same item_id updates the vector in place (R4)"
    - "InMemoryStore implements find_near_duplicate via a stdlib cosine loop so worker tests need no live pgvector (D-07)"
    - "BACKSTOP (non-inferable, needs held-out/concurrency test): concurrent put_enrichment for distinct item_ids under Postgres row-locking does not corrupt rows (R1 concurrency edge)"
  artifacts:
    - libs/store/sql/006-enrichment.sql
    - tests/test_triage_enrichment.py
  key_links:
    - "init_schema() loads sql/*.sql sorted — 006-enrichment.sql is auto-applied on startup with no code change"
    - "CREATE UNIQUE INDEX enrichment_item_id_unique is the precondition for put_enrichment ON CONFLICT (item_id) to work"
    - "CREATE UNIQUE INDEX embeddings_item_id_unique is the precondition for put_embedding ON CONFLICT (item_id) to work"
    - "register_vector() already runs in PostgresStore.__enter__ — find_near_duplicate may use the <=> operator directly"
  prohibitions: []
---

<objective>
Extend the Store interface and Postgres schema so the triage worker can persist scoring
results and run pgvector semantic dedup. Implements D-05 (two new embedding Store methods),
D-06 (find_near_duplicate returns Optional[str]), D-07 (InMemoryStore cosine loop), and
D-10 (006-enrichment.sql migration delivered as idempotent ALTER TABLE).

Purpose: data-layer foundation that the Wave-2 worker plan (05-03) depends on. No event
wiring here — pure store + schema.

Output: 006-enrichment.sql migration; four new Store methods (put_enrichment, get_enrichment,
put_embedding, find_near_duplicate) on the Protocol + PostgresStore + InMemoryStore; a contract
test exercising both store implementations.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-triage-app/05-SPEC.md
@.planning/phases/05-triage-app/05-CONTEXT.md
@.planning/phases/05-triage-app/05-RESEARCH.md
@.planning/phases/05-triage-app/05-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

New symbols introduced by 05-01 (excluded from drift verification — created here):
- File `libs/store/sql/006-enrichment.sql`
- SQL objects: unique indexes `enrichment_item_id_unique`, `embeddings_item_id_unique`; enrichment columns `ccir`, `cnr`, `score`, `bucket`, `why`, `pmesii`, `tessoc`
- Store Protocol + PostgresStore + InMemoryStore methods: `put_enrichment(item_id, fields)`, `get_enrichment(item_id)`, `put_embedding(item_id, vector)`, `find_near_duplicate(vector, window_days=7, threshold=0.84)`
- InMemoryStore internal state `_enrichments`, `_embeddings`; helper `_cosine_sim`
- File `tests/test_triage_enrichment.py` (tests: `test_put_get_enrichment`, `test_put_enrichment_idempotent`, `test_enrichment_score_check`, `test_find_near_duplicate`, `test_find_near_duplicate_empty`, `test_put_embedding_idempotent`, `test_enrichment_schema`)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing contract tests for enrichment + embedding store methods</name>
  <files>tests/test_triage_enrichment.py</files>
  <read_first>
    - tests/test_store_contract.py (the parametrized inmemory+postgres `store` fixture, db_live marker, _get_dsn/_truncate_all helpers — copy this exact pattern)
    - tests/test_store_integration.py (DEV_DSN value, init_schema usage in the postgres fixture)
    - libs/store/src/store/_inmemory.py (InMemoryStore constructor signature InMemoryStore(blob_root=...))
    - libs/contracts/src/contracts/_item.py (Item fields needed to seed an article before enrichment: source, source_type, url, title, lang)
  </read_first>
  <behavior>
    - test_put_get_enrichment: put_enrichment(item_id, dict of all 7 fields) then get_enrichment(item_id) returns a dict whose 7 fields equal what was written
    - test_put_enrichment_idempotent: put_enrichment twice for the same item_id with different fields → get_enrichment returns the second write's values; no duplicate-row error
    - test_enrichment_score_check (db_live, postgres only): put_enrichment with score=11 raises a database error (CHECK constraint); the worker is responsible for clamping before calling
    - test_find_near_duplicate: put_embedding(idA, vecA); find_near_duplicate(vec_near) where cosine(vec_near, vecA) >= 0.84 returns idA; find_near_duplicate(vec_far) where cosine < 0.84 returns None
    - test_find_near_duplicate_empty: with no embeddings stored, find_near_duplicate(any_vec) returns None (first-in-window is never a duplicate)
    - test_put_embedding_idempotent: put_embedding(id, vecA) then put_embedding(id, vecB); the stored vector for id is vecB (no duplicate row)
    - test_enrichment_schema (db_live, postgres only): after init_schema(), query information_schema.columns for infotriage.enrichment and assert ccir, cnr, score, bucket, why, pmesii, tessoc are all present
  </behavior>
  <action>
    Create tests/test_triage_enrichment.py mirroring tests/test_store_contract.py: a pytest
    fixture named store parametrized over "inmemory" and pytest.param("postgres", marks=db_live).
    For postgres, reuse the existing DEV_DSN / _get_dsn and _truncate_all helpers and call
    s.init_schema() inside the fixture. Enrichment rows reference infotriage.articles(id) by FK,
    so each test that writes enrichment against the postgres store must first put_item() a minimal
    Item (source, source_type, url, title, lang) and use that item.id as item_id. Use 1024-length
    float vectors for embedding tests: a base vector, a near-copy differing slightly so cosine is
    >= 0.84, and a clearly different vector for the < 0.84 case. Mark the schema-introspection and
    score-CHECK tests with the db_live marker (postgres only). These tests MUST fail initially
    because the four Store methods do not exist yet (D-05, D-06, D-07).
  </action>
  <verify>
    <automated>pytest tests/test_triage_enrichment.py -x; test $? -ne 0</automated>
  </verify>
  <done>tests/test_triage_enrichment.py exists with the 7 tests; the run fails (RED) because put_enrichment/get_enrichment/put_embedding/find_near_duplicate are not implemented.</done>
  <acceptance_criteria>
    - tests/test_triage_enrichment.py contains def test_put_get_enrichment, test_put_enrichment_idempotent, test_find_near_duplicate, test_find_near_duplicate_empty, test_put_embedding_idempotent, test_enrichment_score_check, test_enrichment_schema
    - The store fixture is parametrized with both "inmemory" and a db_live-marked "postgres"
    - `pytest tests/test_triage_enrichment.py -x` exits non-zero (RED) before Tasks 2 and 3
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add 006-enrichment.sql idempotent migration (unique indexes + 7 columns)</name>
  <files>libs/store/sql/006-enrichment.sql</files>
  <read_first>
    - libs/store/sql/005-stubs.sql (current bare enrichment stub: id SERIAL PK, item_id TEXT NOT NULL FK, created_at — no UNIQUE on item_id)
    - libs/store/sql/003-vectors.sql (infotriage.embeddings: id SERIAL PK, item_id TEXT FK, embedding vector(1024) NOT NULL, model TEXT, created_at — no UNIQUE on item_id; HNSW cosine index idx_embeddings_embedding_hnsw must not be dropped)
    - libs/store/src/store/_postgres.py (init_schema(): confirm it loads sql/*.sql sorted on an autocommit DDL connection, and register_vector runs after DDL — Pitfall 1)
  </read_first>
  <action>
    Create libs/store/sql/006-enrichment.sql delivering D-10. CRITICAL — Postgres has no
    "ALTER TABLE ... ADD CONSTRAINT IF NOT EXISTS" form (the 05-PATTERNS.md sample is invalid
    SQL); use CREATE UNIQUE INDEX IF NOT EXISTS instead, which ON CONFLICT (item_id) honors.
    The file must contain, in order: (1) CREATE UNIQUE INDEX IF NOT EXISTS
    enrichment_item_id_unique ON infotriage.enrichment (item_id); (2) seven idempotent
    ALTER TABLE infotriage.enrichment ADD COLUMN IF NOT EXISTS statements — ccir TEXT, cnr TEXT,
    score INT CHECK (score BETWEEN 0 AND 10), bucket TEXT, why TEXT, pmesii TEXT, tessoc TEXT;
    (3) CREATE UNIQUE INDEX IF NOT EXISTS embeddings_item_id_unique ON infotriage.embeddings
    (item_id) so put_embedding upsert works (D-05). Preserve the existing stub table — do not
    DROP/recreate. Every statement uses IF NOT EXISTS so re-applying via init_schema() is a no-op.
    Header comment must note: the unique indexes back ON CONFLICT upserts; the embeddings HNSW
    cosine index from 003-vectors.sql is preserved.
  </action>
  <verify>
    <automated>pytest tests/test_triage_enrichment.py::test_enrichment_schema -m db_live -x</automated>
  </verify>
  <done>006-enrichment.sql exists; init_schema() applies it (idempotently); the db_live schema test confirms all 7 enrichment columns exist.</done>
  <acceptance_criteria>
    - libs/store/sql/006-enrichment.sql contains CREATE UNIQUE INDEX IF NOT EXISTS enrichment_item_id_unique and CREATE UNIQUE INDEX IF NOT EXISTS embeddings_item_id_unique
    - It contains ADD COLUMN IF NOT EXISTS for all of ccir, cnr, score, bucket, why, pmesii, tessoc with score carrying CHECK (score BETWEEN 0 AND 10)
    - The file contains no ADD CONSTRAINT IF NOT EXISTS (invalid Postgres)
    - `pytest tests/test_triage_enrichment.py::test_enrichment_schema -m db_live -x` passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement put_enrichment / get_enrichment / put_embedding / find_near_duplicate</name>
  <files>libs/store/src/store/_protocol.py, libs/store/src/store/_postgres.py, libs/store/src/store/_inmemory.py</files>
  <read_first>
    - libs/store/src/store/_protocol.py (existing put_item/get_item Protocol stub style)
    - libs/store/src/store/_postgres.py (put_item ON CONFLICT DO UPDATE pattern; get_item dict_row read; the `assert self._conn is not None` guard; register_vector() in __enter__)
    - libs/store/src/store/_inmemory.py (_items dict pattern; put_item/get_item shape)
    - tests/test_triage_enrichment.py (the contract the four methods must satisfy)
  </read_first>
  <behavior>
    - All four methods exist on the Store Protocol and on both PostgresStore and InMemoryStore
    - put_enrichment upserts via ON CONFLICT (item_id) DO UPDATE on all 7 columns (Postgres); InMemoryStore overwrites its dict entry
    - get_enrichment returns a dict of the 7 fields or None when absent
    - put_embedding upserts the vector via ON CONFLICT (item_id) DO UPDATE (Postgres); InMemoryStore overwrites
    - find_near_duplicate (Postgres) uses the <=> cosine operator with a 7-day created_at window, ORDER BY distance LIMIT 1, returns item_id when distance < (1 - threshold) else None
    - find_near_duplicate (InMemoryStore) loops its stored vectors computing cosine similarity with stdlib math, returns the best item_id when similarity >= threshold else None (window_days ignored in memory)
  </behavior>
  <action>
    Add the four method signatures to the Store Protocol in _protocol.py (D-05, D-06): put_enrichment(self, item_id: str, fields: dict) -> None; get_enrichment(self, item_id: str) -> Optional[dict]; put_embedding(self, item_id: str, vector: list[float]) -> None; find_near_duplicate(self, vector: list[float], window_days: int = 7, threshold: float = 0.84) -> Optional[str]. Import Optional. In _postgres.py implement them following the existing put_item/get_item template: each guarded with the `assert self._conn is not None` message; all SQL uses %s bind params only (never f-strings — V5/T-02-01). put_enrichment INSERT into infotriage.enrichment (item_id, ccir, cnr, score, bucket, why, pmesii, tessoc) with ON CONFLICT (item_id) DO UPDATE setting the same 7 columns from EXCLUDED; read field values via fields.get(name). put_embedding INSERT into infotriage.embeddings (item_id, embedding, model) VALUES (%s, %s, %s) with ON CONFLICT (item_id) DO UPDATE SET embedding=EXCLUDED.embedding, model=EXCLUDED.model — model is the embedder id "intfloat/multilingual-e5-large". find_near_duplicate selects item_id and (embedding <=> %s::vector) AS dist from infotriage.embeddings WHERE created_at >= NOW() - INTERVAL %s (bind the interval as the string from window_days, e.g. the value "7 days") ORDER BY embedding <=> %s::vector LIMIT 1; return item_id only when dist < (1.0 - threshold). Use the <=> cosine operator (NOT <-> which is L2). In _inmemory.py add _enrichments and _embeddings dicts in __init__ and a module-level or static _cosine_sim helper using math.sqrt; implement the same four methods (D-07): find_near_duplicate ignores window_days and returns the best-similarity item_id when >= threshold. After implementation the full contract test passes for both stores.
  </action>
  <verify>
    <automated>pytest tests/test_triage_enrichment.py -x && pytest tests/test_triage_enrichment.py -m db_live -x</automated>
  </verify>
  <done>All four methods implemented on Protocol + PostgresStore + InMemoryStore; tests/test_triage_enrichment.py passes for inmemory and (db_live) postgres.</done>
  <acceptance_criteria>
    - _protocol.py declares put_enrichment, get_enrichment, put_embedding, find_near_duplicate
    - _postgres.py and _inmemory.py each implement all four methods
    - _postgres.find_near_duplicate uses the <=> operator and an INTERVAL %s window bound parameter; no f-string SQL anywhere in the new methods
    - `pytest tests/test_triage_enrichment.py -x` passes (inmemory) and `pytest tests/test_triage_enrichment.py -m db_live -x` passes (postgres)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM/embedding output → Postgres | enrichment TEXT fields (ccir, why, pmesii, tessoc) originate from model output and are persisted |
| caller-supplied vector → pgvector query | float vectors are bound into the cosine query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | put_enrichment / find_near_duplicate SQL | high | mitigate | All SQL uses %s bind params; model text and vectors are never interpolated into SQL strings (V5/T-02-01) |
| T-05-02 | Tampering | score column | medium | mitigate | CHECK (score BETWEEN 0 AND 10) at the schema layer is a hard backstop to the worker-side clamp |
| T-05-SC | Tampering | package installs | high | accept | Phase 5 adds no new PyPI packages (RESEARCH Package Legitimacy Audit) — no install task here |
</threat_model>

<verification>
- `pytest tests/test_triage_enrichment.py -x` (inmemory) and `-m db_live -x` (postgres) both green
- 006-enrichment.sql re-applies idempotently (init_schema run twice has no error)
- grep confirms no `ADD CONSTRAINT IF NOT EXISTS` and no f-string SQL in the new store methods
</verification>

<success_criteria>
- infotriage.enrichment has all 7 scoring columns after init_schema()
- put_enrichment / put_embedding are idempotent upserts; get_enrichment round-trips
- find_near_duplicate returns Optional[str] (matched item_id or None) for both stores
- Full contract test green for inmemory and postgres
</success_criteria>

<output>
Create `.planning/phases/05-triage-app/05-01-SUMMARY.md` when done
</output>
