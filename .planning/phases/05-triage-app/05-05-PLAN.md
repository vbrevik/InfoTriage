---
phase: 05-triage-app
plan: 05
type: execute
wave: 4
depends_on: ["05-03", "05-04"]
files_modified:
  - scripts/shadow_run.py
  - README.md
autonomous: false
requirements: [R6]

must_haves:
  truths:
    - "scripts/shadow_run.py reads infotriage.enrichment joined to infotriage.articles, re-runs score_item() standalone per article, and prints a side-by-side table of stored bucket vs rescore bucket with a match column (R6, D-08)"
    - "Parity is defined as same bucket per item (not exact score — the LLM is stochastic); the script counts and reports the number of matching buckets (R6 precision edge, D-09)"
    - "shadow_run compares RAW buckets directly because the worker stores raw score_item vocabulary in enrichment (read|maybe|skip) — no mapping needed for the comparison"
    - "After operator confirms >= 10 matching buckets, fever_triage.py is retired from the production scoring path: the host crontab fever entry is removed and README no longer advertises fever as the scoring path (R6)"
    - "fever_triage.py the FILE is preserved (apps/triage/digest.py imports fever_key/fever/strip_html from it); only its production invocation is retired"
    - "The scheduler service already has no fever entry (ADAPTERS lists only the 4 ingest adapters) — this is verified, not changed"
  artifacts:
    - scripts/shadow_run.py
  key_links:
    - "shadow_run uses a direct psycopg3 dict_row read against DEV_DSN, then score_item({title, source, summary}) per row — same standalone scorer the worker wraps"
    - "fever's only production invocation is the host crontab line documented in README (the scheduler never ran fever); cutover removes that crontab entry"
    - "digest.py depends on fever_triage.py at import time — the file must NOT be deleted"
  prohibitions:
    - statement: "MUST NOT cut over Fever if the shadow-run parity check has fewer than 10 validated sample items"
      status: resolved
      verification: "judgment: the blocking checkpoint requires the operator to confirm the shadow_run table shows >= 10 matching buckets before the crontab/README cutover step is taken"
---

<objective>
Validate that the new event-driven scorer agrees with the proven standalone scorer, then
retire fever_triage.py from the production path. Delivers SPEC R6 via D-08 (scripts/shadow_run.py)
and D-09 (parity = matching bucket on >= 10 articles), gated by a blocking human checkpoint
that enforces the cutover prohibition.

Purpose: the M1 safety gate before the old Fever scoring path is decommissioned.

Output: scripts/shadow_run.py; an updated README that stops advertising fever as the scoring
path; operator confirmation of parity and crontab removal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-triage-app/05-SPEC.md
@.planning/phases/05-triage-app/05-CONTEXT.md
@.planning/phases/05-triage-app/05-RESEARCH.md
@.planning/phases/05-triage-app/05-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

New symbols introduced by 05-05 (excluded from drift verification — created here):
- File `scripts/shadow_run.py`
- README.md edits: fever scoring-path run-commands + host crontab line marked retired; event-driven triage container documented as the scoring path

<tasks>

<task type="auto">
  <name>Task 1: Build scripts/shadow_run.py parity comparison tool (D-08)</name>
  <files>scripts/shadow_run.py</files>
  <read_first>
    - tests/test_store_integration.py (DEV_DSN value; psycopg.connect(dsn, row_factory=dict_row) direct read pattern)
    - apps/triage/triage_score.py (score_item({title, source, summary}) signature and the raw bucket vocabulary read|maybe|skip it returns)
    - .planning/phases/05-triage-app/05-PATTERNS.md (shadow_run.py section — SQL join + tabulated output columns)
    - .planning/phases/05-triage-app/05-CONTEXT.md (D-08/D-09 + the specifics: output columns item_id short, title truncated, enrichment_bucket, rescore_bucket, match)
  </read_first>
  <action>
    Create scripts/shadow_run.py as a one-shot CLI. Connect to Postgres via DEV_DSN (overridable
    by INFOTRIAGE_PG_DSN) using psycopg.connect(dsn, row_factory=dict_row). SELECT e.item_id,
    a.title, a.summary, a.source, e.bucket FROM infotriage.enrichment e JOIN infotriage.articles
    a ON a.id = e.item_id WHERE e.bucket IS NOT NULL LIMIT 100 (use %s bind params for any
    runtime arg). For each row, call score_item({"title": title, "source": source, "summary":
    summary or ""}) and read the rescore bucket. Compare the rescore bucket to the stored
    enrichment bucket directly (both are raw read|maybe|skip — the worker stores raw vocabulary,
    so no mapping is needed). Print a fixed-width table with columns: item_id (first 8 chars),
    title (truncated ~40 chars), enrichment_bucket, rescore_bucket, and a match column (OK /
    MISMATCH). At the end print totals: number of rows compared and number with matching buckets,
    and a clear line stating whether the >= 10 matching-bucket parity bar is met (D-09). The
    script changes no data (read-only) and never deletes fever_triage.py.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('scripts/shadow_run.py').read()); print('parse-ok')"</automated>
  </verify>
  <done>scripts/shadow_run.py parses, queries enrichment+articles, re-runs score_item per row, and prints the side-by-side parity table with a final match count and the >= 10 parity verdict.</done>
  <acceptance_criteria>
    - scripts/shadow_run.py imports score_item from triage_score and reads via psycopg dict_row
    - It prints columns item_id, title, enrichment_bucket, rescore_bucket, match and a final matching-count line referencing the >= 10 bar
    - SQL uses %s bind params for any runtime argument; the script performs no writes/deletes
    - `python -c "import ast; ast.parse(open('scripts/shadow_run.py').read())"` succeeds
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Retire fever from the documented production path (README)</name>
  <files>README.md</files>
  <read_first>
    - README.md (lines ~55-56 fever_triage.py run-commands; line ~66 the host crontab fever entry; line ~72 Fever API password note)
    - apps/scheduler/scheduler_main.py (ADAPTERS dict — confirm it has NO fever entry; this is verified, not edited)
    - apps/triage/digest.py (line 26 imports fever_key/fever/strip_html from fever_triage — the reason the file is preserved)
  </read_first>
  <action>
    Update README.md so it no longer presents fever_triage.py as the scoring path. Replace the two
    fever_triage.py run-command lines and the host crontab line with guidance that scoring now runs
    in the event-driven triage container (docker compose up -d triage; consumes item.ingested,
    writes infotriage.enrichment, publishes verdict.ready; health at http://localhost:22030/health).
    Add a short note that fever_triage.py is preserved only because apps/triage/digest.py imports
    helpers from it, and is no longer invoked as a scoring path. Do not delete fever_triage.py and
    do not change apps/scheduler (its ADAPTERS dict already has no fever entry — state this as
    verified). Keep the change surgical: only the fever scoring-path references.
  </action>
  <verify>
    <automated>grep -nE "fever_triage\.py" README.md; grep -q "triage container\|docker compose up -d triage\|22030" README.md</automated>
  </verify>
  <done>README documents the triage container as the scoring path; the fever_triage.py scoring run-commands and host crontab line are marked retired; fever_triage.py file and scheduler are untouched.</done>
  <acceptance_criteria>
    - README.md references the triage container / port 22030 as the scoring path
    - README.md no longer instructs running fever_triage.py as the periodic scoring path (crontab line removed or marked retired)
    - apps/triage/fever_triage.py still exists; apps/scheduler/scheduler_main.py is unchanged
    - `grep -q "22030" README.md` succeeds
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Shadow-run parity gate + Fever cutover (R6)</name>
  <action>
    Claude prepares the run and the operator confirms parity before cutover. Steps: (1) ensure
    the triage container has scored >= 10 live articles (let ingest/scheduler deliver
    item.ingested, or re-publish item.ingested for >= 10 of the 111 existing articles); (2) run
    python scripts/shadow_run.py and read the side-by-side table + totals; (3) the operator
    confirms >= 10 items have matching buckets (exact score may differ — LLM is stochastic, D-09);
    (4) ONLY IF parity >= 10, the operator removes the host crontab fever entry (crontab -e,
    delete the apps/triage/fever_triage.py line) — if parity < 10, STOP, do not cut over (R6
    prohibition). This is a blocking human gate (auto-advance ignored).
  </action>
  <what-built>
    scripts/shadow_run.py (parity comparison) and the README cutover edits. The triage container
    from 05-04 is running and has scored live articles.
  </what-built>
  <how-to-verify>
    1. Ensure the triage container has processed at least 10 articles: with the stack up, let the
       ingest adapters/scheduler deliver item.ingested events (or re-publish item.ingested for
       >= 10 of the 111 existing articles in infotriage.articles) so >= 10 enrichment rows exist.
    2. Run: python scripts/shadow_run.py
    3. Read the table and the final totals. Confirm >= 10 items show match = OK (same bucket).
       Exact scores may differ (LLM is stochastic) — only the bucket must match (D-09).
    4. ONLY IF parity >= 10 matching buckets: remove the host crontab fever entry
       (crontab -e → delete the line that runs apps/triage/fever_triage.py). If parity < 10, STOP
       — do not cut over (R6 prohibition); investigate and re-run.
    5. Confirm `docker compose up triage` runs worker.py (not fever_triage.py) and that the
       scheduler has no fever job.
  </how-to-verify>
  <resume-signal>Type "approved" once the shadow_run table shows >= 10 matching buckets AND the host crontab fever entry is removed, or describe what blocked parity.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres → shadow_run | enrichment + article rows read into a local ops script |
| operator → host crontab | manual removal of the fever scoring job |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | shadow_run SQL | medium | mitigate | Read-only query; %s bind params for any runtime arg; no writes |
| T-05-07 | Denial of Service | premature Fever cutover | high | mitigate | Blocking checkpoint enforces >= 10 parity before crontab removal (R6 prohibition) |
| T-05-08 | Tampering | fever_triage.py deletion | medium | mitigate | digest.py imports from it — plan forbids deleting the file; only the invocation is retired |
| T-05-SC | Tampering | package installs | high | accept | No new PyPI packages (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('scripts/shadow_run.py').read())"` succeeds
- README references the triage container / 22030 and no longer advertises fever as the scoring path
- apps/triage/fever_triage.py still present; apps/scheduler unchanged
- Operator checkpoint: shadow_run shows >= 10 matching buckets; host crontab fever entry removed
</verification>

<success_criteria>
- shadow_run.py produces a side-by-side parity table with a match count and the >= 10 verdict
- Parity confirmed on >= 10 items (same bucket) before any cutover
- fever retired from the production scoring path; fever_triage.py file preserved for digest.py
</success_criteria>

<output>
Create `.planning/phases/05-triage-app/05-05-SUMMARY.md` when done
</output>
