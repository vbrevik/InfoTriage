---
phase: 05-triage-app
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/contracts/src/contracts/_bus_rabbitmq.py
  - apps/triage/triage_score.py
  - tests/test_triage_score_hotread.py
  - tests/test_bus_consume.py
autonomous: true
requirements: [R2, R3, ccir.md]

must_haves:
  truths:
    - "RabbitMQBus.consume(routing_key, handler, prefetch_count=1) registers a persistent callback consumer on the routing key's queue (R2)"
    - "consume() reuses the existing ROUTING_KEY_TO_QUEUE mapping and the topology declared by _ensure_connection — it does not redeclare exchanges/queues"
    - "A message published to item.ingested is delivered to a consume() handler (live RabbitMQ smoke) (R2)"
    - "score_item() reads ccir.md on every call — editing ccir.md between two calls changes the prompt the scorer sends (R3, D-5)"
    - "score_item()'s public signature score_item(it: dict) -> dict is unchanged; the --sample CLI still works"
  artifacts:
    - tests/test_triage_score_hotread.py
    - tests/test_bus_consume.py
  key_links:
    - "RabbitMQBus._queues is keyed by routing_key (NOT queue name) — consume() looks up self._queues[routing_key]"
    - "subscribe() stays drain-only and unchanged; consume() is a new sibling method, not on the BusClient Protocol"
    - "Removing module-level CCIR=load_ccir() AND switching the f-string from {CCIR} to {ccir} are both required, else the hot-read silently fails (Pitfall 4)"
  prohibitions:
    - statement: "MUST NOT cache ccir.md at startup — hot edits must take effect on the next scoring run (D-5)"
      status: resolved
      verification: "test: tests/test_triage_score_hotread.py monkeypatches CCIR_PATH to a temp file, calls score_item, rewrites the file, calls again, and asserts the second prompt reflects the new content"
---

<objective>
Build the two interfaces the Wave-2 triage worker (05-03) consumes: (1) a persistent
RabbitMQ consumer method on RabbitMQBus, and (2) the hot-read fix to triage_score.py so
ccir.md edits take effect without a restart.

Implements D-02 (load_ccir() moved inside score_item, module-level cache removed) and the
RabbitMQBus.consume() decision from RESEARCH Open Question 2 (add consume() to the class,
not to the BusClient Protocol).

Purpose: both changes are prerequisites the worker depends on; isolating them in Wave 1
keeps the worker plan focused. No worker code here.

Output: RabbitMQBus.consume(); a corrected triage_score.py; a live-RabbitMQ consume smoke
test; a hot-read regression test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-triage-app/05-SPEC.md
@.planning/phases/05-triage-app/05-CONTEXT.md
@.planning/phases/05-triage-app/05-RESEARCH.md
@.planning/phases/05-triage-app/05-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

New/changed symbols introduced by 05-02 (excluded from drift verification — created here):
- Method `RabbitMQBus.consume(routing_key, handler, prefetch_count=1)` in `libs/contracts/src/contracts/_bus_rabbitmq.py`
- Modified `apps/triage/triage_score.py`: module-level `CCIR = load_ccir()` removed; `ccir = load_ccir()` added as first line of `score_item()`; f-string reference changed `{CCIR}` → `{ccir}`
- File `tests/test_triage_score_hotread.py` (test `test_ccir_hot_read`)
- File `tests/test_bus_consume.py` (test `test_consume_delivers_message`, rabbitmq-marked)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: triage_score.py ccir.md hot-read fix (D-02) + regression test</name>
  <files>apps/triage/triage_score.py, tests/test_triage_score_hotread.py</files>
  <read_first>
    - apps/triage/triage_score.py (line 27 module-level CCIR = load_ccir(); line ~58 f-string uses {CCIR}; load_ccir() at line 21; score_item() at line 54)
    - tests/test_score_parse.py (existing scorer test conventions; how llm() is patched/avoided)
  </read_first>
  <behavior>
    - test_ccir_hot_read: monkeypatch triage_score.CCIR_PATH to a temp file containing marker text A and monkeypatch triage_score.llm to a capture function that records the prompt and returns a fixed valid JSON string; call score_item({title,source,summary}); rewrite the temp file to contain marker text B; call score_item again; assert the first captured prompt contains A and the second contains B (proves ccir.md is re-read per call)
  </behavior>
  <action>
    Apply D-02 to apps/triage/triage_score.py: delete the module-level CCIR = load_ccir()
    assignment (line 27); add ccir = load_ccir() as the first statement inside score_item();
    change the f-string CCIR reference (the {CCIR} interpolation in the prompt) to {ccir}
    (lowercase local variable). Pitfall 4: BOTH edits are required — if the f-string still
    references {CCIR} after the module-level var is removed, score_item raises NameError; if
    the module-level var stays, the hot edit is shadowed. Leave score_item()'s signature and
    all other behavior unchanged so the --sample CLI and tests/test_score_parse.py still pass.
    Then create tests/test_triage_score_hotread.py implementing test_ccir_hot_read by
    monkeypatching CCIR_PATH (to a tmp file) and llm (to a prompt-capturing stub returning a
    fixed JSON), per the behavior above. Do not modify or read the real repository ccir.md in
    the test.
  </action>
  <verify>
    <automated>pytest tests/test_triage_score_hotread.py tests/test_score_parse.py -x</automated>
  </verify>
  <done>Module-level CCIR removed; score_item reads ccir.md per call via local ccir; hot-read test passes and the existing score-parse test still passes.</done>
  <acceptance_criteria>
    - apps/triage/triage_score.py contains no module-level `CCIR = load_ccir()`
    - score_item()'s first statement is `ccir = load_ccir()` and the prompt f-string references `{ccir}`
    - tests/test_triage_score_hotread.py contains def test_ccir_hot_read
    - `pytest tests/test_triage_score_hotread.py tests/test_score_parse.py -x` passes
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add RabbitMQBus.consume() persistent consumer + live smoke test</name>
  <files>libs/contracts/src/contracts/_bus_rabbitmq.py, tests/test_bus_consume.py</files>
  <read_first>
    - libs/contracts/src/contracts/_bus_rabbitmq.py (ROUTING_KEY_TO_QUEUE map at module level; _ensure_connection at line 78; _declare_topology populating self._queues keyed by routing_key at line 157-169; subscribe() drain-only at line 196; publish() at line 171)
    - tests/test_store_integration.py or any existing rabbitmq-marked test for the rabbitmq marker + DSN env pattern (INFOTRIAGE_AMQP_DSN, default amqp://infotriage:infotriage_rmq@localhost:22001)
  </read_first>
  <behavior>
    - consume(routing_key, handler, prefetch_count=1): calls _ensure_connection(), resolves the queue via self._queues[routing_key] (which is keyed by routing_key), sets channel QoS to prefetch_count, registers handler via queue.consume(handler), and returns (does not block — the caller keeps the loop alive)
    - Unknown routing_key (not in self._queues) raises ValueError
    - test_consume_delivers_message (rabbitmq-marked): ensure connection, publish one item.ingested payload, register a consume() handler that uses async with message.process() and sets an asyncio.Event with the decoded payload; await the event with a timeout (e.g. 5s); assert the handler received the published item_id
  </behavior>
  <action>
    Add an async method consume(self, routing_key, handler, prefetch_count=1) to RabbitMQBus,
    placed after subscribe(). It must: await self._ensure_connection(); look up queue =
    self._queues.get(routing_key) — note self._queues is keyed by routing_key (e.g.
    "item.ingested"), NOT by queue name; if queue is None raise ValueError naming the routing
    key; await self._channel.set_qos(prefetch_count=prefetch_count); await
    queue.consume(handler); return None. Do not redeclare topology and do not modify subscribe()
    or the BusClient Protocol (RESEARCH Open Q2). Then create tests/test_bus_consume.py with the
    rabbitmq marker implementing test_consume_delivers_message per the behavior above; wrap the
    event wait in asyncio.wait_for with a 5s timeout so the test cannot hang. Use a fresh
    RabbitMQBus from INFOTRIAGE_AMQP_DSN (default localhost:22001) and close it in a finally.
  </action>
  <verify>
    <automated>pytest tests/test_bus_consume.py -m rabbitmq -x</automated>
  </verify>
  <done>RabbitMQBus.consume() exists and a message published to item.ingested is delivered to a registered handler against live RabbitMQ.</done>
  <acceptance_criteria>
    - _bus_rabbitmq.py defines `async def consume(self, routing_key, handler, prefetch_count=1)` looking up self._queues[routing_key]
    - subscribe() and the BusClient Protocol are unchanged
    - consume() raises ValueError for an unknown routing key
    - `pytest tests/test_bus_consume.py -m rabbitmq -x` passes against RabbitMQ :22001
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AMQP broker → worker | messages cross from RabbitMQ into in-process handlers |
| ccir.md file → scorer prompt | operator-edited file content is read into the LLM prompt at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Information Disclosure | RabbitMQBus DSN | medium | mitigate | DSN sourced from INFOTRIAGE_AMQP_DSN; never logged (existing T-03-01 pattern preserved; consume() logs nothing sensitive) |
| T-05-04 | Tampering | ccir.md hot-read | low | accept | ccir.md is a local operator-owned file; hot-read is the intended behavior (D-5); no external write path |
| T-05-SC | Tampering | package installs | high | accept | No new PyPI packages added (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `pytest tests/test_triage_score_hotread.py tests/test_score_parse.py -x` green
- `pytest tests/test_bus_consume.py -m rabbitmq -x` green against RabbitMQ :22001
- grep confirms apps/triage/triage_score.py has no module-level `CCIR = load_ccir()` and the prompt references `{ccir}`
</verification>

<success_criteria>
- ccir.md edits take effect on the next score_item() call (no restart, no cache)
- RabbitMQBus.consume() persistently delivers item.ingested messages to a handler
- subscribe() and the BusClient Protocol are untouched
</success_criteria>

<output>
Create `.planning/phases/05-triage-app/05-02-SUMMARY.md` when done
</output>
