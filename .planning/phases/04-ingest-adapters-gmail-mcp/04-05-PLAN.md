---
phase: 04-ingest-adapters-gmail-mcp
plan: 05
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - gmail-mcp-server/Dockerfile
  - gmail-mcp-server/entrypoint.sh
  - scripts/provision_gmail_oauth.py
  - apps/ingest-gmail/mcp_client.py
  - apps/ingest-gmail/gmail_ingest.py
  - apps/ingest-gmail/main.py
  - apps/ingest-gmail/requirements.txt
  - apps/ingest-gmail/Dockerfile
  - tests/test_ingest_gmail.py
  - apps/ingest/gmail_to_atom.py
autonomous: true
requirements: [ADR-003, ADR-004, ADR-008]
must_haves:
  truths:
    - "The Gmail MCP server (@shinzolabs/gmail-mcp@1.7.4) runs as a Node container, listens for Streamable-HTTP JSON-RPC on its PORT, and holds the OAuth2 refresh token from env only (SPEC R3; ADR-008)"
    - "The ingest-gmail adapter initializes an MCP session, calls read-only Gmail tools via raw httpx JSON-RPC (D-05, no mcp Python SDK), normalizes messages to Item with source_type=\"gmail\", and produces >=1 articles row + >=1 item.ingested event (SPEC R3)"
    - "The OAuth2 provision script requests scopes gmail.readonly + gmail.metadata (D-06) and writes GMAIL_OAUTH2_REFRESH_TOKEN to .env (D-07); the token never appears in a git-tracked file (NF-6)"
    - "apps/ingest/gmail_to_atom.py no longer exists in the repo and is referenced nowhere in docker-compose.yml or README (SPEC R7)"
    - "Re-running ingest-gmail against identical Gmail messages leaves articles row count unchanged and publishes item.ingested only on the first run (SPEC R3 adjacency — covered edge)"
  artifacts:
    - gmail-mcp-server/Dockerfile
    - gmail-mcp-server/entrypoint.sh
    - scripts/provision_gmail_oauth.py
    - apps/ingest-gmail/mcp_client.py
    - apps/ingest-gmail/gmail_ingest.py
    - apps/ingest-gmail/main.py
    - apps/ingest-gmail/Dockerfile
    - apps/ingest-gmail/requirements.txt
    - tests/test_ingest_gmail.py
  key_links:
    - "mcp_client first calls JSON-RPC initialize (protocolVersion 2025-03-26), captures Mcp-Session-Id, then passes it on every tools/call; only read tools (list_threads/list_messages/get_message) are invoked (D-05, ADR-008 read-only)"
    - "gmail_ingest maps each Gmail message to Item(source=..., source_type=\"gmail\", url=synthetic per message id, ...) then awaits persist_and_publish; the MCP server URL comes from GMAIL_MCP_URL (default http://gmail-mcp-server:3000)"
    - "the Gmail MCP server reads CLIENT_ID/CLIENT_SECRET/REFRESH_TOKEN + PORT from env (env_file) — never baked into image layers (NF-6, ADR-008)"
  prohibitions:
    - statement: "ingest-gmail MUST NOT invoke any Gmail write/mutate tool (send/draft/trash/modify/markRead) and MUST use a read-only OAuth2 scope (gmail.readonly + gmail.metadata only)"
      status: resolved
      verification: "test/static — grep ingest-gmail source for write tool names (send_message/draft/trash/modify_message/delete_message/mark_read) returns empty; provision script requests only gmail.readonly + gmail.metadata and no gmail.send/gmail.modify/gmail.compose scope"
    - statement: "OAuth2 refresh token / client secret MUST NOT be stored in any git-tracked file or baked into a Docker image layer (env_file at runtime only)"
      status: resolved
      verification: "test/static — git ls-files shows no .env / client_secrets.json; gmail-mcp-server/Dockerfile + apps/ingest-gmail/Dockerfile contain no ARG/ENV carrying CLIENT_SECRET/REFRESH_TOKEN values"
---

<objective>
Solve Gmail (app passwords hard-blocked by 2SV — ADR-008). Stand up a self-hosted Gmail MCP server container using the verified `@shinzolabs/gmail-mcp@1.7.4` package (RESEARCH Finding 1 — the SPEC/CONTEXT-named `@googleapis/mcp-server-google-workspace` does NOT exist on npm and is REMOVED). Ship a one-time OAuth2 provision script (D-06/D-07) and a thin `ingest-gmail` MCP client adapter that talks to the server via raw httpx JSON-RPC (D-05, no Python MCP SDK), normalizes Gmail messages to `Item`, and `persist_and_publish`. Finally retire the dead-end `apps/ingest/gmail_to_atom.py` (SPEC R7).

Purpose: replace the dead IMAP+app-password Gmail bridge with a read-only OAuth2/MCP path (ADR-008), as a containerized Collection adapter (ADR-003), read-only (ADR-004).
Output: `gmail-mcp-server/`, `scripts/provision_gmail_oauth.py`, `apps/ingest-gmail/` + R3 test; `gmail_to_atom.py` deleted.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ingest-adapters-gmail-mcp/04-SPEC.md
@.planning/phases/04-ingest-adapters-gmail-mcp/04-RESEARCH.md
@.planning/phases/04-ingest-adapters-gmail-mcp/04-CONTEXT.md
@apps/ingest/gmail_to_atom.py
@libs/contracts/src/contracts/_item.py
@libs/ingest_common/src/ingest_common/persist.py
@libs/ingest_common/src/ingest_common/trigger.py
@libs/ingest_common/src/ingest_common/runtime.py
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 05)

- `gmail-mcp-server/Dockerfile` — node:22-slim + `npm install @shinzolabs/gmail-mcp@1.7.4`
- `gmail-mcp-server/entrypoint.sh` — launches the server in HTTP transport mode (PORT set), reading CLIENT_ID/CLIENT_SECRET/REFRESH_TOKEN from env
- `scripts/provision_gmail_oauth.py` — one-time host script: InstalledAppFlow (scopes gmail.readonly + gmail.metadata) → writes GMAIL_OAUTH2_REFRESH_TOKEN to .env
- `apps/ingest-gmail/mcp_client.py` — exports `async def init_mcp_session(client) -> str`, `async def mcp_call(client, session_id, method, params) -> dict`, read-tool wrappers (e.g. `list_messages`, `get_message`)
- `apps/ingest-gmail/gmail_ingest.py` — `fetch_items(...) -> list[Item]`, `async def ingest()`
- `apps/ingest-gmail/main.py` — `app = make_trigger_app(ingest, name="ingest-gmail")`
- `apps/ingest-gmail/Dockerfile`, `apps/ingest-gmail/requirements.txt`
- `tests/test_ingest_gmail.py` — R3 unit test with a mocked MCP server (httpx mock/monkeypatch)
- Deleted: `apps/ingest/gmail_to_atom.py`
- New env vars: `GMAIL_MCP_URL` (default http://gmail-mcp-server:3000), `GMAIL_OAUTH2_REFRESH_TOKEN`, `GMAIL_OAUTH2_CLIENT_ID`, `GMAIL_OAUTH2_CLIENT_SECRET`, `GMAIL_QUERY`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Gmail MCP server container (@shinzolabs/gmail-mcp) + OAuth2 provision script</name>
  <files>gmail-mcp-server/Dockerfile, gmail-mcp-server/entrypoint.sh, scripts/provision_gmail_oauth.py</files>
  <read_first>
    - .planning/phases/04-ingest-adapters-gmail-mcp/04-RESEARCH.md (Finding 1: use @shinzolabs/gmail-mcp@1.7.4 NOT @googleapis/...; Pattern 6 server Dockerfile; Pattern 7 provision script; Pitfall 3 stdio-vs-HTTP transport; Open Questions 1+2 endpoint path + transport activation)
    - .planning/phases/04-ingest-adapters-gmail-mcp/04-CONTEXT.md (D-06 scopes; D-07 write refresh token to .env via env_file)
    - apps/ingest/gmail_to_atom.py (legacy env var names GMAIL_USER/GMAIL_QUERY for continuity; the legacy app-password path is being replaced)
  </read_first>
  <action>
    Create `gmail-mcp-server/Dockerfile` per RESEARCH Pattern 6: `FROM node:22-slim`; `WORKDIR /app`; `RUN npm install @shinzolabs/gmail-mcp@1.7.4` (the OK/approved package from the Package Legitimacy Audit; supply-chain T-04-SC). Add `gmail-mcp-server/entrypoint.sh` that exports/passes `PORT` (default 3000) to force Streamable-HTTP transport (RESEARCH Pitfall 3 — without PORT the server defaults to stdio and the adapter gets connection refused) and launches the server (`npx @shinzolabs/gmail-mcp`), relying on CLIENT_ID/CLIENT_SECRET/REFRESH_TOKEN being present in the environment (supplied via env_file in Plan 06). Do NOT put any credential value in an ARG/ENV in the Dockerfile (NF-6). Resolve RESEARCH Open Questions 1+2 during execution: after building, run the container with `PORT=3000` and confirm (a) it binds an HTTP listener and (b) the JSON-RPC endpoint path (expected `/mcp`); record the confirmed endpoint path in the SUMMARY so Task 2's mcp_client uses the correct path. Create `scripts/provision_gmail_oauth.py` per RESEARCH Pattern 7: `InstalledAppFlow.from_client_secrets_file("client_secrets.json", SCOPES)` with SCOPES = [`https://www.googleapis.com/auth/gmail.readonly`, `https://www.googleapis.com/auth/gmail.metadata`] (D-06 — read-only + metadata only; NO send/modify/compose scope), `run_local_server(port=0)`, then idempotently rewrite the `GMAIL_OAUTH2_REFRESH_TOKEN=` line in `.env` with the obtained `creds.refresh_token` (D-07). Print a confirmation; do not print the token value.
  </action>
  <verify>
    <automated>grep -q '@shinzolabs/gmail-mcp@1.7.4' gmail-mcp-server/Dockerfile && grep -q 'gmail.readonly' scripts/provision_gmail_oauth.py && grep -q 'gmail.metadata' scripts/provision_gmail_oauth.py && ! grep -qiE 'gmail\.(send|modify|compose)' scripts/provision_gmail_oauth.py && ! grep -q '@googleapis/mcp-server-google-workspace' gmail-mcp-server/Dockerfile && echo OK</automated>
  </verify>
  <done>Gmail MCP server Dockerfile uses @shinzolabs/gmail-mcp@1.7.4 in HTTP transport mode; provision script requests only readonly+metadata scopes and writes the token to .env; confirmed MCP endpoint path recorded.</done>
  <acceptance_criteria>
    - gmail-mcp-server/Dockerfile contains `@shinzolabs/gmail-mcp@1.7.4` and `FROM node:22-slim`
    - gmail-mcp-server/Dockerfile does NOT contain `@googleapis/mcp-server-google-workspace`
    - gmail-mcp-server/entrypoint.sh sets/forwards `PORT` (HTTP transport, not stdio)
    - `grep -niE '^(ARG|ENV)\s+\S*(CLIENT_SECRET|REFRESH_TOKEN|CLIENT_ID)\s*=\S' gmail-mcp-server/Dockerfile` returns empty (no credential values baked in)
    - scripts/provision_gmail_oauth.py requests `gmail.readonly` and `gmail.metadata` and NO `gmail.send`/`gmail.modify`/`gmail.compose`
    - scripts/provision_gmail_oauth.py writes `GMAIL_OAUTH2_REFRESH_TOKEN` to `.env` and does not print the token value
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ingest-gmail MCP client adapter (httpx JSON-RPC) → Item + Dockerfile + R3 test</name>
  <files>apps/ingest-gmail/mcp_client.py, apps/ingest-gmail/gmail_ingest.py, apps/ingest-gmail/main.py, apps/ingest-gmail/requirements.txt, apps/ingest-gmail/Dockerfile, tests/test_ingest_gmail.py</files>
  <read_first>
    - .planning/phases/04-ingest-adapters-gmail-mcp/04-RESEARCH.md (Pattern 4 Gmail MCP client raw httpx JSON-RPC; Finding 4 Streamable-HTTP details: initialize first, Mcp-Session-Id header, Accept application/json+text/event-stream, tools/call)
    - .planning/phases/04-ingest-adapters-gmail-mcp/04-CONTEXT.md (D-05 raw httpx no SDK; D-10/D-11 Dockerfile)
    - apps/ingest-imap/Dockerfile (sibling COPY+pip install --no-deps pattern from Plan 02)
    - libs/contracts/src/contracts/_item.py
    - libs/ingest_common/src/ingest_common/persist.py / trigger.py / runtime.py
    - Task 1 SUMMARY note: confirmed MCP endpoint path
  </read_first>
  <behavior>
    - mcp_call posts a JSON-RPC 2.0 body to the MCP endpoint with Accept: application/json, text/event-stream and (after init) the Mcp-Session-Id header; init_mcp_session posts initialize (protocolVersion 2025-03-26) and returns the session id.
    - Only read tools are called (list_threads / list_messages / get_message). No write/mutate tool name appears in the source.
    - fetch_items turns N returned Gmail messages into N Items with source_type="gmail", a synthetic url unique per Gmail message id, title=subject, ts tz-aware, summary=snippet.
    - ingest() over a mocked MCP returning >=1 message produces >=1 row + >=1 item.ingested event; re-run yields no second event.
  </behavior>
  <action>
    Create `apps/ingest-gmail/mcp_client.py` per RESEARCH Pattern 4 + Finding 4 (D-05 — raw httpx, no mcp Python SDK): `async def mcp_call(client, session_id, method, params)` POSTing `{"jsonrpc":"2.0","method":method,"params":params,"id":<n>}` to `f"{GMAIL_MCP_URL}{ENDPOINT}"` (GMAIL_MCP_URL from env, default `http://gmail-mcp-server:3000`; ENDPOINT = the path confirmed in Task 1, default `/mcp`) with headers Content-Type application/json + Accept `application/json, text/event-stream` + (when session_id) `Mcp-Session-Id`; `async def init_mcp_session(client)` posts the `initialize` method (protocolVersion `2025-03-26`, clientInfo name `ingest-gmail`) and returns the session id from the result/headers; read-only tool wrappers calling `tools/call` with names `list_messages`/`get_message` (and/or `list_threads`/`get_thread`) only. Create `apps/ingest-gmail/gmail_ingest.py`: `fetch_items(messages)` mapping each Gmail message to `Item(source="gmail", source_type="gmail", url=f"gmail://message/{msg_id}", title=subject, ts=<tz-aware>, lang="und", summary=snippet[:500])`; `async def ingest()` that opens an `httpx.AsyncClient`, inits the session, lists messages (filtered by `GMAIL_QUERY` env, default `newer_than:7d`), fetches each, builds Items, and awaits `persist_and_publish(store, bus, item)` (store as context manager + bus from runtime), then closes the bus. Create `apps/ingest-gmail/main.py`: `from gmail_ingest import ingest`, `app = make_trigger_app(ingest, name="ingest-gmail")`. Create `apps/ingest-gmail/requirements.txt` (fastapi, uvicorn, aio-pika, psycopg[binary], pgvector, numpy, pydantic, PyYAML, httpx) and `apps/ingest-gmail/Dockerfile` per D-10/D-11 (python:3.12-slim; COPY+pip install --no-deps contracts/store/ingest_common; install requirements.txt; COPY apps/ingest-gmail/ .; uvicorn main:app port 8000; no credential ARG/ENV). Write `tests/test_ingest_gmail.py` (R3): mock the MCP transport (monkeypatch mcp_client.mcp_call / init_mcp_session, or stub httpx via a transport handler) to return one fake Gmail message; run ingest() against InMemoryStore + InMemoryBus; assert 1 row with source_type=="gmail", 1 item.ingested event, and that re-running yields no second event. Assert (static-in-test or via grep gate) that only read tool names are used.
  </action>
  <verify>
    <automated>pytest tests/test_ingest_gmail.py -x</automated>
  </verify>
  <done>ingest-gmail talks MCP via raw httpx, ingests read-only Gmail messages to Item (source_type=gmail), is idempotent; Dockerfile per D-10/D-11; R3 test green.</done>
  <acceptance_criteria>
    - apps/ingest-gmail/mcp_client.py contains `initialize` and `Mcp-Session-Id` and posts JSON-RPC (`"jsonrpc": "2.0"`)
    - `grep -niE 'send_message|send_email|create_draft|trash|modify_message|delete_message|mark_read|markAsRead' apps/ingest-gmail/mcp_client.py apps/ingest-gmail/gmail_ingest.py | grep -vE '^\s*[0-9]+:\s*#'` returns empty (read-only tools only)
    - `grep -rniE 'import mcp\b|from mcp ' apps/ingest-gmail/` returns empty (no Python MCP SDK — D-05)
    - apps/ingest-gmail/gmail_ingest.py contains `source_type="gmail"`
    - apps/ingest-gmail/Dockerfile contains `FROM python:3.12-slim` and `pip install --no-deps /build/contracts /build/store /build/ingest_common`
    - tests/test_ingest_gmail.py asserts 1 gmail row + 1 event on first run, 0 new events on second run
    - `pytest tests/test_ingest_gmail.py -x` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Retire legacy gmail_to_atom.py (SPEC R7)</name>
  <files>apps/ingest/gmail_to_atom.py</files>
  <read_first>
    - apps/ingest/gmail_to_atom.py (the file to delete)
    - .planning/phases/04-ingest-adapters-gmail-mcp/04-SPEC.md (R7 acceptance: git ls-files returns empty; no references in docker-compose or README except ADR-008 historical note)
  </read_first>
  <action>
    Delete `apps/ingest/gmail_to_atom.py` with `git rm apps/ingest/gmail_to_atom.py` (per SPEC R7 — the MCP path from Tasks 1+2 replaces it). Search the repo for remaining references and remove any operational reference to it in `README.md` and `docker-compose.yml` (a historical note in `docs/ARCHITECTURE.md` ADR-008 is allowed to remain). Do NOT delete `apps/ingest/imap_to_atom.py` or `apps/ingest/yt_to_atom.py` — those host scripts are retired later in Phase 7, not here. Note: the legacy `GMAIL_USER`/`GMAIL_APP_PASSWORD` env lines in `.env.example` are cleaned up in Plan 06 alongside the new Gmail OAuth2 vars.
  </action>
  <verify>
    <automated>test -z "$(git ls-files apps/ingest/gmail_to_atom.py)" && ! grep -rIl 'gmail_to_atom' docker-compose.yml README.md 2>/dev/null && echo OK</automated>
  </verify>
  <done>gmail_to_atom.py is gone from the repo; no operational references remain in docker-compose.yml or README.md.</done>
  <acceptance_criteria>
    - `git ls-files apps/ingest/gmail_to_atom.py` returns empty
    - `grep -rIl 'gmail_to_atom' docker-compose.yml README.md` finds no matches
    - apps/ingest/imap_to_atom.py and apps/ingest/yt_to_atom.py still exist (not deleted in this phase)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ingest-gmail → gmail-mcp-server (Docker network) | JSON-RPC over HTTP within the infotriage network |
| gmail-mcp-server → Google Gmail API | OAuth2 token-bearing; read-only scope |
| gmail-mcp-server / provision script ← .env | OAuth2 client secret + refresh token cross here at runtime only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-11 | Information Disclosure | OAuth2 refresh token in image | high | mitigate | env_file/runtime env only; no credential ARG/ENV in either Dockerfile (grep gate); token written only to gitignored .env |
| T-04-12 | Elevation of Privilege | Gmail OAuth2 scope | high | mitigate | provision requests only gmail.readonly + gmail.metadata; no send/modify/compose scope (grep gate) |
| T-04-13 | Tampering | Gmail mutate tools | high | mitigate | adapter calls read tools only; write tool names absent from source (grep gate) |
| T-04-14 | Spoofing | DNS rebinding on local MCP server | medium | mitigate | MCP server bound to 127.0.0.1:22025 (compose, Plan 06); @shinzolabs/gmail-mcp validates Origin per MCP spec |
| T-04-SC | Tampering | npm @shinzolabs/gmail-mcp (supply chain) | high | mitigate | pinned @1.7.4, OK/Approved in Package Legitimacy Audit; nonexistent @googleapis/... REMOVED; [SUS] mcp-google-workspace NOT used |
</threat_model>

<verification>
- `pytest tests/test_ingest_gmail.py -x` green (R3 + idempotency).
- Package/scope/read-only/credential grep gates pass.
- `git ls-files apps/ingest/gmail_to_atom.py` empty (R7).
- Wave-merge integration (Postgres + RabbitMQ + provisioned token + running MCP server): MCP server starts healthy; a real ingest-gmail run produces >=1 gmail row + >=1 item.ingested event.
</verification>

<success_criteria>
The Gmail MCP server runs with @shinzolabs/gmail-mcp@1.7.4 holding an env-only read-only OAuth2 token (ADR-008); ingest-gmail reads Gmail via raw httpx JSON-RPC (D-05), normalizes to Item (source_type=gmail), is read-only (ADR-004) and idempotent (R3); the legacy gmail_to_atom.py is deleted (R7).
</success_criteria>

<output>
Create `.planning/phases/04-ingest-adapters-gmail-mcp/04-05-SUMMARY.md` when done. Record the confirmed MCP endpoint path and transport-activation detail (RESEARCH Open Questions 1+2).
</output>
