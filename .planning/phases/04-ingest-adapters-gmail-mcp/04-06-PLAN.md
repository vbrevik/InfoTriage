---
phase: 04-ingest-adapters-gmail-mcp
plan: 06
type: execute
wave: 3
depends_on: [04-02, 04-03, 04-04, 04-05]
files_modified:
  - apps/scheduler/scheduler_main.py
  - apps/scheduler/main.py
  - apps/scheduler/requirements.txt
  - apps/scheduler/Dockerfile
  - tests/test_scheduler.py
  - docker-compose.yml
  - .env.example
autonomous: true
requirements: [ADR-003, ADR-004, ADR-008]
must_haves:
  truths:
    - "The scheduler container fires all four adapters (imap, youtube, gmail, obsidian) on per-adapter cron schedules read from env (SCHEDULE_IMAP/YOUTUBE/GMAIL/OBSIDIAN) via APScheduler 3.x BackgroundScheduler + CronTrigger.from_crontab (SPEC R5; D-04)"
    - "When an adapter's previous run is still in progress, the scheduler's next trigger receives 409 and logs it as 'skipped — already running' rather than starting a concurrent run; 200 is logged as 'started' (SPEC R5 adjacency — covered edge; D-04)"
    - "docker-compose.yml defines all 6 new services (ingest-imap, ingest-youtube, ingest-gmail, ingest-obsidian, scheduler, gmail-mcp-server) on the infotriage network with build context=project root and per-adapter Dockerfile (RESEARCH Pitfall 2)"
    - "All credentials (POSTGRES_PASSWORD, RABBITMQ creds, Gmail OAuth2 client secret/refresh token, IMAP passwords) reach containers via env_file only — never in image layers (NF-6)"
  artifacts:
    - apps/scheduler/scheduler_main.py
    - apps/scheduler/main.py
    - apps/scheduler/Dockerfile
    - apps/scheduler/requirements.txt
    - tests/test_scheduler.py
    - docker-compose.yml
    - .env.example
  key_links:
    - "scheduler fires triggers with httpx.Client (SYNC) inside APScheduler 3.x thread-pool jobs — NOT httpx.AsyncClient (RESEARCH Pitfall 1: APScheduler 3.x jobs run in threads with no event loop); it posts to http://<adapter>:8000/run on the infotriage network and branches on 200 vs 409"
    - "adapter trigger ports are published host-side on 127.0.0.1 in the 22010–22014 band (D-03); inter-container calls use service name + internal port 8000"
    - "gmail-mcp-server host port entry is 127.0.0.1:22025:3000 (localhost-only) — never 0.0.0.0/bare 22025 (ADR-008, NF-6)"
  prohibitions:
    - statement: "the Gmail MCP server port :22025 MUST NOT be bound to 0.0.0.0 — it must be 127.0.0.1:22025:3000 in docker-compose.yml"
      status: resolved
      verification: "test/static — grep docker-compose.yml shows 127.0.0.1:22025:3000; a grep for a 22025 port entry without the 127.0.0.1 host prefix returns empty"
    - statement: "no credential value MUST be baked into any container image layer — all secrets via env_file at runtime"
      status: resolved
      verification: "test/static — each new service uses env_file (or scoped environment referencing host env), and no Dockerfile carries ARG/ENV credential values (verified per-adapter in Plans 02–05; compose adds no build-args carrying secrets)"
---

<objective>
Wire everything together. Build the `scheduler` container (APScheduler 3.x — NOT 4.x, RESEARCH Finding 3) that fires each adapter's `POST /run` on a per-adapter cron schedule and treats a 409 as a logged skip (D-04, SPEC R5). Then add all six new services to `docker-compose.yml` with correct build contexts (project root — RESEARCH Pitfall 2), localhost-only port mappings in the 22010–22014 band (D-03) plus the localhost-only Gmail MCP port `127.0.0.1:22025:3000` (ADR-008), env_file for credentials (NF-6), and the read-only Obsidian vault bind-mount + writable data/feeds mount. Finally update `.env.example` with the new env vars and drop the legacy Gmail app-password lines.

Purpose: deliver the scheduler (SPEC R5) and the operable multi-service compose surface (D-03/D-04, ADR-008), closing the phase.
Output: `apps/scheduler/` + R5 test, updated `docker-compose.yml`, updated `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ingest-adapters-gmail-mcp/04-SPEC.md
@.planning/phases/04-ingest-adapters-gmail-mcp/04-RESEARCH.md
@.planning/phases/04-ingest-adapters-gmail-mcp/04-CONTEXT.md
@docker-compose.yml
@apps/ingest-imap/Dockerfile
@apps/ingest-gmail/Dockerfile
@gmail-mcp-server/Dockerfile
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 06)

- `apps/scheduler/scheduler_main.py` — `ADAPTERS` map (name → (cron, url)), `fire_adapter(name, url)` (sync httpx, 200/409 branching + logging), `build_scheduler()` / run loop using APScheduler 3.x BackgroundScheduler + CronTrigger.from_crontab
- `apps/scheduler/main.py` — entrypoint that builds + starts the scheduler and blocks
- `apps/scheduler/Dockerfile`, `apps/scheduler/requirements.txt`
- `tests/test_scheduler.py` — R5: first trigger 200→"started", overlapping 409→"skipped" logged
- `docker-compose.yml` — 6 new services added
- `.env.example` — new env vars added, legacy GMAIL_APP_PASSWORD path removed
- New env vars: `SCHEDULE_IMAP`, `SCHEDULE_YOUTUBE`, `SCHEDULE_GMAIL`, `SCHEDULE_OBSIDIAN`, `INFOTRIAGE_AMQP_DSN`, `INFOTRIAGE_BLOB_ROOT`, `OBSIDIAN_VAULT_PATH`, `GMAIL_MCP_URL`, `GMAIL_OAUTH2_CLIENT_ID`, `GMAIL_OAUTH2_CLIENT_SECRET`, `GMAIL_OAUTH2_REFRESH_TOKEN`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scheduler container (APScheduler 3.x, 409=skip) + R5 test</name>
  <files>apps/scheduler/scheduler_main.py, apps/scheduler/main.py, apps/scheduler/requirements.txt, apps/scheduler/Dockerfile, tests/test_scheduler.py</files>
  <read_first>
    - .planning/phases/04-ingest-adapters-gmail-mcp/04-RESEARCH.md (Pattern 3 APScheduler 3.x scheduler; Finding 3 use 3.x not 4.x; Pitfall 1 sync httpx in thread jobs)
    - .planning/phases/04-ingest-adapters-gmail-mcp/04-CONTEXT.md (D-04 scheduler behavior + per-adapter cron env; D-03 ports)
    - apps/ingest-imap/Dockerfile (sibling COPY+pip install pattern; scheduler does NOT need store/contracts but reuse python:3.12-slim base + structure)
  </read_first>
  <behavior>
    - fire_adapter posts to the adapter URL with a sync httpx.Client; on 200 logs "started"; on 409 logs "skipped — already running"; on other status logs a warning; on connection error logs an error (never raises out of the job).
    - The scheduler registers one cron job per adapter from the env-provided crontab strings via CronTrigger.from_crontab.
    - A simulated overlapping run (adapter returns 409 on the second call) is logged as skipped, NOT started, and no concurrent invocation is attempted.
  </behavior>
  <action>
    Create `apps/scheduler/scheduler_main.py` per RESEARCH Pattern 3 (D-04; APScheduler 3.x — `from apscheduler.schedulers.background import BackgroundScheduler` and `from apscheduler.triggers.cron import CronTrigger`; NOT the 4.x `from apscheduler import Scheduler` per Finding 3). Define `ADAPTERS = {"ingest-imap": (os.getenv("SCHEDULE_IMAP","0 */2 * * *"), "http://ingest-imap:8000/run"), "ingest-youtube": (os.getenv("SCHEDULE_YOUTUBE","0 */4 * * *"), "http://ingest-youtube:8000/run"), "ingest-gmail": (os.getenv("SCHEDULE_GMAIL","0 */2 * * *"), "http://ingest-gmail:8000/run"), "ingest-obsidian": (os.getenv("SCHEDULE_OBSIDIAN","*/30 * * * *"), "http://ingest-obsidian:8000/run")}` (inter-container URLs use service name + internal port 8000 — D-03). Implement `fire_adapter(name, url)` with a SYNC `httpx.Client` (RESEARCH Pitfall 1 — APScheduler 3.x jobs run in threads with no event loop, so AsyncClient is wrong): post with a short timeout; 200 → log info "[name] started (200)"; 409 → log info "[name] skipped — already running (409)"; other → log warning; `httpx.RequestError` → log error; never propagate. Implement `build_scheduler()` adding one job per adapter via `CronTrigger.from_crontab(cron)`. Create `apps/scheduler/main.py` that builds + starts the scheduler and blocks (sleep loop with graceful shutdown on KeyboardInterrupt/SystemExit). Create `apps/scheduler/requirements.txt` (`apscheduler>=3.11,<4`, `httpx>=0.27`) and `apps/scheduler/Dockerfile` (`FROM python:3.12-slim`; install requirements.txt; `COPY apps/scheduler/ .`; `CMD ["python", "main.py"]`; no credential ARG/ENV). Write `tests/test_scheduler.py` (R5): monkeypatch `httpx.Client.post` (or inject a fake) so the first call returns a 200 response and the second returns 409; call `fire_adapter` twice; assert via `caplog` that the first logs "started" and the second logs "skipped" (the 409/already-running path), and that no exception is raised. Pin the APScheduler 3.x import path in a test assertion or import-smoke.
  </action>
  <verify>
    <automated>pytest tests/test_scheduler.py -x</automated>
  </verify>
  <done>scheduler uses APScheduler 3.x + sync httpx; 409 logged as skip; R5 test green.</done>
  <acceptance_criteria>
    - apps/scheduler/scheduler_main.py contains `from apscheduler.schedulers.background import BackgroundScheduler` and `CronTrigger.from_crontab`
    - `grep -nE 'from apscheduler import Scheduler|AsyncIOScheduler|httpx\.AsyncClient' apps/scheduler/scheduler_main.py` returns empty (no 4.x API, no async client in thread jobs)
    - scheduler_main.py references `SCHEDULE_IMAP`, `SCHEDULE_YOUTUBE`, `SCHEDULE_GMAIL`, `SCHEDULE_OBSIDIAN` and four `:8000/run` URLs
    - tests/test_scheduler.py asserts a 200→"started" log and a 409→"skipped" log across two fire_adapter calls
    - `pytest tests/test_scheduler.py -x` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: docker-compose.yml — add 6 services (ports D-03, localhost Gmail :22025, env_file, mounts)</name>
  <files>docker-compose.yml</files>
  <read_first>
    - docker-compose.yml (existing services, the infotriage network block, the 127.0.0.1 localhost-only port style on postgres/rabbitmq, env defaults pattern)
    - .planning/phases/04-ingest-adapters-gmail-mcp/04-CONTEXT.md (D-03 port band 22010–22014; gmail-mcp :22025; env_file pattern; D-10/D-11 build context)
    - .planning/phases/04-ingest-adapters-gmail-mcp/04-RESEARCH.md (Pitfall 2 build context = project root; Pitfall 4 vault bind-mount; system diagram service names/ports)
    - apps/ingest-imap/Dockerfile, apps/ingest-youtube/Dockerfile, apps/ingest-obsidian/Dockerfile, apps/ingest-gmail/Dockerfile, gmail-mcp-server/Dockerfile (the dockerfiles compose references)
  </read_first>
  <action>
    Edit `docker-compose.yml` to add six services on the existing `infotriage` network, each `restart: unless-stopped`, each `env_file: [.env]` so credentials arrive at runtime (NF-6). For the four adapters use `build: { context: ., dockerfile: apps/<name>/Dockerfile }` (project-root context — RESEARCH Pitfall 2), `depends_on: [postgres, rabbitmq]`, environment carrying `INFOTRIAGE_PG_DSN`, `INFOTRIAGE_AMQP_DSN` (e.g. `amqp://...@rabbitmq:5672`), `INFOTRIAGE_BLOB_ROOT=/data/blobs`, and a `./data:/data` (or `./data/blobs:/data/blobs`) mount so blobs persist; publish trigger ports localhost-only per D-03: `ingest-imap` `127.0.0.1:22010:8000`, `ingest-youtube` `127.0.0.1:22011:8000`, `ingest-gmail` `127.0.0.1:22012:8000`, `ingest-obsidian` `127.0.0.1:22013:8000`. `ingest-youtube` also mounts `./data/feeds:/app/data/feeds` (writable, for the Atom dual output consumed by the existing `feeds` server). `ingest-obsidian` mounts `${OBSIDIAN_VAULT_PATH}/articles-inbox:/vault/articles-inbox:ro` (READ-ONLY) and sets `OBSIDIAN_VAULT_PATH=/vault`. Add `gmail-mcp-server` with `build: { context: ., dockerfile: gmail-mcp-server/Dockerfile }`, `env_file: [.env]`, environment mapping `PORT=3000`, `CLIENT_ID=${GMAIL_OAUTH2_CLIENT_ID}`, `CLIENT_SECRET=${GMAIL_OAUTH2_CLIENT_SECRET}`, `REFRESH_TOKEN=${GMAIL_OAUTH2_REFRESH_TOKEN}`, and port `127.0.0.1:22025:3000` (LOCALHOST-ONLY — never `0.0.0.0`/bare `22025:3000`; ADR-008). Add `ingest-gmail` env `GMAIL_MCP_URL=http://gmail-mcp-server:3000` + `depends_on: [postgres, rabbitmq, gmail-mcp-server]`. Add `scheduler` with `build: { context: ., dockerfile: apps/scheduler/Dockerfile }`, `env_file: [.env]`, the four `SCHEDULE_*` env vars, `127.0.0.1:22014:8000` (or no published port if it has none), and `depends_on` the four adapters. Do NOT add any build `args` carrying secrets. Validate with `docker compose config`.
  </action>
  <verify>
    <automated>docker compose config >/dev/null && grep -q '127.0.0.1:22025:3000' docker-compose.yml && ! grep -nE '(^|[^.0-9])22025:3000' docker-compose.yml | grep -qv '127.0.0.1:22025:3000' && grep -q ':ro' docker-compose.yml && echo OK</automated>
  </verify>
  <done>All 6 services present; Gmail MCP is 127.0.0.1:22025:3000; adapters on 22010–22014 localhost-only; vault mount is :ro; data/feeds writable for youtube; `docker compose config` valid.</done>
  <acceptance_criteria>
    - docker-compose.yml defines services `ingest-imap`, `ingest-youtube`, `ingest-gmail`, `ingest-obsidian`, `scheduler`, `gmail-mcp-server`
    - docker-compose.yml contains `127.0.0.1:22025:3000` and a grep for a `22025:3000` entry lacking the `127.0.0.1` prefix returns empty
    - docker-compose.yml contains `127.0.0.1:22010:8000`, `127.0.0.1:22011:8000`, `127.0.0.1:22012:8000`, `127.0.0.1:22013:8000`
    - docker-compose.yml ingest-obsidian vault mount ends with `:ro`
    - each new service has `env_file` referencing `.env`; no `build` block carries an `args:` secret
    - `docker compose config` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: .env.example — add Phase 4 env vars, drop legacy Gmail app-password path</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (current contents; note the existing Gmail bridge / IMAP / YouTube sections and the GMAIL_USER/GMAIL_APP_PASSWORD/GMAIL_QUERY block)
    - .planning/phases/04-ingest-adapters-gmail-mcp/04-CONTEXT.md (D-03/D-04/D-07 env var names)
    - apps/scheduler/scheduler_main.py (the SCHEDULE_* defaults to document)
  </read_first>
  <action>
    Edit `.env.example` (a template — never holds real secrets): add a Phase 4 section documenting the new env vars with empty/placeholder values and a one-line comment each — `INFOTRIAGE_AMQP_DSN` (e.g. amqp://infotriage:infotriage_rmq@rabbitmq:5672), `INFOTRIAGE_BLOB_ROOT` (=/data/blobs in container), `OBSIDIAN_VAULT_PATH` (host path to the Obsidian vault, bind-mounted :ro), `GMAIL_MCP_URL` (=http://gmail-mcp-server:3000), `GMAIL_OAUTH2_CLIENT_ID`, `GMAIL_OAUTH2_CLIENT_SECRET`, `GMAIL_OAUTH2_REFRESH_TOKEN` (set by scripts/provision_gmail_oauth.py — D-07), `GMAIL_QUERY` (default newer_than:7d), and the four cron vars `SCHEDULE_IMAP`/`SCHEDULE_YOUTUBE`/`SCHEDULE_GMAIL`/`SCHEDULE_OBSIDIAN` with their defaults as comments. Remove the legacy `GMAIL_USER` and `GMAIL_APP_PASSWORD` lines (the app-password Gmail path is retired in Plan 05); keep `INFOTRIAGE_PG_DSN`/Postgres + RabbitMQ credential placeholders. Do not place any real token value in the file.
  </action>
  <verify>
    <automated>grep -q 'GMAIL_OAUTH2_REFRESH_TOKEN' .env.example && grep -q 'OBSIDIAN_VAULT_PATH' .env.example && grep -q 'SCHEDULE_IMAP' .env.example && ! grep -q 'GMAIL_APP_PASSWORD' .env.example && echo OK</automated>
  </verify>
  <done>.env.example documents all new Phase 4 vars; legacy GMAIL_APP_PASSWORD removed; no real secrets present.</done>
  <acceptance_criteria>
    - .env.example contains `GMAIL_OAUTH2_REFRESH_TOKEN`, `GMAIL_OAUTH2_CLIENT_ID`, `GMAIL_OAUTH2_CLIENT_SECRET`, `OBSIDIAN_VAULT_PATH`, `INFOTRIAGE_AMQP_DSN`, `GMAIL_MCP_URL`
    - .env.example contains all four `SCHEDULE_*` vars
    - `grep -c 'GMAIL_APP_PASSWORD' .env.example` returns 0
    - no line in .env.example assigns a non-empty real token/secret value (placeholders/empty only)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → published adapter ports | only localhost (127.0.0.1) may reach trigger/MCP ports |
| containers ← .env (env_file) | all credentials cross here at runtime, never in images |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Elevation of Privilege | gmail-mcp-server port exposure | high | mitigate | 127.0.0.1:22025:3000 in compose (localhost-only); grep gate forbids a 22025 entry without the 127.0.0.1 prefix |
| T-04-16 | Information Disclosure | credentials in image layers | high | mitigate | env_file at runtime; no build args carrying secrets; per-adapter Dockerfiles credential-free (Plans 02–05) |
| T-04-17 | Tampering | Obsidian vault write via mount | medium | mitigate | vault bind-mount is :ro in compose |
| T-04-18 | Elevation of Privilege | adapter trigger ports exposed | medium | mitigate | trigger ports published on 127.0.0.1 only (22010–22014 band, D-03) |
</threat_model>

<verification>
- `pytest tests/test_scheduler.py -x` green (R5).
- `docker compose config` valid; localhost-only Gmail port + :ro vault + 22010–22014 band gates pass.
- `.env.example` updated; legacy app-password var removed.
- Phase smoke (manual/ops): `docker compose up` builds all 6 services; scheduler fires adapters on schedule; an overlapping run is logged as skipped.
</verification>

<success_criteria>
The scheduler fires all four adapters on per-adapter cron schedules and skips overlapping runs (R5, D-04); docker-compose declares all 6 services with correct project-root build contexts, localhost-only ports (D-03 + Gmail :22025 ADR-008), env_file credentials (NF-6), read-only vault and writable feeds mounts; .env.example documents the new surface and drops the retired app-password path.
</success_criteria>

<output>
Create `.planning/phases/04-ingest-adapters-gmail-mcp/04-06-SUMMARY.md` when done.
</output>
