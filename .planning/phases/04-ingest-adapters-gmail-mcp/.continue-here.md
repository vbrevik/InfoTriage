---
context: phase
phase: 04-ingest-adapters-gmail-mcp
task: UAT in progress
total_tasks: 6 UAT items
status: in_progress
last_updated: 2026-06-29T17:29:26.550Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking code anti-patterns. UAT blockers are credential/config gaps (see below)._

<current_state>
Phase 04 execution complete — all 6 plans done, 195 tests pass. Currently in UAT phase.
UAT items 1 (docker build) and 6 (409 lock) verified ✅. Items 2-5 blocked on credentials.
Docker stack running: postgres, rabbitmq, ingest-imap, ingest-youtube.
</current_state>

<completed_work>
- 04-01 ingest_common library (persist_and_publish, make_trigger_app, build_store/bus) ✅
- 04-02 ingest-imap adapter ✅
- 04-03 ingest-youtube adapter (dual output: Item+blob + Atom XML) ✅
- 04-04 ingest-obsidian adapter (read-only vault) ✅
- 04-05 gmail-mcp-server + ingest-gmail (OAuth2/httpx, gmail_to_atom.py retired) ✅
- 04-06 scheduler (APScheduler 3.x, 409=skip) + docker-compose wiring ✅
- Bug fixes committed (f1d204b): feedgen dep in all adapter requirements.txt + libs/store/pyproject.toml; gmail-mcp-server COPY path fixed
- UAT-1: docker compose build all 6 images ✅
- UAT-6: concurrent POST → 200 + 409 ✅
</completed_work>

<remaining_work>
- UAT-2: Live IMAP run — blocked on Gmail app password + Hover password
- UAT-3: Live YouTube run — blocked on DB table mismatch (adapter uses 'items', schema has 'articles')
- UAT-4: Gmail OAuth2 provision — needs browser: `python scripts/provision_gmail_oauth.py`
- UAT-5: Obsidian clip ingest — OBSIDIAN_VAULT_PATH not set in .env
- Mark phase 04 complete via `gsd_run query phase.complete 04` once UAT passes
</remaining_work>

<decisions_made>
- YouTube channels: Bellingcat, ISW, NATO, NRK Nyheter, Andrej Karpathy, NVIDIA (chosen from CCIR topics)
- Hover IMAP: user=vidar@brevik.net, host=mail.hover.com (password still needed)
- Gmail IMAP: user=vbrevik@gmail.com, host=imap.gmail.com (app password regeneration needed)
</decisions_made>

<blockers>
1. **Gmail IMAP auth failed** — `imaplib.IMAP4.error: [AUTHENTICATIONFAILED] Invalid credentials`
   → Regenerate at https://myaccount.google.com/apppasswords, update MAILBOXES in .env
   → Current value in .env: `metatjernobyl517` (may be expired or IMAP not enabled in Gmail settings)

2. **Hover IMAP password** — `TODO_HOVER_PASS` still placeholder in .env MAILBOXES JSON
   → User is vidar@brevik.net at mail.hover.com

3. **YouTube DB table mismatch** — adapter calls `persist_and_publish` which writes to `items` table,
   but DB schema has `articles` table. Check `libs/store/src/store/_protocol.py` for table name.
   Fix: either update adapter to use correct table name, or add items table migration.

4. **Gmail OAuth2** — `GMAIL_OAUTH2_CLIENT_ID/SECRET/REFRESH_TOKEN` all empty in .env
   → Run: `python scripts/provision_gmail_oauth.py` (opens browser for Google consent)

5. **Obsidian vault** — `OBSIDIAN_VAULT_PATH` empty in .env
   → Find: `find ~/Library ~/Documents -name ".obsidian" -maxdepth 5 -type d 2>/dev/null`
</blockers>

## Required Reading
1. `.planning/phases/04-ingest-adapters-gmail-mcp/04-UAT.md` — UAT checklist with expected behaviors
2. `.planning/phases/04-ingest-adapters-gmail-mcp/04-VERIFICATION.md` — verification report (26/26 automated ✅)

## Infrastructure State
- `infotriage-postgres` — running on 127.0.0.1:22000 (healthy)
- `infotriage-rabbitmq` — running on 127.0.0.1:22001 (healthy)
- `infotriage-ingest-imap` — running on 127.0.0.1:22010 (up, auth failing on run)
- `infotriage-ingest-youtube` — running on 127.0.0.1:22011 (up, needs items table)
- `ingest-obsidian`, `ingest-gmail`, `gmail-mcp-server`, `scheduler` — not started (missing creds)

## .env State (as of pause)
- `MAILBOXES` — Gmail configured, Hover user set (vidar@brevik.net), Hover password = `TODO_HOVER_PASS`
- `YT_CHANNELS` — 6 channels set (Bellingcat, ISW, NATO, NRK, Karpathy, NVIDIA)
- `OBSIDIAN_VAULT_PATH` — empty
- `GMAIL_OAUTH2_*` — all empty

<context>
All code is done and correct. 195 tests pass. The only remaining work is UAT credential setup and one possible DB table name bug. Once credentials are added and the table mismatch is resolved, UAT 2-5 should pass on first try and phase can be marked complete.
</context>

<next_action>
1. Fix YouTube table mismatch: grep 'items\|articles' libs/store/src/store/ to find actual table name used by persist_and_publish
2. Fix Gmail IMAP: regenerate app password → update .env MAILBOXES JSON
3. Add Hover IMAP password to .env MAILBOXES JSON
4. Run `python scripts/provision_gmail_oauth.py` for Gmail OAuth2
5. Find Obsidian vault path and set OBSIDIAN_VAULT_PATH in .env
6. Re-trigger all adapters and verify UAT 2-5 pass
7. `gsd_run query phase.complete 04` to mark phase complete
</next_action>
