---
phase: 00-concept-spike
plan: 07
type: execute
wave: 4
depends_on: ["00-02", "00-03", "00-04", "00-05", "00-06"]
files_modified:
  - .planning/phases/00-concept-spike/SPIKE-FINDINGS.md
  - docs/adr/ADR-005-cop-world-monitor.md
  - docs/adr/ADR-006-microservice-architecture-entity-resolution.md
  - docs/adr/ADR-007-rabbitmq-bus.md
  - docs/adr/ADR-008-self-hosted-mcp-oauth2-ingestion.md
autonomous: false
requirements: [R1, R2, R3, R4, R5, ADR-006, ADR-007]
user_setup: []

must_haves:
  truths:
    - "SPIKE-FINDINGS.md exists in the phase dir with a per-unknown go/no-go/partial verdict for all five of R1-R5"
    - "ADR-005 (COP/World Monitor), ADR-006 (microservice arch + entity resolution), ADR-007 (RabbitMQ), ADR-008 (self-hosted MCP/OAuth2 ingestion) each carry their decision"
    - "Every partial outcome is recorded as a partial, never silently elevated to a pass"
    - "After the durable artifacts are written, all throwaway spike code under .spike/ is deleted and the spike containers torn down (D-06)"
    - "No spike code was merged into apps/ or libs/"
  artifacts:
    - path: ".planning/phases/00-concept-spike/SPIKE-FINDINGS.md"
      provides: "Consolidated per-unknown go/no-go/partial verdicts + raw numbers (the durable record)"
      ephemeral: false
    - path: "docs/adr/ADR-005-cop-world-monitor.md"
      provides: "COP / World Monitor adopt-build-drop decision (from R5)"
      ephemeral: false
    - path: "docs/adr/ADR-006-microservice-architecture-entity-resolution.md"
      provides: "Microservice architecture + entity-resolution decision (from R3)"
      ephemeral: false
    - path: "docs/adr/ADR-007-rabbitmq-bus.md"
      provides: "RabbitMQ topology decision (from R1)"
      ephemeral: false
    - path: "docs/adr/ADR-008-self-hosted-mcp-oauth2-ingestion.md"
      provides: "Self-hosted MCP/OAuth2 ingestion decision (carried from the prior Gmail MCP proof)"
      ephemeral: false
  key_links:
    - from: ".planning/phases/00-concept-spike/findings/R1-VERDICT.md (and R2-R5)"
      to: ".planning/phases/00-concept-spike/SPIKE-FINDINGS.md"
      via: "consolidates each durable per-unknown verdict fragment into the single findings record"
      pattern: "go|no-go|partial"
    - from: "SPIKE-FINDINGS.md"
      to: "docs/adr/ADR-005..008"
      via: "each ADR's decision is sourced from the corresponding unknown's verdict"
      pattern: "ADR-00[5-8]"
  prohibitions:
    - statement: "MUST NOT call a cloud LLM in any spike runtime"
      status: resolved
      verification: "Closeout writes docs only; no LLM calls"
    - statement: "MUST NOT mutate production data or the running pipeline"
      status: resolved
      verification: "Teardown removes only .spike/ + spike containers (down -v); never touches data/ or the :8088/:3000 stack"
    - statement: "MUST NOT merge throwaway spike code into apps/ or libs/"
      status: resolved
      verification: "Pre-teardown grep confirms no spike artifacts under apps/ or libs/; .spike/ deleted (D-06)"
---

<objective>
Convert the five raw verdicts into the only durable deliverables of this phase and then dispose of
the throwaway: write SPIKE-FINDINGS.md (per-unknown go/no-go/partial) and the four ADR stubs
(005-008), each carrying its decision, then tear down the spike containers and delete .spike/ (D-06).

Purpose: the phase's value is KNOWLEDGE, not code. ADRs + SPIKE-FINDINGS.md feed planning for P2
(storage), P3 (bus), P5 (dedup), P8 (entity resolution), P10 (Wiki-LLM), and SP-COP.
Output: SPIKE-FINDINGS.md, docs/adr/ADR-005..008, a clean tree (.spike/ gone, containers down).
</objective>

## Artifacts This Phase Produces (this plan)

| Artifact | Kind | Durable? |
|----------|------|----------|
| `.planning/phases/00-concept-spike/SPIKE-FINDINGS.md` | consolidated verdicts | **Durable** |
| `docs/adr/ADR-005-cop-world-monitor.md` | ADR stub | **Durable** |
| `docs/adr/ADR-006-microservice-architecture-entity-resolution.md` | ADR stub | **Durable** |
| `docs/adr/ADR-007-rabbitmq-bus.md` | ADR stub | **Durable** |
| `docs/adr/ADR-008-self-hosted-mcp-oauth2-ingestion.md` | ADR stub | **Durable** |
| `.spike/` (all throwaway code + data) | scratch | **DELETED** (D-06) |
| RabbitMQ + pgvector spike containers | runtime | **TORN DOWN** (docker compose down -v) |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-concept-spike/00-SPEC.md
@.planning/phases/00-concept-spike/00-CONTEXT.md
@.planning/STATE.md
@.planning/phases/00-concept-spike/00-02-SUMMARY.md
@.planning/phases/00-concept-spike/00-03-SUMMARY.md
@.planning/phases/00-concept-spike/00-04-SUMMARY.md
@.planning/phases/00-concept-spike/00-05-SUMMARY.md
@.planning/phases/00-concept-spike/00-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate SPIKE-FINDINGS.md and write ADR-005..008</name>
  <files>.planning/phases/00-concept-spike/SPIKE-FINDINGS.md, docs/adr/ADR-005-cop-world-monitor.md, docs/adr/ADR-006-microservice-architecture-entity-resolution.md, docs/adr/ADR-007-rabbitmq-bus.md, docs/adr/ADR-008-self-hosted-mcp-oauth2-ingestion.md</files>
  <read_first>
    - .planning/phases/00-concept-spike/findings/R1-VERDICT.md, R2-VERDICT.md, R3-VERDICT.md, R4-VERDICT.md, R5-VERDICT.md
    - .planning/phases/00-concept-spike/00-SPEC.md (acceptance bars + Prohibitions + the Constraint that partials are never a silent pass)
    - .planning/STATE.md (the locked re-architecture assumptions; ADR-008 carries the existing Gmail OAuth2/MCP proof)
    - docs/ARCHITECTURE.md (ADR-001..004 style + numbering to continue)
  </read_first>
  <action>
    Read the five durable verdict fragments under findings/ plus each plan SUMMARY. Write
    `.planning/phases/00-concept-spike/SPIKE-FINDINGS.md` with one section per unknown R1-R5, each
    stating the go/no-go/partial verdict and the raw numbers (R1: round-trip + DLQ; R2: chosen model
    + threshold + collapse_rate + control_overmerge; R3: NATO merge count + languages + control split;
    R4: citation count + coherence judgment + pasted samples; R5: adopt/build/drop + setup friction +
    brief comparison). Preserve every partial AS a partial (SPEC Constraints) — do not round up. Then
    write the four ADR stubs under docs/adr/ continuing the docs/ARCHITECTURE.md ADR style: ADR-005
    (COP/World Monitor) carrying the R5 adopt/build/drop; ADR-006 (microservice architecture + entity
    resolution) carrying the R3 entity-resolution decision (entities + entity_links + pgvector cosine
    linking) and the microservice frame from the locked re-architecture; ADR-007 (RabbitMQ) carrying
    the R1 topology decision (topic exchange infotriage.events, the 4 routing keys, DLX/DLQ);
    ADR-008 (self-hosted MCP/OAuth2 ingestion) carrying the existing Gmail OAuth2/MCP proof from
    STATE.md. Each ADR records Status, Context, Decision, and Consequences. Do NOT write any spike
    code into apps/ or libs/.
  </action>
  <verify>
    <automated>test -f .planning/phases/00-concept-spike/SPIKE-FINDINGS.md && for n in 5 6 7 8; do ls docs/adr/ADR-00$n-*.md >/dev/null || exit 1; done && grep -cEi 'R1|R2|R3|R4|R5' .planning/phases/00-concept-spike/SPIKE-FINDINGS.md</automated>
  </verify>
  <acceptance_criteria>
    - [ ] SPIKE-FINDINGS.md has a go/no-go/partial verdict for each of R1-R5 with raw numbers
    - [ ] docs/adr/ADR-005, ADR-006, ADR-007, ADR-008 each exist with Status/Context/Decision/Consequences
    - [ ] R4 samples are pasted into the findings (survive .spike/ deletion)
    - [ ] No partial was elevated to a pass
  </acceptance_criteria>
  <done>SPIKE-FINDINGS.md (R1-R5 verdicts) + docs/adr/ADR-005..008 written with decisions; no spike code in apps/ or libs/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Confirm durable artifacts, then tear down containers and delete .spike/ (D-06)</name>
  <read_first>
    - .planning/phases/00-concept-spike/00-CONTEXT.md (D-06: delete throwaway spike code after ADRs + SPIKE-FINDINGS.md written)
    - .planning/phases/00-concept-spike/SPIKE-FINDINGS.md + docs/adr/ADR-005..008 (the durable record to confirm before deletion)
  </read_first>
  <action>
    Pause for the human to confirm the durable artifacts are complete, then on approval run the
    destructive teardown (docker compose down -v + rm -rf .spike/) per D-06.
  </action>
  <what-built>
    Task 1 wrote the durable record (SPIKE-FINDINGS.md + ADR-005..008). The next step is DESTRUCTIVE
    and irreversible: `docker compose -f .spike/docker-compose.yml down -v` (removes the spike broker,
    DB, and volumes) and `rm -rf .spike/` (deletes all throwaway code + fetched data). Per D-06 this
    happens only AFTER the durable artifacts are confirmed written. Human confirmation is required
    before the destructive teardown.
  </what-built>
  <how-to-verify>
    1. Confirm SPIKE-FINDINGS.md and all four ADRs exist and read correctly (verdicts present, no
       partial mislabeled as a pass).
    2. Confirm the durable verdict fragments under findings/ are consolidated (nothing important lives
       only inside .spike/).
    3. Approve teardown: run `docker compose -f .spike/docker-compose.yml down -v` then `rm -rf .spike/`.
    4. Confirm the production stack on :8088/:3000 and data/verdicts.jsonl are untouched.
  </how-to-verify>
  <acceptance_criteria>
    - [ ] Human confirms SPIKE-FINDINGS.md + ADR-005..008 are complete before any deletion
    - [ ] After approval: `.spike/` no longer exists and the spike containers are down (down -v)
    - [ ] `grep -rl spike apps libs 2>/dev/null` returns nothing (no spike code merged)
    - [ ] data/verdicts.jsonl and the :8088/:3000 stack are unchanged
  </acceptance_criteria>
  <resume-signal>Type "teardown-approved" to delete .spike/ + spike containers, or "hold" to keep them.</resume-signal>
  <done>On approval: .spike/ deleted + spike containers down -v; prod data/stack untouched; phase knowledge fully durable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| teardown command → filesystem/containers | Destructive rm -rf + docker down -v acts on the spike scratch only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-07-01 | Denial of Service | destructive teardown hitting wrong target | mitigate | Human-verify checkpoint before rm -rf/down -v; scoped to .spike/ + spike compose file only |
| T-00-07-02 | Tampering | prod data loss during teardown | mitigate | Post-teardown assert data/verdicts.jsonl + :8088/:3000 unchanged |
| T-00-07-03 | Repudiation | lost knowledge after deletion | mitigate | All verdicts consolidated into durable SPIKE-FINDINGS.md + ADRs before deletion (D-06 ordering) |
</threat_model>

<verification>
- SPIKE-FINDINGS.md present with R1-R5 verdicts; docs/adr/ADR-005..008 present with decisions.
- After approval: .spike/ deleted, spike containers down, no spike code under apps/ or libs/.
- Production data + stack untouched.
</verification>

<success_criteria>
- All five unknowns have a durable recorded go/no-go/partial; four ADRs carry their decisions.
- Throwaway disposed of cleanly (D-06); durable knowledge feeds P2/P3/P5/P8/P10 + SP-COP.
</success_criteria>

## Assumptions
- ADR stubs live under docs/adr/ (continuing the docs/ARCHITECTURE.md ADR-001..004 lineage); this
  file layout is Claude's discretion and is the chosen durable home.
- ADR-008's decision is carried from the already-proven Gmail OAuth2/MCP pull (STATE.md), not re-spiked
  this phase (it is out of the five unknowns but is a required ADR deliverable).

<output>
Create `.planning/phases/00-concept-spike/00-07-SUMMARY.md` when done.
</output>
