---
phase: 00-concept-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .spike/docker-compose.yml
  - .spike/requirements-spike.txt
  - .spike/r2_dedup/r2_fetch.py
  - .spike/items.json
autonomous: false
requirements: [R1, R2, R3, R4, R5]
user_setup:
  - service: docker
    why: "Ephemeral RabbitMQ (R1) + pgvector (R3) containers — D-04"
    note: "Docker 29.4.0, Python 3.13.5 already available (RESEARCH § Environment Availability). No dashboard config."

must_haves:
  truths:
    - "RabbitMQ broker answers on AMQP port 22060 and management UI 22061 (distinct from prod 8088/3000)"
    - "pgvector Postgres answers on port 22062 with the vector extension installable"
    - "A shared corpus .spike/items.json holds >=30 fresh NRK/BBC/TASS items with stable ids, reused by R2/R3/R4/R5 (D-03)"
    - "This plan, like every spike plan, ends with a recorded go/no-go/partial outcome; partial is never silently treated as a pass"
  artifacts:
    - path: ".spike/docker-compose.yml"
      provides: "Ephemeral RabbitMQ 3.13-management + pgvector/pgvector:pg16 on ports 22060/22061/22062"
      contains: "22060"
      ephemeral: true
    - path: ".spike/requirements-spike.txt"
      provides: "Throwaway pip deps: pika==1.4.1, pgvector==0.4.2, defusedxml"
      ephemeral: true
    - path: ".spike/r2_dedup/r2_fetch.py"
      provides: "Read-only NRK/BBC/TASS RSS fetcher using defusedxml (D-01)"
      ephemeral: true
    - path: ".spike/items.json"
      provides: "Shared fresh corpus consumed by R2/R3/R4/R5"
      ephemeral: true
    - path: ".gitignore"
      provides: "Ignores .spike/ so throwaway code + fetched data never commit"
      contains: ".spike/"
      ephemeral: false
  key_links:
    - from: ".spike/r2_dedup/r2_fetch.py"
      to: ".spike/items.json"
      via: "writes the fetched corpus that all downstream unknowns read"
      pattern: "items\\.json"
    - from: ".spike/docker-compose.yml"
      to: "RabbitMQ + pgvector containers"
      via: "docker compose up -d on the 22060-22062 port band"
      pattern: "2206[0-2]"
  prohibitions:
    - statement: "MUST NOT call a cloud LLM in any spike runtime"
      status: resolved
      verification: "This plan makes no LLM calls; later plans keep LLM_BASE_URL at 127.0.0.1 (ADR-004)"
    - statement: "MUST NOT mutate production data (verdicts.jsonl, real feeds) or the running pipeline"
      status: resolved
      verification: "Fetch is read-only against source URLs; containers use ports 22060-22062 and the spike DB/credentials, never the prod :8088/:3000 stack or data/ files"
    - statement: "MUST NOT merge throwaway spike code into apps/ or libs/"
      status: resolved
      verification: ".spike/ added to .gitignore; nothing written under apps/ or libs/; deleted at closeout (Plan 07, D-06)"
---

<objective>
Stand up the throwaway spike infrastructure and fetch the shared test corpus that every other
unknown builds on. This is the Wave-0 scaffolding the orchestrator called for: ephemeral RabbitMQ
+ pgvector containers on a dedicated port band (D-04), the `.gitignore` guard, the scratch layout,
and a single fresh NRK/BBC/TASS corpus (D-01) reused across R2/R3/R4/R5 (D-03).

Purpose: R1 needs the RabbitMQ container; R3 needs the pgvector container; R2/R4/R5 need the corpus.
Producing these once in Wave 1 lets the five investigations run without re-fetching or re-provisioning.
Output: ephemeral containers (22060/22061/22062), `.spike/items.json`, gitignore guard.
</objective>

## Artifacts This Phase Produces (this plan)

| Artifact | Kind | Durable? |
|----------|------|----------|
| `.spike/docker-compose.yml` (services `rabbitmq`, `pgvector`) | infra config | Ephemeral — deleted Plan 07 |
| `.spike/requirements-spike.txt` (`pika==1.4.1 pgvector==0.4.2 defusedxml`) | deps | Ephemeral |
| `.spike/r2_dedup/r2_fetch.py` (fn `fetch_rss`, `main`) | script | Ephemeral |
| `.spike/items.json` (list of `{id, source, title, summary, link, lang, published}`) | data | Ephemeral |
| `.spike/` scratch layout (`r1_rabbit/ r2_dedup/ r3_entities/ r4_wiki/ r5_worldmonitor/`) | dirs | Ephemeral |
| `.gitignore` entry `.spike/` | repo config | **Durable** |
| Running containers: RabbitMQ `:22060`/`:22061`, pgvector `:22062` | runtime | Ephemeral — torn down Plan 07 |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-concept-spike/00-SPEC.md
@.planning/phases/00-concept-spike/00-CONTEXT.md
@.planning/phases/00-concept-spike/00-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint 1: Package legitimacy gate (pika, pgvector, defusedxml)</name>
  <read_first>
    - .planning/phases/00-concept-spike/00-RESEARCH.md § Package Legitimacy Audit
  </read_first>
  <action>
    Pause for human package-legitimacy sign-off before any pip install. Present the three PyPI pages
    (pika, pgvector, defusedxml) for confirmation; proceed to install only on "approved".
  </action>
  <what-built>
    Nothing yet — this gate runs BEFORE any pip install. RESEARCH rated pika, pgvector, and
    defusedxml as SUS purely because the legitimacy seam cannot read PyPI download counts; each
    was independently verified (version history + official source repo + Context7 HIGH) and
    dispositioned Approved. SUS packages require an explicit human-verify gate before install.
  </what-built>
  <how-to-verify>
    Confirm each package is the genuine, widely-used project at:
    1. https://pypi.org/project/pika/ (official RabbitMQ Python client, github.com/pika/pika)
    2. https://pypi.org/project/pgvector/ (official pgvector adapter, github.com/pgvector/pgvector-python)
    3. https://pypi.org/project/defusedxml/ (CVE-mitigating XML parser, github.com/tiran/defusedxml)
    Pinned versions: pika==1.4.1, pgvector==0.4.2, defusedxml (latest).
  </how-to-verify>
  <acceptance_criteria>
    - [ ] Human confirms all three PyPI pages resolve to the named source repos
    - [ ] No package is [SLOP]; install may proceed
  </acceptance_criteria>
  <resume-signal>Type "approved" to allow the pip install, or name any package to block.</resume-signal>
  <done>Human has approved (or blocked) the three SUS-rated packages; install may proceed only on approval.</done>
</task>

<task type="auto">
  <name>Task 1: Scaffold .spike/, install deps, bring up ephemeral containers (D-04)</name>
  <files>.gitignore, .spike/docker-compose.yml, .spike/requirements-spike.txt</files>
  <read_first>
    - .planning/phases/00-concept-spike/00-RESEARCH.md § R1 (Docker Compose block) and § Cross-Cutting: Ephemeral Docker Setup
    - .planning/phases/00-concept-spike/00-CONTEXT.md (D-04 isolation; D-06 lifecycle)
    - .gitignore (current contents)
  </read_first>
  <action>
    Add a single line `.spike/` to `.gitignore` so all throwaway spike code and fetched data are
    never committed (D-06). Create the scratch layout: `.spike/` with subdirs `r1_rabbit/`,
    `r2_dedup/`, `r3_entities/`, `r4_wiki/`, `r5_worldmonitor/`. Write `.spike/requirements-spike.txt`
    pinning `pika==1.4.1`, `pgvector==0.4.2`, and `defusedxml`. Write `.spike/docker-compose.yml`
    with two services exactly as RESEARCH § R1 specifies: service `rabbitmq` on image
    `rabbitmq:3.13-management` mapping host 22060 to container 5672 (AMQP) and host 22061 to
    container 15672 (management UI), env user/pass `spike`/`spike`, with a `rabbitmq-diagnostics -q
    ping` healthcheck; service `pgvector` on image `pgvector/pgvector:pg16` mapping host 22062 to
    container 5432, env `POSTGRES_USER`/`POSTGRES_PASSWORD`/`POSTGRES_DB` all `spike`, with a
    `pg_isready -U spike` healthcheck. These ports (22060/22061/22062) and the `spike` credentials
    are deliberately distinct from the production stack on 8088/3000 and its data (D-04) — never
    map a host data dir from the project tree as a volume. Install the deps with
    `pip3 install -r .spike/requirements-spike.txt`, then `docker compose -f .spike/docker-compose.yml up -d`.
  </action>
  <verify>
    <automated>docker compose -f .spike/docker-compose.yml ps --format '{{.Service}} {{.State}}' | grep -c running</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `.gitignore` contains a `.spike/` line (grep `^\.spike/` returns it)
    - [ ] `docker compose -f .spike/docker-compose.yml ps` shows both services running/healthy
    - [ ] `nc -z localhost 22060 && nc -z localhost 22061 && nc -z localhost 22062` all succeed
    - [ ] The production stack is untouched: `docker compose ps` (repo root) shows 8088/3000 unchanged
    - [ ] `python3 -c "import pika, pgvector, defusedxml"` exits 0
  </acceptance_criteria>
  <done>Ephemeral RabbitMQ + pgvector running on 22060/22061/22062, deps installed, .spike/ gitignored, prod stack untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fetch shared fresh corpus from NRK/BBC/TASS (D-01)</name>
  <files>.spike/r2_dedup/r2_fetch.py, .spike/items.json</files>
  <read_first>
    - .planning/phases/00-concept-spike/00-RESEARCH.md § R2 (Fresh Fetch Approach + feed URLs) and § Common Pitfalls (Pitfall 7: XXE)
    - bridge/imap_to_atom.py, bridge/yt_to_atom.py (fetch/atom patterns to mirror)
    - .planning/phases/00-concept-spike/00-CONTEXT.md (D-01 fresh fetch, read-only; D-02 same-story concurrency)
  </read_first>
  <action>
    Write `.spike/r2_dedup/r2_fetch.py` that fetches three feeds read-only and writes a normalized
    corpus to `.spike/items.json` (D-01). Use NRK `https://www.nrk.no/nyheter/siste.rss`, BBC
    `https://feeds.bbci.co.uk/news/world/rss.xml`, TASS `https://tass.com/rss/v2.xml`. Parse every
    network-sourced feed with `defusedxml.ElementTree` (a safe drop-in); the unsafe stdlib XML
    parser must NOT be used (XXE / billion-laughs, RESEARCH Pitfall 7). Each item is a dict with a
    stable `id` of the form `{source}_{NNN}` (e.g. `nrk_001`, `bbc_003`, `tass_007`), plus `source`
    (`nrk`/`bbc`/`tass`), `title`, `summary`, `link`, `lang` (`no`/`en`/`ru`), and `published`.
    Target >=30 items total spanning all three sources so >=10 same-story triples can later be
    hand-labeled (D-02). Make the script read-only: it writes only `.spike/items.json`, never
    `data/verdicts.jsonl` or any real feed file. Per D-02 the fetch captures whatever is current;
    if a later count check finds too few concurrent same-story events, the operator re-runs on a
    busier news day (record this as an assumption — see Assumptions below).
  </action>
  <verify>
    <automated>python3 .spike/r2_dedup/r2_fetch.py && python3 -c "import json,sys; d=json.load(open('.spike/items.json')); srcs={i['source'] for i in d}; sys.exit(0 if len(d)>=30 and {'nrk','bbc','tass'} <= srcs else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `.spike/items.json` has >=30 items across all three of nrk/bbc/tass
    - [ ] Every item carries id, source, title, summary, link, lang, published
    - [ ] `grep -nE 'xml\.etree' .spike/r2_dedup/r2_fetch.py` returns nothing (defusedxml only)
    - [ ] `data/verdicts.jsonl` mtime is unchanged (read-only honored)
  </acceptance_criteria>
  <done>.spike/items.json holds >=30 fresh NRK/BBC/TASS items (defusedxml, read-only) ready for R2/R3/R4/R5.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| network → r2_fetch.py | Attacker-influenced RSS XML from NRK/BBC/TASS crosses into the parser |
| PyPI → local env | Third-party packages installed into the spike environment |
| spike containers → host | Ephemeral broker/DB bound to localhost ports |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01-SC | Tampering | pip install (pika/pgvector/defusedxml) | mitigate | Package legitimacy human-verify checkpoint above; pinned versions; blocking for any SUS/ASSUMED |
| T-00-01-XXE | Tampering / DoS | r2_fetch.py XML parsing | mitigate | defusedxml.ElementTree only; stdlib parser forbidden (grep gate) |
| T-00-01-ISO | Tampering | prod data / running pipeline | mitigate | Distinct ports 22060-22062 + `spike` creds; no prod-tree volumes; fetch read-only |
| T-00-01-LEAK | Info Disclosure | spike creds / fetched data in git | mitigate | `.spike/` gitignored; creds are throwaway `spike`/`spike` |
</threat_model>

<verification>
- Both containers report running/healthy on 22060/22061/22062; prod 8088/3000 unaffected.
- `.spike/items.json` exists with >=30 multilingual items; defusedxml used; read-only honored.
- `.spike/` is gitignored (no spike artifacts staged).
</verification>

<success_criteria>
- Ephemeral RabbitMQ + pgvector reachable on the dedicated port band (D-04).
- Shared corpus fetched fresh and read-only (D-01), ready for R2/R3/R4/R5 (D-03).
- Scratch layout + gitignore guard in place for the throwaway lifecycle (D-06).
</success_criteria>

## Assumptions
- The current NRK/BBC/TASS feeds carry enough concurrently-covered events to yield >=10 same-story
  triples (D-02). If not, the operator re-fetches on a busier day before R2 labeling. (RESEARCH A3 region.)
- Docker, Python, pip are available and able to pull `rabbitmq:3.13-management` and
  `pgvector/pgvector:pg16` (RESEARCH § Environment Availability marks both not-yet-pulled).

<output>
Create `.planning/phases/00-concept-spike/00-01-SUMMARY.md` when done, recording container ports,
item count per source, and any re-fetch needed.
</output>
