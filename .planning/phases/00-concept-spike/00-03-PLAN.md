---
phase: 00-concept-spike
plan: 03
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - .spike/r2_dedup/r2_embed.py
  - .spike/r2_dedup/r2_threshold.py
  - .spike/r2_dedup/same_story_triples.csv
  - .planning/phases/00-concept-spike/findings/R2-VERDICT.md
autonomous: false
requirements: [R2]
user_setup: []

must_haves:
  truths:
    - "On >=10 NRK/BBC/TASS same-story triples, >=80% collapse to one cluster at a single chosen cosine threshold"
    - "Zero control pairs over-merge at that same threshold"
    - "Exactly one model (bge-m3 or mE5-large) and one threshold are recorded as the chosen pair"
    - "The test sample meets the floor: >=10 triples plus >=5 control pairs (non-inferable, requires the hand-labeled CSV)"
    - "R2 ends with a recorded go/no-go/partial verdict; partial is never silently treated as a pass"
  artifacts:
    - path: ".spike/r2_dedup/same_story_triples.csv"
      provides: "Hand-labeled ground truth: >=10 same-story triples + >=5 control pairs (D-02)"
      ephemeral: true
    - path: ".spike/r2_dedup/r2_embed.py"
      provides: "Batch-embeds items.json with bge-m3 and mE5-large (normalized), builds cosine matrix"
      ephemeral: true
    - path: ".spike/r2_dedup/r2_threshold.py"
      provides: "Threshold sweep -> collapse_rate + control_overmerge per model"
      ephemeral: true
    - path: ".planning/phases/00-concept-spike/findings/R2-VERDICT.md"
      provides: "Durable R2 verdict: chosen model + threshold + collapse_rate"
      ephemeral: false
  key_links:
    - from: ".spike/r2_dedup/r2_embed.py"
      to: ".spike/items.json"
      via: "reads shared corpus, embeds title + summary[:512] only (long-input caveat)"
      pattern: "items\\.json"
    - from: ".spike/r2_dedup/r2_threshold.py"
      to: ".spike/r2_dedup/same_story_triples.csv"
      via: "scores labeled pairs against swept thresholds for collapse_rate/control_overmerge"
      pattern: "same_story"
  prohibitions:
    - statement: "MUST NOT call a cloud LLM in any spike runtime"
      status: resolved
      verification: "Embeddings run on host sentence-transformers (local); no cloud/LLM endpoint"
    - statement: "MUST NOT mutate production data or the running pipeline"
      status: resolved
      verification: "Reads .spike/items.json only; data/verdicts.jsonl untouched"
    - statement: "MUST NOT merge throwaway spike code into apps/ or libs/"
      status: resolved
      verification: "All code under .spike/r2_dedup/ (gitignored), deleted at closeout"
---

<objective>
Measure whether embedding-based dedup collapses same-story cross-language items, and pick one model
plus one cosine threshold that hits the bar. This resolves R2 and decides the Q5 embedding-model
choice (REQUIREMENTS P-5/P-7/A-1) that Phase 5 builds on. The spike only chooses the model; building
the embedding infra is Phase 5 (Deferred).

Purpose: today's dedup is keyword overlap (score/digest.py cluster()) which fails across NO/EN/RU
phrasings. bge-m3 vs mE5-large is the locked bake-off.
Output: embed + threshold-sweep scripts, hand-labeled CSV, durable R2 verdict (model + threshold).
</objective>

## Artifacts This Phase Produces (this plan)

| Artifact | Kind | Durable? |
|----------|------|----------|
| `.spike/r2_dedup/r2_embed.py` (`embed_items`, dual-model) | script | Ephemeral — deleted Plan 07 |
| `.spike/r2_dedup/r2_threshold.py` (`sweep_threshold`, reports collapse_rate/control_overmerge) | script | Ephemeral |
| `.spike/r2_dedup/same_story_triples.csv` (cols `item_a_id,item_b_id,item_c_id,same_story,notes`) | ground truth | Ephemeral |
| `findings/R2-VERDICT.md` (chosen model + threshold + numbers) | verdict | **Durable** |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-concept-spike/00-SPEC.md
@.planning/phases/00-concept-spike/00-RESEARCH.md
@.planning/phases/00-concept-spike/00-01-SUMMARY.md
@score/digest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embed corpus (bge-m3 + mE5-large) and pre-cluster candidate triples</name>
  <files>.spike/r2_dedup/r2_embed.py, .spike/r2_dedup/same_story_triples.csv</files>
  <read_first>
    - .planning/phases/00-concept-spike/00-RESEARCH.md § R2 (Model recommendation, Serving, Code Examples, Long-Input Caveat) and § Common Pitfalls (Pitfall 6 normalization)
    - .planning/phases/00-concept-spike/00-01-SUMMARY.md (corpus location, per-source counts)
    - score/digest.py cluster() (keyword baseline this approach must beat)
  </read_first>
  <action>
    Write `.spike/r2_dedup/r2_embed.py` that loads `.spike/items.json` and embeds each item's
    `title + summary[:512]` ONLY (never the full body, RESEARCH long-input caveat) with
    sentence-transformers, running BOTH models for the bake-off: `BAAI/bge-m3` and
    `intfloat/multilingual-e5-large` (1024-dim each; for mE5 use the required `query:`/`passage:`
    instruction prefix). Always pass `normalize_embeddings=True` so cosine == dot product (RESEARCH
    Pitfall 6); assert each embedding has shape 1024 before use (Pitfall 3). All inference is local
    host sentence-transformers (already installed) — no cloud, no LLM. To make hand-labeling cheap,
    have the script emit a CANDIDATE `same_story_triples.csv` skeleton: cluster items by
    publish-time proximity + title keyword overlap to propose likely triples with `same_story` left
    blank for the human to confirm, columns `item_a_id,item_b_id,item_c_id,same_story,notes`. Cache
    both models' embeddings to disk so the threshold sweep does not re-encode.
  </action>
  <verify>
    <automated>python3 .spike/r2_dedup/r2_embed.py && python3 -c "import os,sys; sys.exit(0 if os.path.exists('.spike/r2_dedup/same_story_triples.csv') else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Both models load and produce normalized 1024-dim vectors for every corpus item
    - [ ] A candidate same_story_triples.csv skeleton is emitted with the 5 columns
    - [ ] grep confirms `normalize_embeddings` is set and only title+summary[:512] is embedded
  </acceptance_criteria>
  <done>Both bge-m3 and mE5-large embeddings cached (1024-dim, normalized); candidate triple CSV skeleton emitted for labeling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Hand-label the same-story ground truth (D-02)</name>
  <read_first>
    - .planning/phases/00-concept-spike/00-RESEARCH.md § R2 (Hand-Labeling Scheme)
    - .planning/phases/00-concept-spike/00-CONTEXT.md (D-02 concurrent-coverage events + hand label)
    - .spike/r2_dedup/same_story_triples.csv (candidate skeleton from Task 1)
  </read_first>
  <action>
    Pause for the human to hand-label the same-story ground truth (D-02): confirm/correct each
    candidate triple's same_story flag and add control pairs, until >=10 triples + >=5 controls exist.
  </action>
  <what-built>
    Task 1 pre-clustered the corpus into candidate same-story triples by time + keyword overlap. The
    ground truth itself is a human judgment (D-02): only a person can confirm that an NRK, a BBC, and
    a TASS item truly cover the same event, and which pairs are genuine controls that must NOT merge.
  </what-built>
  <how-to-verify>
    1. Open `.spike/r2_dedup/same_story_triples.csv`.
    2. For each candidate row, set `same_story` to `yes` (same event across outlets) or `no` (control).
    3. Ensure >=10 confirmed same-story triples AND >=5 control pairs that must stay separate.
    4. If too few real triples exist, ask the operator to re-run the Plan 01 fetch on a busier news
       day, then re-label. Add a one-word `notes` tag per row (e.g. "NATO summit").
  </how-to-verify>
  <acceptance_criteria>
    - [ ] CSV has >=10 rows with same_story=yes and >=5 with same_story=no
    - [ ] Every labeled id exists in .spike/items.json
  </acceptance_criteria>
  <resume-signal>Type "labeled" when the CSV meets the >=10 triples / >=5 controls floor.</resume-signal>
  <done>same_story_triples.csv hand-labeled with >=10 same-story triples + >=5 control pairs, all ids valid.</done>
</task>

<task type="auto">
  <name>Task 2: Threshold sweep, choose model + threshold, record R2 verdict</name>
  <files>.spike/r2_dedup/r2_threshold.py, .planning/phases/00-concept-spike/findings/R2-VERDICT.md</files>
  <read_first>
    - .planning/phases/00-concept-spike/00-RESEARCH.md § R2 (Cosine Threshold Selection, Code Examples) and § Open Questions (threshold stability)
    - .planning/phases/00-concept-spike/00-SPEC.md (R2 acceptance: >=80% collapse, 0 control over-merge)
  </read_first>
  <action>
    Write `.spike/r2_dedup/r2_threshold.py` that, for EACH model's cached embeddings, sweeps cosine
    thresholds from 0.75 to 0.98 (start guidance 0.85) and computes, per threshold, `collapse_rate`
    (fraction of same_story=yes pairs at or above threshold) and `control_overmerge` (count of
    same_story=no pairs at or above threshold). Choose the single (model, threshold) pair that
    achieves `collapse_rate >= 0.80` AND `control_overmerge == 0`; if both models qualify, prefer the
    higher collapse_rate at zero over-merge. Print the chosen pair and the full sweep table. Then
    write the durable verdict at `.planning/phases/00-concept-spike/findings/R2-VERDICT.md`: a
    go/no-go/partial line, the chosen model + threshold, the achieved collapse_rate, the control
    over-merge count, and the event set it was calibrated on with the note "generalization unverified"
    (RESEARCH Open Question 3). Per SPEC Constraints, if no threshold clears both bars, record a
    partial with the best achievable numbers — never round up to a pass.
  </action>
  <verify>
    <automated>python3 .spike/r2_dedup/r2_threshold.py && grep -nEi 'go|no-go|partial' .planning/phases/00-concept-spike/findings/R2-VERDICT.md && grep -nEi 'bge-m3|e5' .planning/phases/00-concept-spike/findings/R2-VERDICT.md</automated>
  </verify>
  <acceptance_criteria>
    - [ ] Sweep reports collapse_rate + control_overmerge per threshold for both models
    - [ ] Exactly one (model, threshold) chosen with collapse_rate>=0.80 and control_overmerge==0, OR a documented partial
    - [ ] R2-VERDICT.md records chosen model + threshold + numbers + calibration event set
  </acceptance_criteria>
  <done>One (model, threshold) chosen at >=80% collapse + 0 control over-merge (or documented partial); R2 verdict written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus → embedder | Fetched item text crosses into local model inference (no network egress) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-R2-01 | Tampering | embedding endpoint | mitigate | Host sentence-transformers, local-only; no cloud embedding call |
| T-00-R2-02 | Tampering | prod data | mitigate | Reads .spike/items.json only; verdicts.jsonl untouched |
| T-00-R2-03 | Repudiation | inflated pass claim | mitigate | Sweep numbers + calibration set recorded; partial allowed, never silently a pass |
</threat_model>

<verification>
- Sweep produces collapse_rate/control_overmerge per model; one (model, threshold) chosen or partial.
- findings/R2-VERDICT.md exists with chosen model + threshold + numbers.
</verification>

<success_criteria>
- R2 acceptance met (>=80% collapse, 0 control over-merge at one threshold) or explicit partial.
- Q5 model choice locked for Phase 5; durable verdict written.
</success_criteria>

## Assumptions
- Cosine 0.85 is the starting point; the empirical chosen threshold is whatever the sweep yields
  (RESEARCH A3). bge-m3 fits host RAM at ~570MB FP16; if OOM, fall back to Ollama-quantized (RESEARCH A7).
- The chosen threshold is calibrated on today's event set; cross-date generalization is unverified
  (RESEARCH Open Question 3) and recorded as such.

<output>
Create `.planning/phases/00-concept-spike/00-03-SUMMARY.md` when done.
</output>
