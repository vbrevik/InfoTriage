---
phase: 00-concept-spike
plan: 04
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - .spike/r3_entities/r3_schema.sql
  - .spike/r3_entities/r3_ner.py
  - .spike/r3_entities/r3_link.py
  - .planning/phases/00-concept-spike/findings/R3-VERDICT.md
autonomous: true
requirements: [R3, ADR-006]
user_setup: []

must_haves:
  truths:
    - "One known entity (NATO) resolves across >=3 items spanning 2 languages into a single entity_id"
    - "A distinct control entity (e.g. Putin vs Biden) is NOT merged: control entities keep different entity_ids"
    - "Entity NER runs on the local qwen36 llm() (ADR-004), not spaCy and not any cloud model"
    - "R3 ends with a recorded go/no-go/partial verdict; partial is never silently treated as a pass"
  artifacts:
    - path: ".spike/r3_entities/r3_schema.sql"
      provides: "entities + entity_links tables + HNSW vector_cosine_ops index on vector(1024)"
      ephemeral: true
    - path: ".spike/r3_entities/r3_ner.py"
      provides: "Multilingual NER via qwen36 llm() -> [{name,type,lang}]"
      ephemeral: true
    - path: ".spike/r3_entities/r3_link.py"
      provides: "Embeds entities, cosine-links via <=>, inserts/merges; --verify-test asserts NATO merge + control split"
      ephemeral: true
    - path: ".planning/phases/00-concept-spike/findings/R3-VERDICT.md"
      provides: "Durable R3 verdict feeding ADR-006"
      ephemeral: false
  key_links:
    - from: ".spike/r3_entities/r3_link.py"
      to: "entities table (pgvector)"
      via: "cosine link via the <=> operator against the HNSW vector_cosine_ops index"
      pattern: "<=>"
    - from: ".spike/r3_entities/r3_ner.py"
      to: "score/triage_score.py llm()"
      via: "reuses the local qwen36 llm() call for ADR-004-compliant NER"
      pattern: "llm\\("
  prohibitions:
    - statement: "MUST NOT call a cloud LLM in any spike runtime"
      status: resolved
      verification: "NER uses llm() which defaults to LLM_BASE_URL=http://127.0.0.1:8000/v1 (oMLX, local)"
    - statement: "MUST NOT mutate production data or the running pipeline"
      status: resolved
      verification: "Writes only to the spike Postgres on 22062; reads .spike/items.json; verdicts.jsonl untouched"
    - statement: "MUST NOT merge throwaway spike code into apps/ or libs/"
      status: resolved
      verification: "All code under .spike/r3_entities/ (gitignored), deleted at closeout"
---

<objective>
Prove pgvector can link entity mentions across items and languages: extract entities from the shared
corpus, embed them, and resolve one known entity across >=3 items / 2 languages into a single entity
without over-merging a distinct control entity. This resolves R3 and feeds ADR-006 (microservice
architecture + entity resolution). The production schema + Obsidian projection are Phase 8 (Deferred).

Purpose: no entities/entity_links schema or resolution exists yet; pgvector cosine linking is the
candidate mechanism Phase 8 will build on.
Output: schema SQL + NER + linking scripts + durable R3 verdict.
</objective>

## Artifacts This Phase Produces (this plan)

| Artifact | Kind | Durable? |
|----------|------|----------|
| `.spike/r3_entities/r3_schema.sql` (tables `entities`, `entity_links`; HNSW idx) | schema | Ephemeral — deleted Plan 07 |
| `.spike/r3_entities/r3_ner.py` (`extract_entities`) | script | Ephemeral |
| `.spike/r3_entities/r3_link.py` (`link_or_insert_entity`, `--verify-test`) | script | Ephemeral |
| Postgres objects `entities`, `entity_links`, `entities_embedding_idx` | DB objects | Ephemeral (spike DB on 22062) |
| Column/table names `entities`, `entity_links`, `name_norm`, `embedding vector(1024)` | schema decision | **Decision durable in ADR-006** |
| `findings/R3-VERDICT.md` | verdict | **Durable** |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-concept-spike/00-SPEC.md
@.planning/phases/00-concept-spike/00-RESEARCH.md
@.planning/phases/00-concept-spike/00-01-SUMMARY.md
@score/triage_score.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema and extract entities via qwen36 NER (ADR-004)</name>
  <files>.spike/r3_entities/r3_schema.sql, .spike/r3_entities/r3_ner.py</files>
  <read_first>
    - .planning/phases/00-concept-spike/00-RESEARCH.md § R3 (Schema, NER via qwen36, Index Choice) and § Common Pitfalls (Pitfall 2 HNSW, Pitfall 3 dim mismatch, Pitfall 8 oMLX running)
    - score/triage_score.py llm() (signature: llm(messages, max_tokens=400); LLM_BASE_URL default 127.0.0.1:8000/v1)
    - .planning/phases/00-concept-spike/00-01-SUMMARY.md (pgvector port 22062, db/creds spike)
  </read_first>
  <action>
    First ensure the local LLM is up: run `omlx-ensure-server` (or health-check
    http://127.0.0.1:8000/v1) before any NER call (RESEARCH Pitfall 8). Write
    `.spike/r3_entities/r3_schema.sql` creating the vector extension, an `entities` table (id,
    name, name_norm, lang, type, embedding vector(1024), created_at) and an `entity_links` table
    (id, entity_id FK to entities ON DELETE CASCADE, item_id, mention, confidence, lang, created_at),
    plus an HNSW index `entities_embedding_idx` on `embedding vector_cosine_ops` (HNSW, not IVFFlat,
    because it needs no minimum rows, RESEARCH Pitfall 2). Apply it to the spike Postgres on 22062.
    Write `.spike/r3_entities/r3_ner.py` exposing `extract_entities(title, summary)` that reuses the
    existing local qwen36 `llm()` from score/triage_score.py (ADR-004: no spaCy, no cloud) with a
    prompt returning ONLY a JSON array of `{name, type(PER|ORG|GPE|LOC), lang(no|en|ru)}`, parsed by
    extracting the bracketed JSON substring with a try/except fallback to []. Run NER over
    .spike/items.json so NATO appears in NRK (no), BBC (en), and TASS (ru) items.
  </action>
  <verify>
    <automated>psql "postgresql://spike:spike@localhost:22062/spike" -f .spike/r3_entities/r3_schema.sql && python3 -c "import sys; sys.path.insert(0,'.spike/r3_entities'); from r3_ner import extract_entities; r=extract_entities('NATO holder toppmote i Berlin','Forsvarsalliansen NATO samles'); sys.exit(0 if isinstance(r,list) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] entities + entity_links tables + HNSW vector_cosine_ops index exist in the spike DB
    - [ ] extract_entities returns a JSON list of {name,type,lang} from qwen36 (local llm())
    - [ ] grep confirms r3_ner.py calls llm() and no cloud endpoint is configured
  </acceptance_criteria>
  <done>entities + entity_links + HNSW index created on the spike DB; qwen36 NER returns structured multilingual entities.</done>
</task>

<task type="auto">
  <name>Task 2: Cosine-link entities; verify NATO merge + control split; record R3 verdict</name>
  <files>.spike/r3_entities/r3_link.py, .planning/phases/00-concept-spike/findings/R3-VERDICT.md</files>
  <read_first>
    - .planning/phases/00-concept-spike/00-RESEARCH.md § R3 (Cosine Linking Query, Acceptance Test Design, Code Examples)
    - .planning/phases/00-concept-spike/00-SPEC.md (R3 acceptance: 1 entity across >=3 items/2 langs, no control over-merge)
    - .spike/r2_dedup/r2_embed.py (reuse the same embedding model chosen in R2 for entity vectors, if available)
  </read_first>
  <action>
    Write `.spike/r3_entities/r3_link.py` that, for each NER mention, embeds the entity name (reuse
    the R2-chosen model; default bge-m3) with normalized 1024-dim vectors and runs the cosine link
    query: SELECT the nearest existing entity WHERE `1 - (embedding <=> %s::vector) >= LINK_THRESHOLD`
    ordered by `embedding <=> %s::vector` LIMIT 1; if a match is found, reuse its entity_id, else
    INSERT a new entity. Record every mention as an entity_links row (entity_id, item_id, mention,
    lang). Add a `--verify-test` mode that asserts the acceptance bar: the NATO entity has exactly
    one entity_id with `COUNT(*) >= 3` distinct items spanning >=2 languages in entity_links, AND a
    distinct control entity (e.g. Putin vs Biden) resolves to different entity_ids (no over-merge,
    RESEARCH Acceptance Test Design). Then write the durable verdict at
    `.planning/phases/00-concept-spike/findings/R3-VERDICT.md`: go/no-go/partial, the NATO merge
    count + languages, the LINK_THRESHOLD used, and confirmation that controls stayed distinct. Per
    SPEC Constraints, if NATO fails to merge or a control over-merges, record a partial with the
    observed counts — never a silent pass.
  </action>
  <verify>
    <automated>python3 .spike/r3_entities/r3_link.py --verify-test && grep -nEi 'go|no-go|partial' .planning/phases/00-concept-spike/findings/R3-VERDICT.md</automated>
  </verify>
  <acceptance_criteria>
    - [ ] NATO resolves to a single entity_id with >=3 distinct items across >=2 languages
    - [ ] Control entities keep distinct entity_ids (no over-merge)
    - [ ] R3-VERDICT.md records go/no-go/partial + merge count + threshold + control result
  </acceptance_criteria>
  <done>NATO merged across >=3 items/2 langs into one entity, controls distinct (or documented partial); R3 verdict written for ADR-006.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| corpus → qwen36 NER | Item text crosses into the local LLM for entity extraction |
| linking script → spike Postgres | Entity rows written to the ephemeral DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-R3-01 | Tampering | NER LLM endpoint | mitigate | llm() default 127.0.0.1:8000/v1 (oMLX, local); verify LLM_BASE_URL not cloud |
| T-00-R3-02 | Tampering | prod data | mitigate | Writes only spike DB on 22062; reads items.json; verdicts.jsonl untouched |
| T-00-R3-03 | Information Disclosure | over-resolution false merge | mitigate | --verify-test asserts control entities stay distinct (R3 acceptance) |
</threat_model>

<verification>
- Schema + HNSW index created; NER returns structured entities from local qwen36.
- --verify-test passes: NATO merged across >=3 items/2 langs, controls distinct.
- findings/R3-VERDICT.md exists with a go/no-go/partial line.
</verification>

<success_criteria>
- R3 acceptance met (entity merge + control split) or explicit partial documented.
- Durable verdict ready for ADR-006.
</success_criteria>

## Assumptions
- qwen36 returns reliable multilingual NER JSON (proven for triage scoring); parse falls back to []
  on malformed output (reuses the triage_score.py extraction pattern).
- oMLX must be running before this plan (RESEARCH Pitfall 8 / A-note); the plan health-checks it first.

<output>
Create `.planning/phases/00-concept-spike/00-04-SUMMARY.md` when done.
</output>
