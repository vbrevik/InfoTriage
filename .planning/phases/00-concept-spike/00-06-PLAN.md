---
phase: 00-concept-spike
plan: 06
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - .spike/r5_worldmonitor/r5_prep.py
  - .spike/r5_worldmonitor/scored_20.json
  - .planning/phases/00-concept-spike/findings/R5-VERDICT.md
autonomous: false
requirements: [R5]
user_setup:
  - service: worldmonitor
    why: "D-05 evaluates COP by cloning + running the real World Monitor repo against the local LLM"
    note: "Clone github.com/koala73/worldmonitor into .spike/r5_worldmonitor/worldmonitor; Node 22.12.0 + Rust 1.91.1 available (RESEARCH § Environment Availability). First Tauri build can take 10-20 min."

must_haves:
  truths:
    - "20 InfoTriage items (the same fresh corpus, D-03) are scored through the existing pipeline and exported for World Monitor"
    - "World Monitor runs against the LOCAL LLM (oMLX/Ollama) only — no cloud LLM call occurs in any tier"
    - "A CCIR-structured brief is produced and compared head-to-head with InfoTriage write_bluf() on the same 20 items"
    - "An adopt / build / drop decision is recorded for ADR-005, including setup friction"
    - "R5 ends with a recorded go/no-go/partial verdict; partial is never silently treated as a pass"
  artifacts:
    - path: ".spike/r5_worldmonitor/r5_prep.py"
      provides: "Scores 20 corpus items via existing triage pipeline; exports WM-compatible JSON"
      ephemeral: true
    - path: ".spike/r5_worldmonitor/scored_20.json"
      provides: "20 pre-scored items (ccir/cnr/score/why) for the COP display + brief comparison"
      ephemeral: true
    - path: ".planning/phases/00-concept-spike/findings/R5-VERDICT.md"
      provides: "Durable R5 adopt/build/drop verdict + setup friction, feeding ADR-005"
      ephemeral: false
  key_links:
    - from: ".spike/r5_worldmonitor/r5_prep.py"
      to: "score/triage_score.py + score/digest.py"
      via: "reuses the existing scoring + write_bluf() to pre-score 20 items and to produce the comparison CCIR brief"
      pattern: "triage_score|write_bluf"
    - from: "World Monitor (Tauri)"
      to: "local LLM 127.0.0.1"
      via: "Ollama/oMLX endpoint configured in WM settings; network tab confirms local-only"
      pattern: "127\\.0\\.0\\.1"
  prohibitions:
    - statement: "MUST NOT call a cloud LLM in any spike runtime"
      status: resolved
      verification: "WM 4-tier fallback (Ollama->Groq->OpenRouter->T5) is pinned to local Ollama/oMLX; network tab watched for any Groq/OpenRouter/cloud call (R5 is the highest cloud-leak risk in this phase)"
    - statement: "MUST NOT mutate production data or the running pipeline"
      status: resolved
      verification: "r5_prep reads .spike/items.json read-only; scoring writes only .spike/r5_worldmonitor/; verdicts.jsonl untouched"
    - statement: "MUST NOT merge throwaway spike code into apps/ or libs/"
      status: resolved
      verification: "WM clone + r5_prep live under .spike/r5_worldmonitor/ (gitignored), deleted at closeout"
---

<objective>
Ground the COP decision in a real test, not aspiration. Clone and run the real World Monitor repo
against the local LLM (D-05), feed it 20 InfoTriage-scored items (the same fresh corpus, D-03), have
it produce a brief, and compare that brief head-to-head with InfoTriage's existing CCIR write_bluf()
brief on the same items. Record an adopt / build / drop decision for ADR-005. The full COP/map UI
build is the SP-COP gated spike / later, depending on this verdict (Deferred).

Purpose: COP is currently aspiration ("Palantir at personal scale"); World Monitor is unproven for
CCIR/SAB. WM has no native CCIR taxonomy (it scores a Country Instability Index), so the CCIR-structured
brief comes from InfoTriage pre-scoring + comparison.
Output: r5_prep export + WM run observations + durable adopt/build/drop verdict.
</objective>

## Artifacts This Phase Produces (this plan)

| Artifact | Kind | Durable? |
|----------|------|----------|
| `.spike/r5_worldmonitor/worldmonitor/` (cloned repo) | external repo | Ephemeral — deleted Plan 07 |
| `.spike/r5_worldmonitor/r5_prep.py` (`prep_20`) | script | Ephemeral |
| `.spike/r5_worldmonitor/scored_20.json` (20 items w/ ccir,cnr,score,why) | data | Ephemeral |
| `findings/R5-VERDICT.md` (adopt/build/drop + friction + brief comparison) | verdict | **Durable** |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-concept-spike/00-SPEC.md
@.planning/phases/00-concept-spike/00-RESEARCH.md
@.planning/phases/00-concept-spike/00-01-SUMMARY.md
@ccir.md
@score/digest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-score 20 corpus items + build the InfoTriage CCIR comparison brief (D-03)</name>
  <files>.spike/r5_worldmonitor/r5_prep.py, .spike/r5_worldmonitor/scored_20.json</files>
  <read_first>
    - .planning/phases/00-concept-spike/00-RESEARCH.md § R5 (CCIR Scoring Gap, Injection Strategy Option A, ADR-005 Decision Inputs)
    - score/triage_score.py (existing scorer; returns {ccir,cnr,score,why}) and score/digest.py write_bluf() (the CCIR brief baseline)
    - ccir.md (CCIR taxonomy WM lacks)
    - .planning/phases/00-concept-spike/00-01-SUMMARY.md (shared corpus)
  </read_first>
  <action>
    Health-check the local LLM first (RESEARCH Pitfall 8). Write `.spike/r5_worldmonitor/r5_prep.py`
    exposing `prep_20()` that takes 20 items from the shared .spike/items.json (D-03 reuse), scores
    each through the EXISTING pipeline (score/triage_score.py, returning {ccir,cnr,score,why} against
    ccir.md), and writes .spike/r5_worldmonitor/scored_20.json in a WM-compatible event shape (per
    RESEARCH Option A: pre-score with InfoTriage, feed to WM). Also generate the InfoTriage baseline
    CCIR brief for those same 20 items via the existing write_bluf() and save it alongside, because
    R5's deliverable is a COMPARISON of WM's brief against InfoTriage's write_bluf() output (RESEARCH
    Open Question 2 — this comparison IS the ADR-005 input). All scoring runs on local qwen36
    (ADR-004), read-only against the corpus.
  </action>
  <verify>
    <automated>omlx-ensure-server >/dev/null 2>&1; python3 .spike/r5_worldmonitor/r5_prep.py && python3 -c "import json,sys; d=json.load(open('.spike/r5_worldmonitor/scored_20.json')); sys.exit(0 if len(d)==20 and all('ccir' in i for i in d) else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] scored_20.json holds exactly 20 items, each with ccir/cnr/score/why
    - [ ] An InfoTriage write_bluf() baseline brief for the same 20 items is saved for comparison
    - [ ] grep confirms scoring uses the local pipeline (llm()), no cloud endpoint
  </acceptance_criteria>
  <done>20 corpus items pre-scored (ccir/cnr/score/why) + InfoTriage write_bluf() baseline brief saved for comparison.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Run World Monitor against local LLM, compare brief, record adopt/build/drop (D-05)</name>
  <read_first>
    - .planning/phases/00-concept-spike/00-RESEARCH.md § R5 (Repository, Critical Architecture Finding, Setup Steps, Known Blockers, ADR-005 Decision Inputs) and § Common Pitfalls (Pitfall 4 web mode needs Vercel)
    - .planning/phases/00-concept-spike/00-VALIDATION.md (manual-only R5 GUI steps)
    - .spike/r5_worldmonitor/scored_20.json + the saved InfoTriage baseline brief
  </read_first>
  <action>
    Pause for the human to clone + run the real World Monitor (D-05) against the local LLM, confirm
    no cloud LLM call, compare its brief to the InfoTriage baseline, and record the adopt/build/drop
    verdict with setup friction.
  </action>
  <what-built>
    Task 1 produced 20 pre-scored items + an InfoTriage CCIR baseline brief. World Monitor is a GUI
    (Tauri desktop) app whose launch, COP globe render, local-LLM wiring, and brief quality can only
    be judged by a human (VALIDATION manual-only). Ollama local-LLM support is exclusive to the Tauri
    desktop runtime, not web dev mode (RESEARCH Critical Finding).
  </what-built>
  <how-to-verify>
    1. Clone github.com/koala73/worldmonitor into .spike/r5_worldmonitor/worldmonitor; npm install.
    2. Build the desktop app: `npm run tauri dev` (Rust 1.91.1 available). If it fails, fall back to
       `npm run dev` + `npx vercel dev` and record the friction (RESEARCH A5).
    3. In WM Settings -> LLMs, set the endpoint to the local Ollama (127.0.0.1:11434) or oMLX
       (127.0.0.1:8000); model qwen36-ud-4bit. Confirm auto-discover via /v1/models or set manually.
    4. CRITICAL local-only check: open the network tab and confirm every LLM request hits 127.0.0.1 —
       NO Groq / OpenRouter / cloud call in any fallback tier (ADR-004). Abort and record no-go if cloud is hit.
    5. Feed/display the 20 pre-scored items; have WM produce its brief; COMPARE it side-by-side with
       the InfoTriage write_bluf() baseline on CCIR coverage, CNR elevation, and usefulness.
    6. Record at .planning/phases/00-concept-spike/findings/R5-VERDICT.md: adopt / build / drop
       (go/no-go/partial), the clone->running setup time (hours), whether oMLX was accepted, the CII-vs-CCIR
       gap, and the brief comparison. Per SPEC Constraints, a setup that only partly works is a partial, never a silent pass.
  </how-to-verify>
  <acceptance_criteria>
    - [ ] World Monitor runs against the local LLM with NO observed cloud LLM call
    - [ ] WM brief produced and compared to the InfoTriage write_bluf() baseline on the same 20 items
    - [ ] findings/R5-VERDICT.md records adopt/build/drop + setup friction + comparison
  </acceptance_criteria>
  <resume-signal>Type "adopt", "build", "drop", or "partial" with the setup-time + comparison notes.</resume-signal>
  <done>WM ran against local LLM (no cloud call); brief compared to write_bluf() baseline; ADR-005 adopt/build/drop verdict recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| World Monitor → LLM backends | WM's 4-tier fallback can reach cloud providers (Groq/OpenRouter) if not pinned local |
| WM web mode → Vercel/external APIs | Web dev mode proxies external data via Vercel; many sources fail without it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-R5-01 | Information Disclosure | WM cloud-LLM fallback tiers | mitigate | Pin WM to local Ollama/oMLX; watch network tab; no-go if any cloud LLM call occurs (ADR-004) |
| T-00-R5-02 | Tampering | prod data / running pipeline | mitigate | r5_prep reads items.json read-only; WM clone isolated under .spike/; verdicts.jsonl untouched |
| T-00-R5-03 | Tampering | WM external data calls (web mode) | accept | Spike uses Tauri desktop (local sidecar); record any external calls if web-mode fallback is used |
</threat_model>

<verification>
- 20 items pre-scored (ccir/cnr/score/why); InfoTriage baseline brief saved.
- WM runs against local LLM with no cloud call; brief compared to write_bluf() baseline.
- findings/R5-VERDICT.md exists with an adopt/build/drop (go/no-go/partial) line.
</verification>

<success_criteria>
- R5 acceptance met (WM scores 20 items + CCIR-structured brief, compared) or explicit partial.
- ADR-005 adopt/build/drop recorded with real setup friction (D-05).
</success_criteria>

## Assumptions
- `npm run tauri dev` builds on this Mac with Rust 1.91.1 + XCode CLT (RESEARCH A5); else web-mode +
  vercel dev fallback, friction recorded.
- WM accepts the oMLX :8000/v1 endpoint via /v1/models auto-discovery (RESEARCH A6/A8); if it only
  speaks Ollama /api/tags, run Ollama with qwen36 pulled instead.
- WM has no native CCIR taxonomy (RESEARCH): the CCIR-structured brief is InfoTriage's; WM is judged
  on COP display + brief usefulness vs the write_bluf() baseline.

<output>
Create `.planning/phases/00-concept-spike/00-06-SUMMARY.md` when done.
</output>
