---
phase: 02-storage-postgres-blobs
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - apps/triage/digest.py
  - tests/test_digest_retrofit.py
autonomous: true
requirements: [ADR-001]
user_setup: []

must_haves:
  truths:
    - "Running the retrofitted digest persists each scored verdict through the store as a contracts.Item — rows/entries appear via store.put_item (R6)."
    - "The verdict-dict to contracts.Item mapping puts core fields in columns and the score fields (ccir, cnr, pmesii, tessoc, score, why, bucket) into Item.payload (R6, D-02)."
    - "The legacy data/verdicts.jsonl append path is no longer the persistence mechanism; no historical backfill of prior verdicts.jsonl or feed state is performed (R6, fresh-forward)."
    - "The full pytest suite (87 prior + all new Phase 2 tests) is green with no DB running — integration tests auto-skip (R6, R8)."
  artifacts:
    - apps/triage/digest.py
    - tests/test_digest_retrofit.py
  key_links:
    - "digest.main() constructs a Store from INFOTRIAGE_PG_DSN as a context manager (D-03) and calls store.put_item(item) where it previously called persist() (R6)."
    - "the retrofit unit test injects an InMemoryStore to assert digest persistence routes through the store without requiring a live DB."
  prohibitions:
    - statement: "MUST NOT silently lose or truncate a write — a failed persist raises; never a silent success (documented content-dedup no-op excepted)."
      status: resolved
      verification: "the retrofit routes through store.put_item, which raises on failure (verified in plans 02/03); the retrofit does not wrap put_item in a swallow/except-pass."
---

<objective>
Retrofit the existing triage `digest.py` so persistence flows through the `store` interface instead of appending to `data/verdicts.jsonl` (R6), then prove the whole Phase 2 surface is green. This is the final success criterion of the phase goal: "a single store interface mediates all reads/writes; existing scripts go through it."

Purpose: The store only earns its keep once a real caller uses it. digest.py is the one script in scope that persists (RESEARCH assumption A4: triage_score.py / fever_triage.py do not write a store). The retrofit is deliberately shallow — map the scored verdict dict to a contracts.Item and call store.put_item — with no migration of historical verdicts.jsonl (fresh-forward, SPEC out-of-scope).

Output: a retrofitted digest.py persisting via the store, a retrofit unit test using InMemoryStore, and a green full suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-storage-postgres-blobs/02-SPEC.md
@.planning/phases/02-storage-postgres-blobs/02-PATTERNS.md

# Retrofit target + analogs
@apps/triage/digest.py
@apps/triage/triage_score.py
@libs/contracts/src/contracts/_item.py
@libs/store/src/store/_inmemory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrofit digest.py persistence onto the store</name>
  <read_first>
    - 02-PATTERNS.md "apps/triage/digest.py (retrofit — modify existing)" — the persist() target (lines ~117-121), main() construction pattern (lines ~322-348), and the verdict→Item mapping
    - apps/triage/digest.py current persist(verdicts), STORE constant, fetch_window (verdict shape), and main()
    - apps/triage/triage_score.py score_item return: {**it, **v} → verdict dict has title, source, summary, ccir, cnr, pmesii, tessoc, score, why, bucket; digest's fetch_window adds id (fever id), url, t (epoch seconds)
    - libs/contracts/src/contracts/_item.py (Item fields + the computed sha256 id)
  </read_first>
  <files>apps/triage/digest.py</files>
  <action>
    Replace the file-append persistence in digest.py with store-backed persistence (R6). Introduce a helper that maps one verdict dict to a contracts.Item: source = verdict["source"]; source_type = "rss" (digest pulls FreshRSS/Fever items — RSS); url = verdict.get("url", ""); title = verdict["title"]; ts = datetime.fromtimestamp(verdict["t"], tz=timezone.utc) (verdict carries epoch seconds in "t"); lang = "no" (digest corpus is Norwegian-primary; not tracked per-verdict — documented default, executor may refine if a lang field becomes available); summary = verdict.get("summary"); body_ref = None (no blob in this retrofit). Put the score/enrichment fields into Item.payload: ccir, cnr, pmesii, tessoc, score, why, bucket, and the upstream fever id (e.g. payload["fever_id"] = verdict.get("id")). Item.id is then the computed sha256 of source_type+url+title.

    In main(): read INFOTRIAGE_PG_DSN from the environment and construct a PostgresStore(dsn, blob_root=Path("data/blobs")) as a context manager (D-03 pattern), call store.init_schema() once, and persist each verdict via store.put_item(map_verdict_to_item(v)) where the old code called persist(verdicts). Remove the STORE constant and the verdicts.jsonl write path — do not read or migrate the old file (fresh-forward; R6 out-of-scope backfill). Keep the rendering/view logic (write_brief/write_cluster/write_bluf/write_list) unchanged — those still operate on the in-memory verdicts list for this phase. Do not wrap put_item in a try/except that swallows errors (no-silent-loss prohibition): let a failed persist raise.

    Keep the retrofit minimal and surgical — only the persistence seam changes; scoring, fetching, and digest rendering are untouched.
  </action>
  <verify>
    <automated>python -c "import ast,sys; src=open('apps/triage/digest.py').read(); t=ast.parse(src); names={n.id for n in ast.walk(t) if isinstance(n,ast.Name)}; assert 'put_item' in src and 'init_schema' in src, 'must persist via store'; assert 'INFOTRIAGE_PG_DSN' in src, 'must read DSN from env'; import importlib; print('digest imports + parses ok')" && python -c "import importlib,sys; sys.path.insert(0,'apps/triage'); import digest; assert hasattr(digest,'main'); print('digest module loads')"</automated>
  </verify>
  <acceptance_criteria>
    - digest.py persists via store.put_item (no append to data/verdicts.jsonl); STORE/verdicts.jsonl write path removed.
    - main() constructs a Store from INFOTRIAGE_PG_DSN and calls init_schema once.
    - The verdict→Item mapping places score fields in Item.payload; digest still imports and parses cleanly.
    - No swallow-on-failure around put_item.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retrofit unit test + full-suite green</name>
  <files>tests/test_digest_retrofit.py</files>
  <read_first>
    - libs/store/src/store/_inmemory.py (InMemoryStore — inject as the test double)
    - apps/triage/digest.py (the retrofitted mapping/persist seam to exercise)
    - tests/test_write_bluf.py (existing digest test style to mirror)
  </read_first>
  <behavior>
    - Mapping a representative verdict dict yields a contracts.Item whose core fields match and whose payload carries ccir/cnr/score/why/bucket.
    - Persisting a list of verdicts through an InMemoryStore results in get_item(item.id) returning each mapped Item (rows appear via the store, R6).
    - Two verdicts that map to the same Item.id (same source_type+url+title) collapse to one stored entry (relies on the R5 upsert).
  </behavior>
  <action>
    Author tests/test_digest_retrofit.py exercising the retrofit's verdict→Item mapping and store-backed persistence against an InMemoryStore (no live DB needed). Import the mapping helper from digest, build representative verdict dicts (mirroring the score_item + fetch_window shape: title/source/summary/ccir/cnr/pmesii/tessoc/score/why/bucket/id/url/t), map and put them through an InMemoryStore, and assert each is retrievable via get_item with payload preserved, and that same-identity verdicts upsert to one entry. Do not call the LLM or FreshRSS — exercise only the persistence seam.

    Then confirm the entire phase suite is green with no DB: the existing 87 tests plus all Phase 2 unit tests, with integration tests auto-skipped.
  </action>
  <verify>
    <automated>pytest tests/test_digest_retrofit.py -x -q && pytest tests/ -q -k "not db_live"</automated>
  </verify>
  <acceptance_criteria>
    - The retrofit test passes: mapping correctness, store-backed persistence retrievable via get_item, and same-identity upsert.
    - Full suite green with no DB (87 prior + new unit tests; integration auto-skipped) — no regressions.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scored verdict → store | LLM-derived verdict fields cross into persisted Item.payload |
| env → digest | INFOTRIAGE_PG_DSN enters digest.main() from the environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | digest persistence via store | high | mitigate | Persistence goes through store.put_item, which uses parameterized SQL (plan 03); digest never builds SQL. Verdict values are bound, not interpolated. |
| T-02-03 | Information Disclosure | INFOTRIAGE_PG_DSN in digest.main() | high | mitigate | DSN read from env only (D-03); not logged or written to digest output. |
| T-02-06 | Information Disclosure | LLM verdict fields persisted to payload | low | accept | Verdict fields (ccir/cnr/score/why) are non-secret triage metadata; PII/secrets-in-payload review is deferred to /gsd-secure-phase per SPEC breadcrumb. |
</threat_model>

<verification>
- digest.py persists via store.put_item; no verdicts.jsonl append path; main() builds a Store from INFOTRIAGE_PG_DSN.
- `pytest tests/test_digest_retrofit.py -q` green (InMemoryStore-backed; no DB).
- `pytest tests/ -q -k "not db_live"` green: 87 prior + all new Phase 2 unit tests, no regressions.
</verification>

<success_criteria>
- The existing digest script persists through the single store interface (R6) — phase goal criterion 3 satisfied.
- No historical backfill (fresh-forward); legacy verdicts.jsonl path removed.
- Full suite green with no DB; integration tests skip cleanly.
</success_criteria>

<output>
Create `.planning/phases/02-storage-postgres-blobs/02-04-SUMMARY.md` when done.
</output>
