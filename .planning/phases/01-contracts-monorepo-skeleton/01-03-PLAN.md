---
phase: 01-contracts-monorepo-skeleton
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - .planning/REQUIREMENTS.md
  - README.md
autonomous: true
requirements: [R6]
user_setup: []

must_haves:
  truths:
    - "REQUIREMENTS.md C-9 and C-13 describe yt_to_atom.py and imap_to_atom.py as implemented (their obsolete 'scaffolded' descriptor is removed) — SPEC R6 target (a)."
    - "REQUIREMENTS.md A-5 (PMESII operational-domain tagging) is marked done rather than [PLANNED], since Phase 1.5 PMESII enrichment already shipped — SPEC R6 target (b)."
    - "README.md and PROJECT.md document that .env.example exists (no claim that it is missing) — SPEC R6 target (c)."
    - "README.md run-commands and path references point to apps/triage, apps/ingest, and apps/opml after the restructure — they no longer invoke the removed score/, bridge/, or opml/ directories."
  artifacts:
    - .planning/REQUIREMENTS.md
    - README.md
  key_links:
    - "Every doc path reference resolves to a file that exists post-restructure: apps/triage/triage_score.py, apps/triage/fever_triage.py, apps/ingest/gmail_to_atom.py, apps/ingest/RSS_BRIDGE_NOTES.md, apps/opml/feeds.opml (the move landed in plan 01-02)."
  prohibitions: []
---

<objective>
Correct the three stale documentation claims SPEC R6 names — (a) imap/youtube bridges still
called "scaffolded", (b) PMESII/TESSOC tagging still marked as a future plan, (c) any claim
that .env.example is missing — and bring README.md's run-commands and path references in line
with the apps/ tree produced by plan 01-02, so the headline user doc stays accurate after the move.

Purpose: Delivers SPEC R6. Documentation-only; no code, schema, or test changes.

Output: corrected .planning/REQUIREMENTS.md (C-9, C-13, A-5) and README.md (status wording +
apps/ path references). PROJECT.md already carries the three corrections and is confirmed, not rewritten.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-contracts-monorepo-skeleton/01-SPEC.md
@.planning/REQUIREMENTS.md
@README.md

# SPEC R6 names three stale claims. Live state at planning time: PROJECT.md already carries
# all three corrections (its bridges row, score row, and .env.example row). README already
# references .env.example and describes the bridges as functional. The remaining stale
# assertions are in REQUIREMENTS.md C-9 / C-13 (bridges) and A-5 (PMESII). README's
# script paths (score/, bridge/, opml/) are stale only because plan 01-02 moved the files.
</context>

<artifacts_this_phase_produces>
Documentation edits only (no new symbols or files):
- .planning/REQUIREMENTS.md — C-9 and C-13 source-notes reworded to "implemented"; A-5 status
  changed from [PLANNED] to done; path references in those rows updated to apps/ingest/*.
- README.md — run-command and prose path references updated score/ -> apps/triage/,
  bridge/ -> apps/ingest/, opml/feeds.opml -> apps/opml/feeds.opml, bridge/RSS_BRIDGE_NOTES.md
  -> apps/ingest/RSS_BRIDGE_NOTES.md.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Correct the three stale R6 doc claims in REQUIREMENTS.md (+ confirm PROJECT.md / README)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/01-contracts-monorepo-skeleton/01-SPEC.md §Requirements R6 (the three claims + acceptance)
    - .planning/REQUIREMENTS.md (rows C-9 line 29, C-13 line 33, A-5 line 55)
    - .planning/PROJECT.md (the bridges row, score/PMESII row, and .env.example row that already carry corrections)
  </read_first>
  <action>
    <!-- planner-discipline-allow: scaffolded -->
    In .planning/REQUIREMENTS.md correct the three stale claims SPEC R6 names. Row C-9
    (YouTube): replace the obsolete "scaffolded 2026-06-23, operator-pivot" descriptor in the
    Source column with wording that states yt_to_atom.py is implemented (XML-gen + escaping
    verified) and update the file reference from bridge/yt_to_atom.py to apps/ingest/yt_to_atom.py;
    the [SPIKE] status MAY remain because a transcribe backend is still pending at runtime, but
    the obsolete descriptor must go. Row C-13 (multi-IMAP): make the same change for
    imap_to_atom.py -> apps/ingest/imap_to_atom.py, replacing the obsolete descriptor with
    "implemented" wording. Row A-5 (PMESII operational-domain tagging): change the status from
    [PLANNED] to a done marker and reword the source note to reflect that Phase 1.5 PMESII
    enrichment already shipped (it lives in .planning/archive/phase-1.5-pmesii-enrichment/).
    Do NOT touch the unrelated NF-10 row, which legitimately uses the word "scaffold" to mean
    the GSD planning scaffold. Confirm (do not rewrite) that .planning/PROJECT.md already states
    the bridges are working, PMESII/TESSOC enrichment is done, and .env.example exists, and that
    README.md references .env.example — these are the "each correction is present" half of R6.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -z "$(grep -nE '(yt_to_atom|imap_to_atom)' .planning/REQUIREMENTS.md | grep -i 'scaffolded')" && test -z "$(grep 'A-5' .planning/REQUIREMENTS.md | grep 'PLANNED')" && grep -n 'apps/ingest/yt_to_atom.py' .planning/REQUIREMENTS.md && grep -n 'apps/ingest/imap_to_atom.py' .planning/REQUIREMENTS.md && grep -niE 'env\.example' .planning/PROJECT.md README.md && echo R6-DOCS OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify prints `R6-DOCS OK`.
    - No REQUIREMENTS.md row mentioning yt_to_atom or imap_to_atom still carries the obsolete "scaffolded" descriptor (the targeted negative grep is empty).
    - C-9 and C-13 reference the new apps/ingest/ paths; A-5 is no longer marked [PLANNED] (it shows a done marker).
    - .planning/PROJECT.md and README.md both reference .env.example as existing (the third R6 correction is present).
    - The NF-10 row is unchanged (its "scaffold" wording is untouched).
  </acceptance_criteria>
  <done>The three stale R6 claims are corrected in REQUIREMENTS.md and the already-present corrections in PROJECT.md / README are confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md run-commands and path references to the apps/ tree</name>
  <files>README.md</files>
  <read_first>
    - README.md (run-command lines invoking the moved scripts and the prose Bridges / Feeds sections)
    - .planning/phases/01-contracts-monorepo-skeleton/01-02-PLAN.md §artifacts_this_phase_produces (the authoritative old->new path map)
  </read_first>
  <action>
    In README.md update every reference to a moved file so the headline user doc matches the
    apps/ tree from plan 01-02. Apply this old->new map to both runnable command lines (the
    `python3 ...` invocations and the cron example) and prose/link references:
    score/triage_score.py -> apps/triage/triage_score.py; score/fever_triage.py ->
    apps/triage/fever_triage.py; bridge/gmail_to_atom.py -> apps/ingest/gmail_to_atom.py;
    bridge/imap_to_atom.py -> apps/ingest/imap_to_atom.py; bridge/yt_to_atom.py ->
    apps/ingest/yt_to_atom.py; the "Three `bridge/` scripts" phrasing -> "Three `apps/ingest/`
    scripts"; bridge/RSS_BRIDGE_NOTES.md (link text and href) -> apps/ingest/RSS_BRIDGE_NOTES.md;
    opml/feeds.opml -> apps/opml/feeds.opml. Do NOT change the data/ output paths (data/feeds,
    data/digests, data/triage.log) — those did not move. Do NOT alter the unrelated ASCII
    diagram's conceptual labels except where they name a runnable script path. Leave the
    "oMLX/Ollama" wording alone (out of R6 scope; tracked separately).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -z "$(grep -nE 'python3 (score|bridge)/' README.md)" && test -z "$(grep -n 'opml/feeds.opml' README.md)" && test -z "$(grep -n 'bridge/RSS_BRIDGE_NOTES.md' README.md)" && grep -nE 'apps/(triage|ingest)/' README.md && grep -n 'apps/opml/feeds.opml' README.md && echo README-PATHS OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify prints `README-PATHS OK`.
    - No runnable `python3 score/...` or `python3 bridge/...` command remains in README.md (the targeted negative greps are empty).
    - README.md references apps/triage/ and apps/ingest/ run-commands, apps/opml/feeds.opml, and apps/ingest/RSS_BRIDGE_NOTES.md.
    - data/ output paths are unchanged.
  </acceptance_criteria>
  <done>README.md run-commands and path references point at the apps/ tree; no stale score/, bridge/, or opml/ script path remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan changes documentation files only — no executable code, no input parsing, no
network or credential surface is touched. There is no meaningful attack surface in this phase.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-DOC | Information Disclosure | README.md / REQUIREMENTS.md edits | low | accept | Edits are status-wording and path corrections; reviewer confirms no secret, token, or credential is introduced into the docs. No code path is affected. |
</threat_model>

<verification>
- Task 1 verify prints `R6-DOCS OK`; Task 2 verify prints `README-PATHS OK`.
- The three SPEC R6 corrections are present and the obsolete bridge descriptor is gone from the C-9/C-13 rows.
- README path references resolve to files that exist under apps/ after plan 01-02.
</verification>

<success_criteria>
- SPEC R6 satisfied: the three named stale claims are corrected and each correction is present across REQUIREMENTS.md / PROJECT.md / README.md.
- README is accurate against the post-restructure tree (no broken script paths).
</success_criteria>

<output>
Create `.planning/phases/01-contracts-monorepo-skeleton/01-03-SUMMARY.md` when done.
</output>
