---
phase: 01-contracts-monorepo-skeleton
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - pyproject.toml
  - apps/ingest/gmail_to_atom.py
  - apps/ingest/imap_to_atom.py
  - apps/ingest/yt_to_atom.py
  - apps/ingest/_util.py
  - apps/ingest/RSS_BRIDGE_NOTES.md
  - apps/triage/triage_score.py
  - apps/triage/digest.py
  - apps/triage/fever_triage.py
  - apps/triage/sab_html.py
  - apps/opml/_check.py
  - apps/opml/feeds.opml
  - apps/opml/working.opml
  - tests/test_bridge_escape.py
  - tests/test_ccir_sync.py
  - tests/test_opml_check.py
  - tests/test_opml_roundtrip.py
  - tests/test_score_parse.py
  - tests/test_write_bluf.py
autonomous: true
requirements: [R5]
user_setup: []

must_haves:
  truths:
    - "Every root-reaching path expression across the 5 moved scripts resolves to the repo root (not to apps/) after re-home — the 7 fixed expressions all point one level higher (RESEARCH Pitfall 1, highest risk)."
    - "`pytest tests/` exits 0 with zero failures after the restructure; no test is dropped or weakened (R5 / D-04 coverage-preserving, NOT a hard count of 56)."
    - "apps/ingest, apps/triage, and apps/opml exist; the old bridge/, score/, and opml/ directories no longer exist (R5 / D-06)."
    - "apps/triage/digest.py imports Item from contracts and the import resolves via the editable install (R5 / D-08 — the shared contract is wired, not just present)."
    - "No module under apps/ imports a module belonging to a different apps/ subdir; apps import only their own siblings and libs/contracts (SPEC Prohibition P1)."
    - "No migrated test file contains a sys.path.insert call or an `if __name__ == \"__main__\": unittest.main()` entrypoint (D-05 / D-07)."
    - "tests/test_opml_check.py and tests/test_opml_roundtrip.py locate feeds.opml under apps/opml; tests/test_ccir_sync.py still locates ccir.md at the repo root (it did not move)."
  artifacts:
    - pyproject.toml
    - apps/ingest/gmail_to_atom.py
    - apps/ingest/imap_to_atom.py
    - apps/ingest/yt_to_atom.py
    - apps/ingest/_util.py
    - apps/ingest/RSS_BRIDGE_NOTES.md
    - apps/triage/triage_score.py
    - apps/triage/digest.py
    - apps/triage/fever_triage.py
    - apps/triage/sab_html.py
    - apps/opml/_check.py
    - apps/opml/feeds.opml
    - apps/opml/working.opml
    - tests/test_bridge_escape.py
    - tests/test_ccir_sync.py
    - tests/test_opml_check.py
    - tests/test_opml_roundtrip.py
    - tests/test_score_parse.py
    - tests/test_write_bluf.py
  key_links:
    - "Root pyproject.toml [tool.pytest.ini_options] pythonpath = [apps/triage, apps/opml, apps/ingest] makes `import digest`, `import _check`, `import triage_score`, `from _util import escape` resolve during tests — replacing every removed sys.path.insert (RESEARCH Pattern 5)."
    - "apps/triage/digest.py `from contracts import Item` resolves through the editable install created in plan 01-01 (the cross-phase contract seam, D-08)."
    - "Each depth-fixed constant (CCIR_PATH, digest ROOT, sab_html ROOT, fever ENV, the two .env load calls, gmail OUT) now reaches repo-root ccir.md/.env/data after gaining one directory level."
  prohibitions:
    - statement: "No module under apps/ may import another apps/* package; apps may only import their own siblings and libs/contracts."
      requirement: R5
      status: resolved
      verification: "test — grep import-boundary check across apps/ for cross-subdir module imports returns empty."
---

<objective>
Re-root the repo into apps/ + libs/ without changing pipeline behavior: re-home the flat
bridge/, score/, opml/ scripts into apps/ingest/, apps/triage/, apps/opml/ (D-06), apply
every path-depth fix the move requires, wire one re-homed script to import Item from
contracts (D-08), add the root pytest config that replaces the sys.path hacks, and migrate
the 6 test files from unittest to pytest (D-03/D-05/D-07) — ending with the full suite green.

Purpose: Delivers SPEC R5 (the monorepo restructure) and proves the contract seam is wired,
while preserving every existing behavior (the 56 tests stay green, coverage-preserving per D-04).

Output: apps/** (re-homed + depth-fixed scripts), root pyproject.toml pytest config, 6
migrated test files, and a green `pytest tests/` over the whole suite (migrated tests +
test_contracts.py from plan 01-01).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contracts-monorepo-skeleton/01-SPEC.md
@.planning/phases/01-contracts-monorepo-skeleton/01-RESEARCH.md
@.planning/phases/01-contracts-monorepo-skeleton/01-PATTERNS.md

# 01-RESEARCH.md §Critical File Change Inventory + §Common Pitfalls (1-7) and
# 01-PATTERNS.md §Pattern Assignments hold the path-fix and migration specifics. NOTE the
# corrections in this plan: the path inventory below is EXHAUSTIVE (7 expressions in 5
# files) — it adds three expressions RESEARCH/PATTERNS missed (sab_html.py ROOT,
# gmail_to_atom.py OUT, gmail_to_atom.py .env load) and corrects two facts
# (RSS_BRIDGE_NOTES.md lives in bridge/, not opml/; the triage_score .env load is at
# line 180, not line 43). There is NO score/_util.py (RESEARCH Pitfall 7).
</context>

<artifacts_this_phase_produces>
This plan MOVES (not net-new) the following and creates the root pytest config. New/changed
paths the rest of the repo now depends on:

- New dir apps/ingest/ <- bridge/* : gmail_to_atom.py, imap_to_atom.py, yt_to_atom.py,
  _util.py, RSS_BRIDGE_NOTES.md.
- New dir apps/triage/ <- score/* : triage_score.py, digest.py, fever_triage.py, sab_html.py.
- New dir apps/opml/ <- opml/* : _check.py, feeds.opml, working.opml.
- Removed dirs: bridge/, score/, opml/.
- New file pyproject.toml (root) with [tool.pytest.ini_options] testpaths + pythonpath.
- apps/triage/digest.py gains `from contracts import Item` + a module-level `__contract__ = Item`
  reference (D-08 wiring; type/marker only — zero runtime behavior change).
- 6 test files migrated in place to pytest function style.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add the root pyproject.toml pytest config (pythonpath replaces sys.path hacks)</name>
  <files>pyproject.toml</files>
  <read_first>
    - .planning/phases/01-contracts-monorepo-skeleton/01-RESEARCH.md §Architecture Patterns Pattern 5 (pytest config) and §Open Questions Q2/Q3 (pyproject over pytest.ini / conftest.py)
    - requirements.txt (confirm no existing root pyproject — there is none today)
  </read_first>
  <action>
    Create a repo-root pyproject.toml containing a [tool.pytest.ini_options] table with
    testpaths = ["tests"] and pythonpath = ["apps/triage", "apps/opml", "apps/ingest"]
    (resolves Open Question 3 in favor of pyproject.toml, NOT a separate pytest.ini, and NOT
    a conftest.py — the pythonpath option alone replaces every sys.path.insert removed in
    Task 3). The pythonpath entries point at directories created in Task 2; listing
    not-yet-existing paths is harmless. Do not add any other tables to this file in this phase.
  </action>
  <verify>
    <automated>python3 -c "import tomllib; d=tomllib.load(open('pyproject.toml','rb')); p=d['tool']['pytest']['ini_options']; assert p['testpaths']==['tests']; assert p['pythonpath']==['apps/triage','apps/opml','apps/ingest']; print('TASK1 OK')"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml parses as valid TOML and the Task 1 verify prints `TASK1 OK`.
    - `[tool.pytest.ini_options]` declares testpaths ["tests"] and pythonpath ["apps/triage","apps/opml","apps/ingest"].
    - No pytest.ini and no tests/conftest.py is created (the pyproject pythonpath is the single config seam).
  </acceptance_criteria>
  <done>The root pytest config exists and declares the three app dirs on pythonpath, ready to replace the per-test sys.path.insert calls.</done>
</task>

<task type="auto">
  <name>Task 2: Re-home all scripts into apps/ with the EXHAUSTIVE path-depth fixes + D-08 wiring</name>
  <files>apps/ingest/gmail_to_atom.py, apps/ingest/imap_to_atom.py, apps/ingest/yt_to_atom.py, apps/ingest/_util.py, apps/ingest/RSS_BRIDGE_NOTES.md, apps/triage/triage_score.py, apps/triage/digest.py, apps/triage/fever_triage.py, apps/triage/sab_html.py, apps/opml/_check.py, apps/opml/feeds.opml, apps/opml/working.opml</files>
  <read_first>
    - .planning/phases/01-contracts-monorepo-skeleton/01-RESEARCH.md §Critical File Change Inventory and §Common Pitfalls 1 (path depth) + 7 (no score/_util.py)
    - .planning/phases/01-contracts-monorepo-skeleton/01-PATTERNS.md §Pattern Assignments (per-file analog + identity-move list)
    - score/triage_score.py, score/digest.py, score/fever_triage.py, score/sab_html.py, bridge/gmail_to_atom.py (the five files that carry root-reaching path expressions)
  </read_first>
  <action>
    Move files with `git mv` to preserve history, then apply the path-depth fixes. Each move
    adds one directory level, so every os.path expression that used a single ".." to reach the
    repo root must gain a second "..". Apply EXACTLY these 7 fixes (file:line -> change). This
    inventory is exhaustive and corrects gaps in RESEARCH/PATTERNS — verify each line number
    against the moved file, then edit:

    apps/triage/ (from score/):
    - triage_score.py CCIR_PATH (was score/triage_score.py:19): in os.path.join(os.path.dirname(__file__), "..", "ccir.md") insert one more ".." before "ccir.md".
    - triage_score.py .env load (was score/triage_score.py:180 — NOT line 43 as RESEARCH states): in the load_dotenv(os.path.join(os.path.dirname(__file__), "..", ".env")) call insert one more ".." before ".env".
    - digest.py ROOT (was score/digest.py:24): in ROOT = os.path.join(os.path.dirname(__file__), "..") append a second ".." — this single fix cascades to OUT (data/digests), STORE (data/verdicts.jsonl), the CCIR drift-guard open(os.path.join(ROOT,"ccir.md")), and load_dotenv(os.path.join(ROOT,".env")); no further edits to those lines.
    - fever_triage.py ENV (was score/fever_triage.py:20): in ENV = os.path.join(os.path.dirname(__file__), "..", ".env") insert one more ".." before ".env".
    - sab_html.py ROOT (was score/sab_html.py:23 — MISSED by RESEARCH/PATTERNS, which wrongly called this an identity move): in ROOT = os.path.join(os.path.dirname(__file__), "..") append a second ".." — cascades to STORE (line 24), OUT (line 25), and load_dotenv(os.path.join(ROOT,".env")) (line 1176); no further edits. sab_html.py MUST land in apps/triage/ (NOT apps/brief/) because it does `from triage_score import load_dotenv, llm` as a sibling via sys.path.insert(0, os.path.dirname(__file__)) — this resolves Open Question 1: the sibling import forces co-location with triage_score.py.

    apps/ingest/ (from bridge/):
    - gmail_to_atom.py OUT (was bridge/gmail_to_atom.py:18 — MISSED by RESEARCH/PATTERNS): in OUT = os.path.join(os.path.dirname(__file__), "..", "data", "feeds", "gmail.xml") insert one more ".." before "data".
    - gmail_to_atom.py .env load (was bridge/gmail_to_atom.py:57 — MISSED by RESEARCH/PATTERNS): in the load_dotenv(os.path.join(os.path.dirname(__file__), "..", ".env")) call insert one more ".." before ".env".
    - imap_to_atom.py, yt_to_atom.py, _util.py: identity moves, no edits (they carry no root-reaching path expression; the `from _util import escape` sibling import still works because Python adds the script's own dir to sys.path at runtime).
    - RSS_BRIDGE_NOTES.md: move bridge/RSS_BRIDGE_NOTES.md -> apps/ingest/RSS_BRIDGE_NOTES.md (it lives in bridge/, NOT opml/ as RESEARCH/PATTERNS state).

    apps/opml/ (from opml/):
    - _check.py, feeds.opml: identity moves (the sibling feeds.opml moves with _check.py, so _check.py's OPML path is unchanged).
    - working.opml: it IS git-tracked (confirmed via git ls-files), so move it (resolves Open Question 2: tracked -> move, do not leave behind).

    D-08 wiring: in apps/triage/digest.py add `from contracts import Item` at the top of the
    import block and a module-level `__contract__ = Item` after the constants. Type/marker
    reference only — annotations and a module attribute are runtime no-ops, so pipeline
    behavior is unchanged while proving the contract resolves. Leave the existing intra-app
    sys.path.insert(0, os.path.dirname(__file__)) lines in digest.py / fever_triage.py /
    sab_html.py in place (they resolve to apps/triage/ post-move; redundant under pytest
    pythonpath but harmless at runtime — RESEARCH).

    Finally, remove the now-empty bridge/, score/, opml/ directories (git rm any remaining
    tracked files; the test suite will be RED between this task and Task 3 because the 6 test
    files still reference the old dirs — Task 3 restores green; this is expected mid-plan).
    There is NO score/_util.py to move (RESEARCH Pitfall 7).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && python3 -c "
import os
root=os.getcwd()
def res(sub, rel): import os; return os.path.realpath(os.path.join(root, sub, rel))
# every depth-fixed constant must resolve to a repo-root artifact that exists
assert res('apps/triage', os.path.join('..','..','ccir.md'))==os.path.join(root,'ccir.md'); assert os.path.exists(os.path.join(root,'ccir.md'))
import sys
for d in ('apps/triage','apps/ingest','apps/opml'): assert os.path.isdir(os.path.join(root,d)), d
for d in ('bridge','score','opml'): assert not os.path.isdir(os.path.join(root,d)), 'old dir remains: '+d
assert os.path.exists('apps/opml/feeds.opml') and os.path.exists('apps/opml/working.opml')
assert os.path.exists('apps/ingest/RSS_BRIDGE_NOTES.md')
print('STRUCT OK')
" && grep -n '"\.\.", "\.\.", "ccir.md"' apps/triage/triage_score.py && grep -nE 'os\.path\.join\(os\.path\.dirname\(__file__\), "\.\.", "\.\."\)' apps/triage/digest.py apps/triage/sab_html.py && grep -n '"\.\.", "\.\.", "data"' apps/ingest/gmail_to_atom.py && grep -n '"\.\.", "\.\.", "\.env"' apps/triage/triage_score.py && grep -n '"\.\.", "\.\.", "\.env"' apps/triage/fever_triage.py && grep -n '"\.\.", "\.\.", "\.env"' apps/ingest/gmail_to_atom.py && grep -n 'from contracts import Item' apps/triage/digest.py && test -z "$(grep -rolE 'import (gmail_to_atom|imap_to_atom|yt_to_atom)' apps/triage apps/opml)" && test -z "$(grep -rolE 'import (triage_score|digest|fever_triage|sab_html|_check)' apps/ingest apps/opml)" && echo PATHS+BOUNDARY OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify prints `STRUCT OK` then `PATHS+BOUNDARY OK`: apps/{ingest,triage,opml} exist, bridge/score/opml are gone, feeds.opml + working.opml + RSS_BRIDGE_NOTES.md landed under apps/.
    - All 7 fixed expressions are present: CCIR_PATH and the .env load in apps/triage/triage_score.py each carry two ".." before the leaf; digest.py and sab_html.py ROOT each end in os.path.join(..., "..", ".."); fever_triage.py ENV carries two ".." before ".env"; gmail_to_atom.py OUT carries two ".." before "data" and its .env load carries two ".." before ".env".
    - `grep -n "from contracts import Item" apps/triage/digest.py` matches and `python3 -c "import sys; sys.path.insert(0,'apps/triage'); import digest; assert digest.Item.__name__=='Item'"` exits 0 (D-08 import resolves).
    - The import-boundary greps are empty: no file under apps/triage or apps/opml imports an apps/ingest module, and no file under apps/ingest or apps/opml imports an apps/triage module (Prohibition P1).
  </acceptance_criteria>
  <done>All scripts are re-homed under apps/ with every path-depth fix applied and verified to resolve to the repo root; digest.py imports Item from contracts; the import boundary holds.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the 6 test files to pytest + fix OPML paths + restore full green suite</name>
  <files>tests/test_bridge_escape.py, tests/test_ccir_sync.py, tests/test_opml_check.py, tests/test_opml_roundtrip.py, tests/test_score_parse.py, tests/test_write_bluf.py</files>
  <read_first>
    - .planning/phases/01-contracts-monorepo-skeleton/01-RESEARCH.md §Architecture Patterns Pattern 7 (unittest -> pytest migration) and §Critical File Change Inventory (test OPML path fixes)
    - .planning/phases/01-contracts-monorepo-skeleton/01-PATTERNS.md §tests migration patterns (setUp/tearDown -> tmp_path, try/finally -> monkeypatch, assertRaises -> pytest.raises)
    - tests/test_bridge_escape.py, tests/test_ccir_sync.py, tests/test_opml_check.py, tests/test_opml_roundtrip.py, tests/test_score_parse.py, tests/test_write_bluf.py (all 6 current files)
  </read_first>
  <action>
    Migrate each file from unittest.TestCase classes to pytest functions, preserving every
    assertion (D-04 — assertion-preserving, zero coverage lost; do NOT drop or weaken any
    case). For every file: delete the sys.path.insert line(s) (pytest pythonpath from Task 1
    now resolves imports), delete the `if __name__ == "__main__": unittest.main()` block,
    unwrap each `class Test*(unittest.TestCase)` into module-level `def test_*()` functions,
    convert self.assertEqual(a,b) -> assert a==b, self.assertRaises(E) -> pytest.raises(E),
    and remove the now-unused `import unittest` (keep `unittest.mock` only if a file actually
    uses it). Apply these per-file specifics:

    - tests/test_bridge_escape.py: drop sys.path.insert(..., "bridge"); keep `from _util import escape` (resolved via apps/ingest on pythonpath).
    - tests/test_ccir_sync.py: drop sys.path.insert(..., "score"); the CCIR_MD constant stays UNCHANGED (os.path.join(..., "..", "ccir.md")) because ccir.md did not move (it remains at repo root).
    - tests/test_opml_check.py: drop sys.path.insert(..., "opml"); change the OPML constant from os.path.join(..., "..", "opml", "feeds.opml") to os.path.join(..., "..", "apps", "opml", "feeds.opml"); migrate any setUp/tearDown tmpdir to the built-in tmp_path fixture.
    - tests/test_opml_roundtrip.py: change the OPML constant from os.path.join(..., "..", "opml", "feeds.opml") to os.path.join(..., "..", "apps", "opml", "feeds.opml") (this file has no sys.path.insert; it parses the OPML file directly).
    - tests/test_score_parse.py: drop sys.path.insert(..., "score") (imports resolve via apps/triage on pythonpath).
    - tests/test_write_bluf.py: drop sys.path.insert(..., "score"); convert BOTH manual `original = triage_score.llm` / try / finally / restore blocks into the monkeypatch fixture (def test_*(monkeypatch): monkeypatch.setattr(triage_score, "llm", failing_llm)) — and PRESERVE the assertion that the credential substring does not appear in the rendered markdown (the credential-leak guard must remain).

    Do not gate completion on the literal number 56 (D-04); gate on a green suite with zero
    failures/errors and no weakened assertions.
  </action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` exits 0 with zero failures and zero errors (covers the 6 migrated files + tests/test_contracts.py from plan 01-01).
    - `grep -rln "sys.path.insert" tests/` returns nothing (all path hacks removed, D-05/D-07).
    - `grep -rln "unittest.main()" tests/` returns nothing (script entrypoints removed, D-05).
    - `grep -nE 'apps.*opml' tests/test_opml_check.py` and `grep -nE 'apps.*opml' tests/test_opml_roundtrip.py` match (OPML path fixed); tests/test_ccir_sync.py still references "..", "ccir.md" (unchanged).
    - tests/test_write_bluf.py uses the monkeypatch fixture and still asserts the credential substring is absent from the rendered markdown.
  </acceptance_criteria>
  <done>All 6 test files run as pytest functions with no sys.path hacks, the OPML paths point at apps/opml, and the entire suite is green — coverage preserved (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| moved script -> repo-root files | Depth-fixed os.path expressions read ccir.md / .env / data from the repo root; a wrong depth silently reads the wrong location. |
| script -> .env (gitignored) | The .env load calls must continue to read the gitignored repo-root .env; secrets must never be committed via the move. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-PATH | Tampering | depth-fixed path constants in moved scripts | high | mitigate | Apply the exhaustive 7-expression fix table and verify each constant resolves to an existing repo-root artifact (Task 2 verify). A missed fix silently loads fallback CCIR / wrong data — the per-constant resolution check is the gate. |
| T-02-SECRET | Information Disclosure | .env load paths in triage_score.py / fever_triage.py / digest.py / gmail_to_atom.py | medium | mitigate | The depth fix keeps these reading the gitignored repo-root .env; no .env or credential is added to git. The preserved credential-leak test in test_write_bluf.py guards against secrets reaching rendered output. |
| T-02-BOUND | Tampering | cross-app imports under apps/ | low | mitigate | Import-boundary grep (Task 2 verify, Prohibition P1) ensures no apps/ subdir imports another. |

No package installs occur in this plan, so no supply-chain (T-SC) checkpoint applies. No high-severity threat is left un-mitigated.
</threat_model>

<verification>
- `pytest tests/ -q` exits 0 over the whole suite.
- All 7 path-depth fixes resolve to existing repo-root artifacts (Task 2 verify).
- apps/{ingest,triage,opml} exist; bridge/, score/, opml/ are gone.
- Import-boundary grep returns empty (Prohibition P1).
- No sys.path.insert and no unittest.main() remain under tests/.
</verification>

<success_criteria>
- SPEC R5 satisfied: repo re-rooted into apps/ + libs/; a re-homed script imports Item from contracts; the full suite is green with coverage preserved (D-04).
- No behavior change: every moved script reads the same repo-root files it did before the move.
</success_criteria>

<output>
Create `.planning/phases/01-contracts-monorepo-skeleton/01-02-SUMMARY.md` when done.
</output>
