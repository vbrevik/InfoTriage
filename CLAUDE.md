# InfoTriage — project instructions

## Developer Preferences

See `USER-PROFILE.md` for the full behavioral profile. Key preferences:

- **Direct and task-oriented.** Keep responses concise and actionable.
- **Testing-first.** Always verify with tests, typechecks, or health checks when relevant.
- **GSD framework.** Maintain `.planning/` files (PROJECT.md, STATE.md, ROADMAP.md, HANDOFF.json) in sync with work.
- **No surprises.** Do not modify files unless explicitly or implicitly requested.
- **Milestone commits.** Bundle related work into single, descriptive commits; exclude temp files and secrets.

## Design / UI

- **Sketch findings for InfoTriage** (design decisions, CSS patterns, visual direction for the SP-COP
  interactive-SAB canvas) → `Skill("sketch-findings-infotriage")`. Auto-load when building SP-COP UI.
