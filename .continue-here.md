# .continue-here.md — InfoTriage pause handoff

**Paused at:** 2026-06-23 ~14:37 CEST
**Project:** InfoTriage (local OSINT/all-source intelligence hub; CCIR-driven; ADR-004 says all LLM = local qwen3.6)
**Operator context:** interactive session in Codebuff CLI orchestrating a Buffalo/Buffy-Claude sonnet stack; not a multi-phase GSD run

---

## Position

Paused mid-stream after six completed rounds of in-session work. No outstanding **mandatory** action — everything shipped from the most recent followups (MAJOR-1 closed, DRIFT-1 closed) was verified via end-to-end smoke. Operator functional state at pause:

- All Python source compiles clean (`python3 -m py_compile`).
- Document map complete at `.planning/codebase/` (7 docs from gsd-map-codebase run).
- Scorer prompt now knows all 12 CCIRs (PIR-1..6, FFIR-1..3, SIR-1, SIR-2).
- Drift guard is in place: editing `score/digest.py:CCIR_ORDER` or `ccir.md` without the other raises `AssertionError` at module-import.

Still parked from earlier: Gmail bridge.

**Smoke update 2026-06-24:** YouTube bridge passed end-to-end (stub-mode wiring confirmed); Gmail bridge now passes the shape probe but Gmail still rejects credentials — most likely account-mismatch, requires the operator to regenerate the app password for the Gmail account matching `GMAIL_USER`. FreshRSS at `http://localhost:8088` is at the login page (operator-auth needed to verify the subscribe step).

---

## Completed this session — by round

### Round A — BLUF digest feature (`score/digest.py`)
- Added `("PIR-6", "OSINT & etterforskning")` to `CCIR_ORDER`.
- New function `write_bluf(verdicts, period, top_n=12)` per-topic BLUF — Norwegian prompt, numbered `[1]..[N]` citations, explicit contradiction handling, empty-topic placeholder.
- New CLI: `--mode bluf`, `--bluf-top N`.
- Hardened exception handler: stderr gets full detail, markdown gets stingy Norwegian placeholder. Verified end-to-end that simulated `GMAIL_APP_PASSWORD=abcd1234` exceptions never reach `bluf.md`.

### Round B — File-only work while Gmail bridge parked
- `opml/feeds.opml`: +3 Ukraine feeds in *Forsvar & geopolitikk (intl)* (Ukrainska Pravda ⚠️, RUSI, Euromaidan Press); +2 new top-level outlines (Midtøsten & US-Iran (SIR-1), Sport — VM 2026 (SIR-2), 4 feeds each).
- **Splice bug fixed:** SIR-1 + SIR-2 outline blocks initially landed inside the `<!-- ===== NO native RSS … -->` comment block because the anchor lived inside the comment. XML parsers ignored them. Repositioned via regex+replace; canonical XML parse now sees 61 rss feeds / 10 top-level outlines.
- `ccir.md`: new `## SIR` section between FFIR and CNR. CNR Routine bullet carries NB cross-referencing the SIR-2 carve-out.

### Round C — `CCIR_ORDER` extends to include SIR-1, SIR-2 (`score/digest.py`)
- Now 11 entries (was 9): PIR-1..6, FFIR-1..3, SIR-1, SIR-2.
- `py_compile` clean. `write_bluf` end-to-end emits the new section headers.
- `score/digest.py:CCIR_ORDER`:
  ```python
  [
      ("PIR-1", "Russland / Ukraina"), ("PIR-2", "Nordområdene & Arktis"),
      ("PIR-3", "NATO & europeisk sikkerhet"), ("PIR-4", "Hybrid- & cybertrusler"),
      ("PIR-5", "Stormaktsrivalisering"), ("PIR-6", "OSINT & etterforskning"),
      ("FFIR-1", "Norsk forsvar & sikkerhetspolitikk"),
      ("FFIR-2", "Norsk politikk & samfunn"),
      ("FFIR-3", "Egen teknologikapabilitet"),
      ("SIR-1", "Midtøsten & US-Iran"),
      ("SIR-2", "Sport — VM 2026 (FIFA)"),
  ]
  ```

### Round D — `gsd-map-codebase` deliverable
- 7 docs written to `.planning/codebase/`: `STACK.md`, `INTEGRATIONS.md`, `ARCHITECTURE.md`, `STRUCTURE.md`, `CONVENTIONS.md`, `TESTING.md`, `CONCERNS.md`.
- `CONCERNS.md` enumerates open gaps: MAJOR-1 (scorer prompt unaware of new CCIR tiers), MAJOR-2 (CNR carve-out is dead doc), DRIFT-1 (ccir.md × CCIR_ORDER sync), 9 MINORs, 1 X-FILE.

### Round E — Closed MAJOR-1 (scorer prompt tier enum)
- `score/triage_score.py:score_item` prompt extended with:
  1. A `Tier quick-reference` block listing every tier (PIR-1..6 explicit, FFIR-1..3 explicit, SIR-1, SIR-2, none) with one-line descriptions — including the newest tiers (PIR-6 OSINT/etterforskning, SIR-1 Midtøsten/US-Iran with IRGC/proliferation/atom, SIR-2 VM 2026 sport security lens with CARVE-OUT).
  2. JSON enum widened from `<PIR-1..5 | FFIR-1..3 | none>` to `<PIR-1 | PIR-2 | PIR-3 | PIR-4 | PIR-5 | PIR-6 | FFIR-1 | FFIR-2 | FFIR-3 | SIR-1 | SIR-2 | none>`.
- `py_compile` clean; smoke confirms both the new block and the wider enum are present.

### Round F — Closed DRIFT-1 (×2 patches)
- `score/digest.py`: replaced bare `assert` with explicit `if … : raise AssertionError(...)` so the guard survives `python3 -O`.
- The guard:
  ```python
  _ccir_md_bullets = len(re.findall(r'^\s*-\s+\*\*([A-Z]{3,4}-\d+)\b', open(os.path.join(ROOT, "ccir.md"), encoding="utf-8").read(), re.MULTILINE))
  if len(CCIR_ORDER) != _ccir_md_bullets:
      raise AssertionError(f"CCIR_ORDER has {len(CCIR_ORDER)} entries but ccir.md top-level CCIR bullet count is {_ccir_md_bullets}; update both together.")
  ```
- Both counts in the message (CCIR_ORDER length + ccir.md regex count).
- Smoke: 11 = 11 fresh. Drift injection in `/tmp/drift_test` confirmed the v2 patch correctly raises under `-O`.

---

## Parked / blocked

### Gmail bridge (`bridge/gmail_to_atom.py`) — partial smoke 2026-06-24
- **Smoke 2026-06-24 (this session):** shape probe **PASSES** (length=16, fullmatch `[a-z0-9]{16}` ✓). Real IMAP run **STILL FAILS**: `imaplib.IMAP4.error: b'[AUTHENTICATIONFAILED] Invalid credentials (Failure)'`. Exit 1, no file written to `data/feeds/`.
- **Diagnosis:** password is well-formed but Gmail rejects it. The two most-probable causes (in priority order):
  1. **Account mismatch.** The 16-char app password belongs to a different Gmail account than `GMAIL_USER`. App passwords are per-account.
  2. **Revoked / regenerated.** 2FA rotation revokes all existing app passwords silently.
- **Parked because:** Google's IMAP layer throttles repeated `AUTHENTICATIONFAILED` — cooldown is opaque, 30 min to hours per attempt. If multiple attempts have happened today, cooldown may now be live — wait before retrying.
- **Next move when operator returns:**
  1. Confirm `len=` and shape probe as in Step 2 below; expect PASS.
  2. If Gmail still rejects despite shape PASS: regenerate the app password for **the exact Gmail account matching `GMAIL_USER`** at `myaccount.google.com/security` → "App passwords" → "Other (InfoTriage)". Paste fresh 16-char value into `.env` (lowercase, no spaces).
  3. If the inferred-account hypothesis is confirmed and the password is regenerated correctly, retry — expected outcome: `Wrote N entries -> …data/feeds/gmail.xml`.

### IMAP bridge (`bridge/imap_to_atom.py`)
- `.mailboxes.json` does NOT exist (intentionally — operator-only).
- No smoke yet. Bridge is correct per code; first operational run will be the test.

### YouTube bridge (`bridge/yt_to_atom.py`) — ✅ smoke-passed 2026-06-24
- **Smoke 2026-06-24:** run via inline `YT_CHANNELS` env var with one channel (`https://www.youtube.com/@YouTube`, `max_per_run:3`, `transcribe: false`). Emitted a 9-entry well-formed Atom at `data/feeds/youtube-youtubeofficial.xml`; stub-mode wiring confirmed end-to-end (every entry's `<summary>` reads `(transcription disabled — set transcribe:true to enable; install mlx-whisper or whisper)`).
- Pre-existing conditions still hold: yt-dlp installed (`/opt/homebrew/bin/yt-dlp`, version `2026.06.09`); mlx_whisper / whisper NOT installed (so a future `transcribe: true` will fall back to `(transcription produced no output)`).
- **No parking.** Pipeline works in stub mode out of the box.
- **Operator's next move:** drop a `.yt_channels.json` (gitignored) listing the actual channels of interest — e.g. NRK Urix, VG, or OSINT-relevant YouTube sources. Then `python3 bridge/yt_to_atom.py` runs as a scheduled task end-to-end.

### Abroad-OSINT tools
- World Monitor spike (RESEARCH-REPORT.md Step 0 / Open-Q1) is the first question to resolve before any "adopt World Monitor" decision. Skipped until operator returns.

---

## Outstanding / next-session pickups

(Picked up from the prior session's `suggest_followups` and surfaced by CONCERNS.md / reviewer.)

1. **Few-shot examples per new tier + SAMPLE extension.** Soft-closes the MAJOR-1 semantic gap (overlapping-tier disambiguation). Add 1–2 examples per `PIR-6`, `SIR-1`, `SIR-2` inside `score/triage_score.py:score_item`; extend `SAMPLE` fixture so `--sample` exercises the new tiers.
2. **Reconcile `bridge/RSS_BRIDGE_NOTES.md` drift.** README references a doc that does not exist; either create the how-to (CSS-selector / XPathBridge / known quirks for Forsvarets forum, FFI, NUPI, UTSYN, High North News), or fix the README reference.
3. **Enhance DRIFT-1 guard with diff-based failure message.** Compute symmetric diff (`missing_in_order`, `missing_in_ccir`) and surface specific IDs in the AssertionError, so operator doesn't have to re-run regex to learn the discrepancy.
4. **`CCIR_ORDER` re-ordering (CONCERNS.md MINOR-2).** Conventional NATO/US intel hierarchy: PIR → SIR → FFIR. Current: PIR → FFIR → SIR. One-line change in `score/digest.py:CCIR_ORDER`.
5. **Atomic `data/digests/*.md` write.** Write to `.tmp` then `os.replace()` — closes the X-FILE concern in CONCERNS.md about partial writes.
6. **PROFILE alias cleanup (`score/triage_score.py:30`).** Either delete and update the two README references or downgrade + add explanatory comment.
7. **Highest-value tests (TESTING.md):**
   - `tests/test_triage_score.py` — `score_item` enum-table (ccir/cnr/score → bucket).
   - `tests/test_write_bluf.py` — credential-leak guard (simulate `Exception("GMAIL_APP_PASSWORD=abcd")` and assert markdown doesn't contain `abcd` or `GMAIL_APP_PASSWORD`).
   - `tests/test_opml_roundtrip.py` — write/re-parse/feed-count check.

---

## Decisions reaffirmed this session

(Repeatedly referenced; do not regress these in the next session.)

- **ADR-004 (hard):** every LLM call in the runtime pipeline must be local qwen3.6. No cloud LLM at runtime. Cloud models only used by the assistant during design.
- **Read-only posture against sources.** IMAP `readonly=True`; no STORE / no EXPUNGE.
- **Secrets hygiene.** `.env`, `.mailboxes.json`, `.yt_channels.json` are plaintext (gitignored). Never echo credential characters; length-only / shape-only probes; redact 16-char alphanumeric in error output.
- **Stdlib-first.** Only declared dep is `feedgen>=1.0` (currently unused in production — bridges hand-write Atom).
- **Norwegian-first user-facing prose**; bilingual headings (`## PIR — Priority Intelligence Requirements`).
- **No cloud services.** Self-hosted FreshRSS, rss-bridge, feeds server, oMLX/Ollama, Postgres (planned).
- **CCIR is the brain.** `ccir.md` is the authoritative taxonomy. The scorer reads it; the renderer (`CCIR_ORDER`) mirrors it (now drift-guarded).

---

## Files modified this session

- `score/digest.py` — added `write_bluf`, `CCIR_ORDER` extended to 11 entries, BLUF CLI flag, drift guard.
- `score/triage_score.py` — `score_item` prompt extended with Tier quick-reference + 12-ID enum.
- `opml/feeds.opml` — 11 new feeds (+3 Ukraine, +4 SIR-1, +4 SIR-2). 50 → 61 visible.
- `ccir.md` — new `## SIR` section, NB carve-out note in CNR Routine.

## Files created this session

- `.planning/codebase/STACK.md`
- `.planning/codebase/INTEGRATIONS.md`
- `.planning/codebase/ARCHITECTURE.md`
- `.planning/codebase/STRUCTURE.md`
- `.planning/codebase/CONVENTIONS.md`
- `.planning/codebase/TESTING.md`
- `.planning/codebase/CONCERNS.md`
- `.continue-here.md` (this file)

---

## Resume instructions

When you reopen this project, here is the suggested order of operations to pick up cleanly:

1. **Smoke the closure state first.**
   ```bash
   for f in score/digest.py score/triage_score.py score/fever_triage.py \
            bridge/gmail_to_atom.py bridge/imap_to_atom.py bridge/yt_to_atom.py; do
       python3 -m py_compile "$f" || break
   done
   ```
   All should exit 0. If any fails, read the error — likely from out-of-band edits.

2. **Probe the Gmail bridge axis** (only if operator's `.env` says they're ready to retry):
   ```bash
   unset GMAIL_APP_PASSWORD
   python3 << 'PYEOF'
   import os, imaplib, re
   def ld(p):
       if os.path.exists(p):
           for line in open(p):
               line=line.strip()
               if line and not line.startswith("#") and "=" in line and not line.startswith("["):
                   k,v=line.split("=",1); os.environ.setdefault(k.strip(), v.strip())
   ld(".env")
   v = os.environ.get("GMAIL_APP_PASSWORD","")
   print("len=", len(v), "shape_ok=", bool(v and v.isalnum() and v.islower()))
   # No string echo.
   PYEOF
   ```
   If `shape_ok` is True and the password is 16 chars, the bridge should now work. Run `python3 bridge/gmail_to_atom.py` and expect: `Wrote N entries -> /Users/vidarbrevik/projects/InfoTriage/data/feeds/gmail.xml`. Then subscribe `http://feeds/gmail.xml` in FreshRSS.

3. **Pick exactly one outstanding item** (suggested priority):
   - **Few-shot examples per new tier** (closes MAJOR-1 semantically; cheap; high signal)
   - **Diff in DRIFT-1 guard failure message** (operator-friendly enhancement; cheap)
   - **Re-order CCIR_ORDER PIR → SIR → FFIR** (MINOR; one-line change in `score/digest.py`)
   - **Reconcile `bridge/RSS_BRIDGE_NOTES.md`** (doc hygiene)
   Avoid doing more than one in a single session unless they're tightly coupled.

4. **Verify the new pick end-to-end.** Load the `score/triage_score.py:score_item` source if you changed the prompt; confirm `ccir.md`'s bullet count matches `len(CCIR_ORDER)`; if a test exists, run it.

5. **Update CONCERNS.md** to reflect the new state (move MINOR-1 / MAJOR-1 / etc. out of the open list once closed). This is the in-codebase record; keep it honest.

6. **At end of session:** rerun `gsd-pause-work` to refresh this `.continue-here.md`. Or, if the project is closed out, archive `data/`, `docker compose down`, and commit a final WIP if a git repo is initialised.

---

## Quick reference — where things live

- **Triage brain:** `ccir.md` (4 sections: PIR/FFIR/SIR/CNR)
- **Scorer / windowed digest:** `score/{triage_score,fever_triage,digest}.py`
- **Bridges:** `bridge/{gmail_to_atom,imap_to_atom,yt_to_atom}.py`
- **Long-form docs:** `docs/ARCHITECTURE.md` (ADRs 001..004), `docs/RESEARCH-REPORT.md` (23-source verification)
- **Codebase map (this session's deliverable):** `.planning/codebase/`
- **Project framing:** `.planning/PROJECT.md` (status, hard constraints, north-star benchmark)
- **Requirements:** `.planning/REQUIREMENTS.md`
- **This handoff:** `.continue-here.md`
