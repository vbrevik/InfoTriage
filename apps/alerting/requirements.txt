# alerting worker third-party runtime dependencies
# Transitive deps of the local libs (contracts, store) are listed here because
# those libs are installed with --no-deps in the Dockerfile.
aio-pika>=9.6
psycopg[binary]>=3.3
pgvector>=0.4.2
# store/__init__.py imports _atom.render_atom at module level, which imports
# feedgen.feed.FeedGenerator — required even though alerting_worker.py never
# calls render_atom itself (same pattern as apps/triage/apps/wiki).
feedgen>=0.3.1
# contracts (pydantic models) and store (yaml-based config) import these at
# module level too — both already vetted deps.
pydantic>=2.0
# contracts.setup_logging is invoked at module level; json-log-formatter is a
# contracts runtime dep that's stripped by --no-deps in the Dockerfile.
json-log-formatter>=1.1
PyYAML>=6.0
# outbox.py's NtfyClient uses httpx.AsyncClient for the authenticated ntfy
# POST — already used by five sibling services (opml_health, ingest-gmail,
# ingest-barentswatch, dlq_consumer, scheduler).
httpx>=0.25
