# InfoTriage scheduler — APScheduler 3.x + sync httpx (D-04, RESEARCH Finding 3)
# APScheduler 4.x is pre-release; 3.x is latest stable on PyPI.
# httpx.Client (SYNC) only — APScheduler 3.x jobs run in threads, no event loop.
apscheduler>=3.11,<4
httpx>=0.27
# contracts.setup_logging is invoked at module level; json-log-formatter is a
# contracts runtime dep that's stripped by --no-deps in the Dockerfile. (Phase 7)
json-log-formatter>=1.1
# contracts.__init__ eagerly imports ._events (pydantic BaseModel FeedUnhealthy),
# ._codec (PyYAML frontmatter), and ._bus_rabbitmq (aio_pika). Even though the
# scheduler never calls into any of those, the eager `from contracts import
# setup_logging` brings the whole package in. The Dockerfile installs contracts
# with --no-deps so the transitive deps do not propagate automatically —
# each one is hand-listed here. (Phase 7 07-03 runtime-deps follow-up.)
pydantic>=2.0
PyYAML>=6.0
aio-pika>=9.6
