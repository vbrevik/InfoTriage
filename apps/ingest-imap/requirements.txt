# ingest-imap third-party runtime dependencies
# Transitive deps of the local libs (contracts, store, ingest_common) are listed
# here because those libs are installed with --no-deps in the Dockerfile (D-10).
fastapi>=0.115
uvicorn>=0.30
aio-pika>=9.6
psycopg[binary]>=3.3
pgvector>=0.4.2
numpy>=1.24
pydantic>=2.0
PyYAML>=6.0
httpx>=0.27
feedgen>=0.3.1
# contracts.setup_logging is invoked at module level; json-log-formatter is a
# contracts runtime dep that's stripped by --no-deps in the Dockerfile. (Phase 7)
json-log-formatter>=1.1
